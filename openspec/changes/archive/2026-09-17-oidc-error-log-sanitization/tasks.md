## 1. Dependency and sanitization module

- [x] 1.0 Add `"oauth4webapi": "3.8.6"` to `packages/backend/package.json`'s `dependencies` (design D8) — an exact pin, declared explicitly rather than relying on it resolving only via hoisting through `openid-client`. Run an install to update the lockfile; confirm the resolved version doesn't change (it shouldn't — this is a declaration of an already-resolved transitive dependency, not a version bump).
- [x] 1.1 Create `packages/backend/src/auth/oidc-error-sanitizer.ts` exporting `sanitizeOidcError(err: unknown, log: FastifyBaseLogger): Record<string, unknown>`. Import `OperationProcessingError` from `oauth4webapi` directly (now a direct dependency per 1.0), not via `openid-client`.
- [x] 1.2 Implement the `ResponseBodyError | AuthorizationResponseError` branch (design D3, case 1a): allowlist `errorClass`, `message`, `stack`, `code`, `error`; `error_description` and `cause` replaced with `"[redacted]"`.
- [x] 1.2a Implement the `WWWAuthenticateChallengeError` branch (design D3, case 1b) separately from 1.2: allowlist `errorClass`, `message`, `stack`, `code`; `cause` replaced with `"[redacted]"`. Do **not** include `error`/`error_description` keys at all for this class — it has neither field (verified against `oauth4webapi` source; see design D3's 1a/1b split), so there is nothing to redact and no redaction marker to add.
- [x] 1.3 Implement the `OperationProcessingError | ClientError` branch (design D3, case 2): allowlist `errorClass`, `message`, `stack`, `code`; `cause` replaced with `"[redacted]"` explicitly (do not rely on native `cause` non-enumerability).
- [x] 1.4 Implement the `MissingClaimError` branch (design D3, case 3): allowlist `errorClass`, `message`, `stack`, `claim` — preserve this app's existing claim-name-only discipline.
- [x] 1.5 Implement the fail-closed default branch (design D3, cases 4/5) for any other `Error` or non-`Error` thrown value: redact `message` and `stack`, set `unrecognized: true`, and have the wrapper itself emit a separate `log.error({ event: "oidc_error_sanitizer.unrecognized_class", errorClass })` call.
- [x] 1.6 Add the onboarding code comment at the top of the module documenting: (a) the enumerability invariant from `exploration-notes.md` Finding 2 (native `Error(message, { cause })` vs. plain `this.cause = x` assignment); (b) why the class-name field is named `errorClass` and not `type` — pino's default `err` serializer unconditionally recomputes a `type` key from `err.constructor.name` (always `"Object"` for a plain object literal), silently clobbering a field named `type` (design D3); (c) that `WWWAuthenticateChallengeError` (case 1b) genuinely has no `error`/`error_description` fields — not an oversight if a future reader goes looking for them; (d) a checklist for what to verify before trusting a new error class or a second IdP's client library; (e) that the wrapper's own `unrecognized_class` signal line and the call site's normal `log.error(...)` using the wrapper's return value are two separate, both-expected log lines per invocation — not duplicate logging to be "cleaned up."

## 2. Call-site integration

- [x] 2.1 `packages/backend/src/routes/auth.ts` `/auth/callback` catch block (~line 334): replace the raw `err` passed to `request.log.error` with `sanitizeOidcError(err, request.log)`.
- [x] 2.2 `packages/backend/src/auth/middleware.ts` `refreshSessionTokens` catch block (~line 103):
  - [x] 2.2a Fix `isRevocation` (design D10): replace the `err.message.includes("invalid_grant")` / `err.code === "invalid_grant"` check with `err instanceof ResponseBodyError && err.error === "invalid_grant"`. This must run against the *original* error, not the sanitized object — sanitization happens only at the log boundary, never before revocation-detection logic that needs the real values. **Note (architect review):** unlike 2.2b, this sub-task has no dependency on section 1 — it's a pure logic correction against `ResponseBodyError`, already reachable via `openid-client` today, and never touches `sanitizeOidcError`. It's grouped under 2.2 because it shares this catch block and the D9/D10 design discussion with 2.2b, not because it's gated on section 1; it can be implemented and landed independently (e.g. to parallelize work) if useful.
  - [x] 2.2b Add a new `log.error({ err: sanitizeOidcError(err, log), userId, sessionId, source, event: "auth.token_refresh_error", retryCount: retries }, ...)` call immediately after the retry loop exhausts, before the `transient_failure` audit event and return (design D9) — this call site had no error logging at all before this change; this is a deliberate new log line, not a routing of an existing one.
- [x] 2.3 `packages/backend/src/routes/auth.ts` `/auth/logout` handler (~line 426): add a `try/catch` around `getEndSessionUrl(idToken, getAppOrigin())` (design D4). On error: log via `sanitizeOidcError`, emit a new `auth.idp_logout_failed` audit event (`userId`, `sessionId` — design D4 addendum), then fall back to `return reply.send({ redirectUrl: "/" })`, matching the existing fallback shape used earlier in the same handler.

## 3. Regression tests

- [x] 3.1 Create `packages/backend/src/auth/__tests__/oidc-error-sanitizer.test.ts`.
- [x] 3.2 For `ResponseBodyError`, `AuthorizationResponseError`, `WWWAuthenticateChallengeError`, and `ClientError`: construct a real instance from the installed `oauth4webapi`/`openid-client` packages with a canary string (e.g. `CANARY_TOKEN_<uuid>`) injected into every `cause`/`error_description`-bearing constructor argument.
  - **Note (design review §4):** `AuthorizationResponseError`'s `cause` constructor argument must be `URLSearchParams`-like — the constructor calls `.get('error')`/`.get('error_description')` on it directly. A plain object literal (e.g. `{ error_description: "CANARY_..." }`) throws a `TypeError` at construction time rather than producing a testable instance. Construct it with an actual `URLSearchParams` instance.
  - **`ClientError`** (BA review finding 1): unlike `OperationProcessingError`, this class is directly and trivially constructible — `openid-client` exports it and its constructor is `new ClientError(message, { code, cause })` (native `Error(message, options)` form, same non-enumerable-`cause` mechanism as `OperationProcessingError`). Construct one with the canary in `cause` and run it through the sanitizer, asserting it lands in the same case-2 branch/allowlist as `OperationProcessingError` (design D3 case 2). This gives `ClientError` its own regression case rather than leaving it covered only by inference from `OperationProcessingError`'s test.
  - For `OperationProcessingError`: do **not** construct it arbitrarily. Instead, exercise the actual `getEndSessionUrl()` → `buildEndSessionUrl()`-reachable path (design D8) — call `getEndSessionUrl()` directly from the test file (not through the `/auth/logout` route handler; this test therefore has no dependency on task 2.3, which wraps only the *handler's* call site, not the `getEndSessionUrl()` function itself, per architect review) — simulate discovered issuer metadata with a missing or malformed `end_session_endpoint` and confirm the real, unwrapped `OperationProcessingError` this throws is what the sanitizer's `instanceof` check matches, with a canary value where feasible. This proves the call site that actually needs this works, not just that the class exists somewhere in the test file.
- [x] 3.3 Log each sanitized result through a real `pino()` instance configured the same way `app.ts` configures its logger; capture and `JSON.parse` the emitted line.
- [x] 3.4 Assert the canary string does not appear anywhere in the serialized output text (not just the parsed object's known fields — guard against it leaking into an unexpected key).
- [x] 3.5 Assert the allowlisted fields (`errorClass`, `message`, `code`, `error` where applicable) are present with their real, non-redacted values in the final serialized JSON output (not the wrapper's intermediate return value — per design D3, `errorClass` is named to survive pino's default `err` serializer's `type` recomputation; asserting against final JSON is what actually catches a regression here, consistent with D6).
- [x] 3.5a Add a case for `WWWAuthenticateChallengeError` (design D3 case 1b) specifically asserting `error` and `error_description` keys are **absent** from the sanitized output (not present-and-redacted) — this class has neither field on the real instance.
- [x] 3.5b Assert the positive redaction-marker case (BA review finding 2): for case 1a (`ResponseBodyError`/`AuthorizationResponseError`), assert `error_description` and `cause` are present in the final serialized JSON with the literal value `"[redacted]"`; for case 2 (`OperationProcessingError`/`ClientError`), assert `cause` is present with the literal value `"[redacted]"`. This proves the field is masked, not deleted — 3.4 only proves the canary is gone from the output, and an implementation that `delete`s the field instead of setting it to `"[redacted]"` would currently pass 3.4 and 3.5 without this assertion catching it.
- [x] 3.6 Add a case constructing an unrecognized `Error` subclass; assert the `unrecognized: true` shape and the separate `oidc_error_sanitizer.unrecognized_class` log line.
- [x] 3.6a Add a case throwing a plain non-`Error` thrown value (e.g. a bare string or plain object literal), not just an unrecognized `Error` subclass (design D3 case 5, BA review finding 3): assert `errorClass` reflects `typeof err` (distinct from 3.6's `err.constructor.name` derivation) and the `unrecognized: true` shape, with the same `oidc_error_sanitizer.unrecognized_class` log line.
- [x] 3.7 Add a case for `MissingClaimError`; assert `claim` is present and no other field beyond the case-3 allowlist appears.

## 4. User-facing page verification

- [x] 4.1 Add or extend a test that exercises the actual `/auth/callback` error-redirect path (not just unit-testing `mapAuthError()` in isolation) with a `ResponseBodyError`/`AuthorizationResponseError` carrying a canary `error_description`, and assert the redirect URL's `message` query parameter does not contain the canary value, both before and after this change's sanitization is wired in — confirming sanitization of the log has zero effect on the user-facing error categorization path.
- [x] 4.2 Add a test that exercises the actual `/auth/logout` handler with `getEndSessionUrl()` mocked to throw, asserting: (a) the response falls back to `{ redirectUrl: "/" }` rather than propagating an uncaught error, (b) the local session is already destroyed before that fallback response is returned, (c) the error is routed through `sanitizeOidcError` before being logged, (d) an `auth.idp_logout_failed` audit event is emitted with `userId` and `sessionId` (design D4 addendum), distinct from the earlier `auth.session_invalidated` event. This makes the behavior change documented in design D4 a CI-enforced condition rather than only a one-time manual check (see 5.1).
- [x] 4.3 Add a test for `middleware.ts`'s `refreshSessionTokens` (design D9/D10): (a) construct a real `ResponseBodyError` with `.error === "invalid_grant"` and assert it's classified as `status: "revoked"` with `failureType: "revoked"` in the audit event, with no retry delay; (b) construct an unrecognized error and assert the retry loop still exhausts, `failureType: "transient"` is audited as before, and the new `log.error` (2.2b) fires exactly once, routed through `sanitizeOidcError`.

## 5. End-to-end validation walkthrough

- [x] 5.1 Manually trigger a realistic failure (e.g. an expired or reused authorization code) against a test IdP. Confirm in one pass: (a) the log gives an operator enough to identify the failure category, (b) the user sees a sane error page, (c) nothing sensitive appears in either. Record the outcome in the PR description.
  - **Done (2026-09-17):** Ran against the real local OIDC stub (`docker/oidc`, `oidc-provider`) with Postgres/Redis up via `docker compose`. Completed one full `/auth/login` → auto-approved authorization (`loginHint=participant-001`) → `/auth/callback` round trip to obtain a real, once-used authorization code, then replayed that same code against a freshly-seeded Redis state entry to force the token endpoint to reject it. Result, captured from the running backend's actual log output:
    `{"errorClass":"ResponseBodyError","code":"OAUTH_RESPONSE_BODY_ERROR","error":"invalid_request","error_description":"[redacted]","cause":"[redacted]","stack":"ResponseBodyError: server responded with an error in the response body\n    at checkOAuthBodyError (.../oauth4webapi/...)...","event":"auth.callback_error","correlationId":"ff00fdff-d1b2-402e-ab5b-b4b7e2452eb0"}`
    (a) Operator triage: `errorClass`/`code`/`error` identify this as a real OAuth-level rejection from the token endpoint (a reused/invalid grant), `stack` shows the real call path (`checkOAuthBodyError` → `authorizationCodeGrant` → `handleCallback` → the `/auth/callback` route), and `correlationId` ties it to the user-facing redirect — enough to diagnose the failure category without needing the raw IdP text.
    (b) User-facing redirect: `http://localhost:5173/auth/error?category=authentication_failed&message=An%20unexpected%20error%20occurred%20during%20sign-in.%20Please%20try%20again...&correlationId=ff00fdff-...` — generic, safe message, unaffected by sanitization (confirms task 4.1's assertion holds against a real IdP too, not just the synthetic canary tests).
    (c) Nothing sensitive in either: `error_description` and `cause` both show the literal `"[redacted]"` marker in the log (not the IdP's actual descriptive text); the redirect message is the fixed generic string. This is the same class this change's Finding 2 identified as actively leaking prior to this change.

## 6. Follow-up scoping (not implemented in this change)

Recommended (per executive review): file each of 6.1–6.3 as a tracked GitHub issue
(`gh issue create`) once implementation lands, rather than leaving them only as
PR-description prose — a note in a merged PR is where follow-through tends to die. The
PR description should at minimum summarize them and link the issues once filed.

- [x] 6.1 File a tracked follow-up (GitHub issue preferred; PR description at minimum) naming the deferred, non-blocking decision: debug-level capture of `error_description`, pending sign-off from whoever owns log retention/access policy. **Filed: [#121](https://github.com/surratt-dev/project-dipstick/issues/121)**
- [x] 6.2 File a tracked follow-up (GitHub issue preferred; PR description at minimum) naming the deferred task for whichever future change onboards a second OIDC provider: re-verify this sanitization mechanism against that provider's actual error responses (design D7) — the canary tests in this change use synthetic errors, not a second real provider's output. **Filed: [#122](https://github.com/surratt-dev/project-dipstick/issues/122)**
- [x] 6.3 File a tracked follow-up (GitHub issue preferred; PR description at minimum) to whoever owns dependency policy that `openid-client`'s `^6.8.4` caret range, and `openid-client`'s own `^3.8.5` caret range on `oauth4webapi` (now a direct dependency of this package per design D8, but still caret-ranged one level up), both allow a minor/patch bump to change error-shape behavior with no change-review trigger on `package.json` alone — the regression tests in this change are the safety net, not a substitute for reviewing that flag. **Filed: [#123](https://github.com/surratt-dev/project-dipstick/issues/123)**
- [x] 6.4 Note in the PR description that final sign-off on the field allowlist (design D3) from whoever owns log policy is explicitly non-blocking: this change ships with D3's allowlist as the default, and does not wait on that sign-off to merge. If sign-off later requires changes, that's a fast-follow amendment to the allowlist, not a gate on this change.
