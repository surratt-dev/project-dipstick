## 1. Prerequisite verification

- [x] 1.1 **Formal merge dependency:** `fix-default-topic-seed-data` (stub filed at `openspec/changes/fix-default-topic-seed-data/`) must be proposed, implemented, and merged before this change merges. It replaces `4_seed_data.sql`'s six default topics with the twelve AC-verbatim topics from `requirements/use cases/08 - Topic Management - Use Cases.md`, plus its own regression test pinning the seeded prompts. If it is not yet a real proposal when this change is picked up, whoever picks up this change files it first — do not fold its migration work into this change, and do not merge this change ahead of it. This is enforced, not just tracked: task 4.4 below adds a real-data integration test that fails if the prerequisite hasn't landed, so "confirm it landed" is a test result, not a manual check. — Resolved: filed and run as GitHub issue #161, light-track pipeline, merged to main as migration `11_default_topics_correction.sql` (PR #162, squash-merged). Task 4.4's real-data test now passes against the corrected twelve-topic seed.

## 2. Database migrations (this change's own)

- [x] 2.1 Add a new migration creating `CREATE UNIQUE INDEX teams_name_unique_normalized ON teams (lower(btrim(name)))`, with a down migration that drops only this index. Confirm the next available migration number at implementation time — do not assume `11`; `fix-default-topic-seed-data` (task 1.1's prerequisite) also claims a new migration number and this change is blocked on it merging first (design.md D4). — Originally implemented as `11_teams_name_unique_normalized.sql` (11 was unclaimed at implementation time); renumbered to `12_teams_name_unique_normalized.sql` per 2.1a below once the prerequisite claimed 11. Applied and verified against real Postgres.
- [x] 2.1a **Pre-merge re-verification, not just implementation-time.** Immediately before merging this change (not only when 2.1 was first implemented), re-confirm the migration number chosen in 2.1 is still unclaimed on `main`. If `fix-default-topic-seed-data` merged its own migration in the interim and claimed the same number, renumber this migration before merging. Task 1.1 only guarantees the prerequisite merges *before* this change — if both are developed on parallel branches, the number picked at implementation time can be claimed by the prerequisite later, after this task was written but before this change merges. This closes that window the same way 4.4 turned the seed-data dependency from a prose caveat into an enforced check. — Triggered exactly as anticipated: `fix-default-topic-seed-data` merged to main as `11_default_topics_correction.sql` (PR #162) while this migration was still `11_teams_name_unique_normalized.sql` on an unmerged branch. Renumbered to `12_teams_name_unique_normalized.sql` and re-applied cleanly against real Postgres.
- [x] 2.2 Verify the existing `teams_name_unique` constraint (`2_create_tables.sql:23`) is left untouched. — Confirmed via `\d teams` against real Postgres after migration 12: both `teams_name_unique` and `teams_name_unique_normalized` coexist.

## 3. Backend: `POST /api/v1/teams`

- [x] 3.1 Implement the route in `facilitator-sessions.ts`, alongside `POST /draft` — not in `teams.ts` (that file is the existing TEAM-005/TEAM-006 admin/role-management route file, an unrelated domain; see design.md D3). Note the choice in a code comment referencing this change.
- [x] 3.2 Enforce `users.global_role === 'facilitator'` (403 otherwise), matching the existing `POST /draft` check pattern. This is the first check in the handler, ahead of name validation and uniqueness (see design.md D8 and the spec's check-ordering requirement) — do not reorder behind a shared "validate the request body" helper or equivalent refactor.
- [x] 3.2a On the `403` rejection, write an audit log row (`team.creation_denied_role`) identifying the actor, actor role, and actor IP (design.md D3, security review F1).
- [x] 3.3 Validate the submitted team name is non-empty after trimming; return a validation error otherwise. Runs after 3.2, before 3.4.
- [x] 3.3a Add `TeamNameCollisionResponse { errorState: "team_name_collision"; providedName: string }` to `@dipstick/shared`, alongside `SessionAlreadyExistsResponse` and `RevealFailureResponse` (design.md D2). Has no dependency on anything else in this section — do this before 3.4, which consumes the type.
- [x] 3.4 Implement the normalized-name pre-check (lowercase + trim comparison) for a fast, clear inline error, then rely on the `teams_name_unique_normalized` index as the authoritative backstop. Catch the `23505` violation on the `teams` INSERT by `err.code === "23505"` alone (not by a single `err.constraint` match) — both `teams_name_unique` and `teams_name_unique_normalized` are live on that INSERT, and an exact-duplicate-name race can report either constraint name nondeterministically (design.md D4, engineer review Finding 1). Translate to the typed `TeamNameCollisionResponse` (3.3a, design.md D2), not a message string.
- [x] 3.5 Implement the single transaction: `INSERT INTO teams` → default-topic copy (4.1 — implemented alongside this task as part of the same transaction, not strictly after it despite the section ordering below) → `INSERT INTO sessions` with `status = 'lobby'`, `is_first_session = true`, `session_number = 1` → audit log row → `COMMIT`; roll back entirely on any failure.
- [x] 3.6 Generate the join link as part of the same request (mirroring `POST /draft`'s `join_token` generation).
- [x] 3.7 Write the audit log row identifying actor, actor role, actor IP, new team id, and new session id, matching the `POST /draft`-established pattern; name the new `AuditEventName` value `team.created_with_session`.
- [x] 3.8 Confirm no `team_memberships` row is inserted for the creating facilitator anywhere in this handler.

## 4. Backend: default topic provisioning

- [x] 4.1 Implement the topic-copy step as `INSERT INTO topics (...) SELECT ... FROM topics WHERE team_id = '00000000-0000-0000-0000-000000000001' AND is_default = true`, run inside the Section 3 transaction, preserving `display_order`.
- [x] 4.2 Confirm no "locked" column or flag is written anywhere in this step.
- [x] 4.3 Write an integration test using a test-local fixture asserting the copied topic set matches the twelve-topic canonical shape — independent of whatever is actually seeded in the test database. Assert the full field set the spec requires copied unmodified (`name`, `prompt`, `vote_type`, `display_order`, `is_default`, `first_session_description`), not just count and prompt text — 4.4 below checks the full field set too, but only against this change's own seeded content; nothing else asserts the copy *mechanism* itself preserves every field against an arbitrary fixture. This test covers "the copy mechanism is correct" only; it cannot detect a stale seed (see 4.4).
- [x] 4.4 **Real-data integration test — the actual prerequisite merge gate.** Separate from 4.3, write a test that queries the *actually seeded* `topics` rows against the real migrated test database (`SELECT name, prompt, vote_type, display_order, first_session_description FROM topics WHERE team_id = '00000000-0000-0000-0000-000000000001' AND is_default = true ORDER BY display_order`) and asserts twelve rows matching, verbatim, the twelve prompts enumerated in `requirements/use cases/08 - Topic Management - Use Cases.md`'s Acceptance Criteria (count, prompt text, vote type, order). This test fails against the current six-topic seed by design — it is what actually enforces task 1.1's merge dependency on `fix-default-topic-seed-data`, rather than relying on someone reading tasks.md carefully. — Implemented in `packages/backend/src/routes/__tests__/default-topic-provisioning-integration.test.ts`. Initially FAILED as expected (6 rows found, 12 asserted) before `fix-default-topic-seed-data` landed — confirming the gate was real, not just documented. Now PASSES against real Postgres following `fix-default-topic-seed-data`'s merge (PR #162, migration `11_default_topics_correction.sql`).
- [x] 4.5 Write an integration test asserting the copy is a real, independent row, not a reference (design.md D5; spec scenarios "Editing a team's copied topics does not affect the canonical defaults or any other team" and "A later change to the canonical defaults does not retroactively affect an already-created team"). Two assertions: (a) after creating a team and mutating/deleting one of its copied topics, confirm the sentinel `__default_topics__` team's source rows are unchanged; (b) after creating a team, mutate a sentinel source row, then confirm the already-created team's previously-copied topic is unaffected. The `INSERT ... SELECT` copy mechanism (4.1) guarantees this by construction — there is no FK from a team's topics back to the sentinel rows — so this test exists to catch a future refactor (e.g., replacing the copy with a lazy-materialized reference for performance) that would silently break that guarantee, not because the current mechanism is in doubt.

## 5. Frontend: new-team screen

- [x] 5.1 Extend `SessionCreationPage.tsx`'s `Screen` union with a `{ name: "new-team" }` variant.
- [x] 5.2 Add the "Create a new team" affordance to the picker screen, transitioning to the new-team screen.
- [x] 5.3 Build the new-team form: team name input, inline validation error display (empty name, name collision). Use a dedicated `newTeamError: { kind: "empty_name" } | { kind: "name_collision"; providedName: string }` state, separate from the existing-team confirm screen's `ConfirmError` union — the two screens' error vocabularies are unrelated (design.md D2). Reset `newTeamError` on every transition into or out of the new-team screen.
- [x] 5.4 Implement the submit control's name-echo label (e.g., "Create team '<name>' and open session room"), updating live as the Facilitator types.
- [x] 5.5 Implement "back to picker" navigation from the new-team screen as a pure client-side state transition (no request fired).
- [x] 5.6 On successful submission, navigate directly to the new session's live participant-readiness view (not a draft control view).
- [x] 5.7 Add the new-team-specific landing acknowledgment copy ("Team created. Default topics assigned.") to the session room view, scoped so it doesn't alter the existing-team landing copy. Revised post-sync-verification: dropped the original "Share the link below to get started" clause — the live-readiness-view doesn't render a join link (a pre-existing gap shared by the existing-team flow, tracked separately as a participant-readiness-view follow-up), so the copy shouldn't promise one.

## 6. Frontend: empty-state copy

- [x] 6.1 Update the picker's empty eligible-teams-list message to invite new-team creation rather than stating only that no teams are available.
- [x] 6.2 Confirm the "create a new team" affordance remains reachable from the empty-list state.

## 7. Tests

- [x] 7.1 Backend integration tests for `POST /api/v1/teams`: success path, non-facilitator rejection (asserting the `team.creation_denied_role` audit row per design.md D3/F1), non-facilitator rejection using an already-taken team name — asserting the response (status and body) is identical to the non-facilitator rejection with a fresh, non-colliding name, per design.md D8 and the spec's "a non-facilitator caller cannot probe name existence" scenario: no signal of collision may leak pre-authorization — empty-name rejection, exact-duplicate-name rejection, case/whitespace-variant-duplicate rejection, concurrent-duplicate-name race with **identical** strings (exercises the exact-match-constraint path, design.md D4 Finding 1 — not just the case/whitespace-variant race), concurrent-duplicate-name race with case/whitespace variants (only one succeeds), transaction-rollback-on-failure, success-path audit row written, no membership row created.

  **Security-critical:** the "no membership row created" assertion must carry an inline comment naming design.md D6 and the facilitator-from-another-team constraint it protects — this is a security regression test, not an ordinary functional assertion, and must not be weakened or dropped in a future refactor without that being a visible, deliberate decision (security review Finding F2).
- [x] 7.2 Frontend tests for the new-team screen: happy path, landing-acknowledgment copy is asserted on arrival at the participant-readiness view (5.7) — named as its own assertion, not folded silently into "happy path", back-navigation safety (no request fired, and `newTeamError`/`confirmError` both cleared — design.md D2), name-echo label updates with input, empty/duplicate-name inline errors, empty-state copy and affordance.
- [x] 7.3 Run the full existing `session-creation` and `default-topic-provisioning` (new) test suites to confirm no regression to the existing-team flow.

## 8. Documentation: use-case addendum (in this change's own scope, gated on this change merging)

- [x] 8.1 Apply the following addendum to "Create Session for a New Team" in `requirements/use cases/02 - Session Setup - Use Cases.md`, matching the pattern the existing-team use case already established with its own "Addendum (draft-landing decision)." Land this in the same PR wave that merges `inline-team-creation` — not as a separate tracked follow-up.

  **Main Flow** — append after step 10:

  > **Addendum (draft-skip decision):**
  >
  > 11. Unlike the existing-team flow (see "Create Session for Existing Team," Addendum: draft-landing decision), this flow does not pass through `draft` status. A brand-new team has no prior team context for the Facilitator to review before opening the room, so the review-gate rationale that motivates the existing-team flow's draft/lobby split does not apply here. The application lands the Facilitator directly in `lobby` (step 10 above), with an inline name-echo confirmation on the submit control ("Create team '\<name>' and open session room") taking the place of the draft gate as the point where the irreversible action — team creation — is confirmed before it happens.

  **Postconditions** — replace the existing "Success" line with:

  > - **Success:** A new team record exists with the provided name and the default topic set assigned. A session record exists for that team, associated with the Facilitator, in `lobby` status (not `draft` — see Main Flow Addendum: draft-skip decision). A unique join link has been generated and is immediately usable. The Facilitator lands on the participant readiness view, with an on-screen acknowledgment that the team was created and its default topics were assigned.
  > - **Failure:** No team or session record is created. The application state is unchanged.

  If the implementer finds the landing-acknowledgment copy or the name-echo wording diverges from what's finally shipped (per design.md D2), update this addendum's illustrative quotes to match the shipped copy rather than leaving stale examples.
