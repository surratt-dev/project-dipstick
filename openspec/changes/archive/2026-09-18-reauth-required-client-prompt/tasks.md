## 1. Vote-Loss Disclosure Gate (Prerequisite — Blocks Copy Finalization)

**No code dependencies. Blocks task 3.1's copy finalization (design.md Decision D5). Must be re-checked, not assumed, at the actual time this change is implemented, since the compose UI's ship date is the variable. (Task 3.6, the copy-side half of this gate, is scoped here but sequenced after Group 2's extraction and Group 3's copy land — see Group 3's header. Per architecture review, this keeps Group 1 free of a forward reference into an artifact that doesn't exist yet.)**

- [x] 1.1 **Build-enforced, not a recorded human grep (per Tomás Ferreira's security review — "don't rely on developer discipline"):** add an automated test (mirroring task 4.12's/`connectionHealth.grep.test.ts`'s source-grep pattern) that asserts no vote-compose UI component in the current codebase imports and calls `voteDraft.ts`'s (`packages/frontend/src/realtime/voteDraft.ts`) persist/restore hooks. This is the single fact task 3.1's vote-loss sentence depends on, now checked by CI instead of recorded in a commit message. (The companion check — that the shared copy's vote-loss statement matches this fact — is task 3.6, sequenced after the copy and shared subcomponent exist.)
- [ ] 1.2 If a future codebase change makes 1.1's test fail — a vote-compose UI does import `voteDraft.ts`'s hooks — or task 3.6's test finds the copy's vote-loss statement present/absent inconsistently with that fact, update the copy per design.md Decision D5 and spec.md's "Conditional vote-loss disclosure in the reauth-required treatment" requirement, and adjust 1.1's and/or 3.6's test to match the new baseline.
- [ ] 1.3 **Forward-pointing, not this change's to close, and explicitly manual/discipline-dependent (per Tomás Ferreira's security review):** whichever future change builds the vote-compose UI MUST re-run 1.1's check and, if its result has flipped since this change's own implementation, update this banner's copy accordingly (via 3.6's test and 1.2's process). This step cannot be automated today — the future compose UI's file doesn't exist yet for 1.1's test to check against — so it relies on whoever builds that UI actually noticing and acting on it. **Current limitation, stated honestly (per BA review, Marcus Delgado):** `vote-compose-recovery`'s own tasks.md task 8.3 (`openspec/changes/archive/2026-09-11-vote-compose-recovery/tasks.md:56-59`) and its `spec.md` (`openspec/specs/vote-compose-recovery/spec.md`) do not reference this change or this recheck — verified by reading both directly. The citation here runs one way (this document → that task); it is not yet a two-way link. Today, discovering this recheck still depends on whoever builds the compose UI finding this document (e.g. via a repo-wide search for "reauth-required-client-prompt" or GitHub issue #32, or noticing this task while reading task 8.3's sibling checklist). Task 1.4 closes this gap going forward rather than leaving it as an aspirational claim.
- [x] 1.4 **Close the forward-reference loop (per BA review, Marcus Delgado):** when this change ships, add a short note to `openspec/specs/vote-compose-recovery/spec.md`'s Build status / follow-up section explicitly pointing back to this change's task 1.1/1.3 recheck, so a future implementer working purely from `vote-compose-recovery`'s own artifacts can discover this requirement without independently finding this document. Prefer editing the living `spec.md` (not archived) over the archived `tasks.md` under `openspec/changes/archive/2026-09-11-vote-compose-recovery/` — that directory is a historical record of a completed change and should not be edited after archival. If a pointer inside the archived tasks.md itself is later judged necessary, get explicit sign-off before touching it; do not do so as a side effect of this task.

## 2. Call-to-Action Wiring

**Depends on: nothing. Independent of Group 1/3 — this is mechanism, not copy. (Internally resequenced per architecture review: the D9 extraction must run before the tasks that build inside it, so it is listed first as 2.1.)**

- [x] 2.1 **Extraction (design.md Decision D9; resolves engineer design-review Finding 1):** create a new shared subcomponent (e.g. `packages/frontend/src/components/ReauthRequiredTreatment.tsx`) and wire both `ConnectionStatusBanner.tsx`'s and `FacilitatorReadinessGrid.tsx`'s `reauth-required` case to render it — deleting `FacilitatorReadinessGrid.tsx`'s own independently-duplicated `REAUTH_REQUIRED_TEXT` constant and `<div role="status">` markup rather than leaving them alongside it. The subcomponent takes no props derived from cause, role, or session-moment state. This IS in scope for this change — distinct from issue #33's deferred, additional facilitator-only cue (design.md Non-Goals). The CTA button, its wiring, and its tests are added into this subcomponent by tasks 2.2–2.5 below.
- [x] 2.2 Add a `<button>` (or equivalent interactive control) to the `reauth-required` case, calling `window.location.href = "/auth/login"` on activation — the identical pattern already used by `AuthContext.tsx:82` and `AuthErrorPage.tsx:29`. No new route or alternate re-authentication mechanism is introduced. Implement this in the shared subcomponent created by task 2.1, not directly in `ConnectionStatusBanner.tsx`.
- [x] 2.3 Confirm the control is present and interactive (not disabled, no elapsed-time gate) from the very first render of the `reauth-required` state — verify against `connectionHealth.ts`'s existing entry point for this state, no new gating logic added.
- [x] 2.4 **Test:** on entering `reauth-required` (via either the `reauth_required` message or `REAUTH_GRACE_EXPIRED_CLOSE_CODE` fallback), assert the call-to-action control is present in the rendered output and not disabled, in both cases (spec.md "The call-to-action is present and interactive on first render").
- [x] 2.5 **Test:** simulate activating the control; assert `window.location.href` is set to exactly the literal string `/auth/login` (strict equality, not a prefix or substring match) and no other navigation or network call is triggered (spec.md "Activating the call-to-action navigates via the existing login route"). Per Tomás Ferreira's security review, the exact-literal assertion is deliberate: a future PR that appends a query string (e.g. `?reason=reauth`) MUST fail this test rather than silently reopening the SEC-26 non-disclosure boundary.

## 3. Content and Copy

**Depends on: Group 1 (vote-loss determination) for the final sentence set, and Group 2's task 2.1 (D9 extraction) for the shared subcomponent that tasks 3.2 and 3.6 write into and assert against. The content checklist itself (design.md Decision D4) does not depend on either and can be scaffolded first.**

- [ ] 3.1 Draft `reauth-required` copy satisfying the required-content checklist (design.md Decision D4): no countdown/numeric grace-period value; no SEC-26 sub-cause disclosed; a plain-language statement that continuing requires leaving and returning to the page; tone = expected token-lifetime event, not an error; plus the vote-loss statement per Group 1's determination. Route to Priya Nair for sign-off against actual layout (not in isolation) — literal sentence construction is her call, per design.md's Open Questions.
- [~] 3.2 Replace the placeholder `REAUTH_REQUIRED_TEXT` constant with the signed-off copy from 3.1, in the shared subcomponent created by task 2.1 — not separately in `ConnectionStatusBanner.tsx` and `FacilitatorReadinessGrid.tsx`. **Partially done, per architect implementation review (Ingrid Sollenberger):** the mechanical half is complete — the constant lives in the shared subcomponent, satisfies the D4 required-content checklist, and is consumed by both hosts — but the text itself is still draft/placeholder, not the signed-off copy from 3.1, since 3.1 is still open. Do not treat this checkbox as "copy finalized." Update the header comment's "COPY IS NOT FINAL" note once sign-off lands (do not leave it stale if copy changes later without updating the comment). While editing this comment, also correct its stale "both strings below are placeholders pending Priya Nair's sign-off" framing: `unknown-reconnecting`'s copy sign-off already closed (per the archived `websocket-staleness-signal` spec's pilot-readiness gate status notes) — only `reauth-required`'s copy is still pending as of this change.
- [x] 3.3 **Digit-pattern CI test (design.md Decision D3, spec.md "The rendered copy contains no countdown or numeric grace-period value"):** add an automated test asserting the full rendered `reauth-required` output — text content and element attribute values alike (`aria-label`, `title`, `data-*`), not only the visible text node — never matches a digit-plus-time-unit regex (e.g. `/\d+\s*(s|sec|second|m|min|minute)s?\b/i`), mirroring the existing `unknown-reconnecting` rendered-output-identity test pattern (task 3.2 in the `websocket-staleness-signal` change). Widened per Tomás Ferreira's security review to catch a value expressed via an attribute rather than the text node.
- [x] 3.4 **Sub-cause non-disclosure check (spec.md "The rendered copy does not disclose the SEC-26 sub-cause"):** per Marcus Oyelaran's engineer design-review (Finding 2), `connectionHealth.ts` is cause-blind by construction — the frontend only ever observes `REAUTH_GRACE_EXPIRED_CLOSE_CODE` or an inbound `{ eventType: "reauth_required" }` message, with no field distinguishing retry-budget exhaustion, definitive revocation, or concurrent session destruction from one another. A three-fixture test that tries to simulate all three separately is not constructible against the current frontend-visible surface. Instead: (a) keep the automated byte-for-byte identity test across the two frontend-visible triggers that do exist (`reauth_required` message vs. `REAUTH_GRACE_EXPIRED_CLOSE_CODE` close — already covered by task 2.3's setups), and (b) do a code-review / grep check, mirroring task 5.1's pattern, confirming no code path in the shared subcomponent or `connectionHealth.ts` branches on, or has access to, a sub-cause value. Do not go looking for a three-way fixture that the state machine's own design makes impossible to construct.
- [x] 3.5 **Leaving-and-returning statement test (spec.md "The rendered copy states that continuing requires leaving and returning to the page"):** by default, assert the rendered text contains the literal phrase used in the signed-off copy (D4 checklist item 3 is a fixed, known phrase requirement, not free-form copy, so this is string-matchable in the overwhelming majority of cases). A manual/code-review check is permitted only as an escape hatch for the case where sign-off produces implicit phrasing (e.g., conveyed via layout/iconography rather than a sentence) — and that escape hatch MUST be justified in the PR description, not silently invoked in place of the automated assertion.
- [x] 3.6 **Vote-loss statement presence check (per architecture review — this is task 1.1's original part (b), moved here because it asserts against the shared subcomponent's actual rendered copy, which doesn't exist until task 2.1's extraction and doesn't have finalized text until 3.1/3.2 land):** assert the shared `reauth-required` copy (in the D9 shared subcomponent from task 2.1) includes the vote-loss statement whenever task 1.1's grep check finds a vote-compose UI importing `voteDraft.ts`'s hooks, and excludes it otherwise, per design.md Decision D5's conditional. This is the copy-side half of Group 1's gate; task 1.1 covers the code-fact side.

## 4. ARIA Role, Persistence, and Visual Register

**Depends on: Group 2's task 2.1 (D9 extraction) — tasks 4.1, 4.7, and 4.8 all target or require the shared subcomponent it creates, so this group is not structurally independent of Group 2 the way its "nothing structurally" framing previously implied (per architecture review). The final visual styling (4.4) is additionally blocked on Priya Nair's mock sign-off (Group 6) — mechanism and role can ship ahead of final styling using a placeholder, per the precedent `websocket-staleness-signal` set for its own grid marker.**

- [x] 4.1 Change the `reauth-required` case's root element from `role="status"` to `role="alert"`, in the shared subcomponent created by task 2.1. Do not change `unknown-reconnecting`'s `role="status"` in either `ConnectionStatusBanner.tsx` or `FacilitatorReadinessGrid.tsx` — this is a `reauth-required`-only change.
- [x] 4.2 Implement persistence: no dismiss affordance is added in this change (simplest path to compliance), or, if a dismiss affordance is added, it demotes the treatment to a small persistent badge rather than removing it — never to nothing. Confirm which approach is taken and document it in a code comment referencing design.md Decision D1.
- [x] 4.3 Confirm the treatment does not use a native `<dialog>` element in modal mode, a focus trap, or any interaction-blocking mechanism (spec.md "The reauth-required treatment stops short of a full modal") — code-review checklist item.
- [x] 4.4 **Gate — do not ship final visual styling ahead of Group 6's mock sign-off.** Implementation may proceed on role, persistence, and CTA mechanism (4.1–4.3, Group 2) using an intentionally distinct-but-placeholder style (e.g. a border/background differentiator sufficient to satisfy "visually distinct register" trivially, pending the real mock). The final visual register (color, weight, icon) is not implemented until Group 6 closes.
- [x] 4.5 **ARIA role test (spec.md "The reauth-required treatment uses an assertive live region, distinct from unknown-reconnecting's passive one"):** assert `unknown-reconnecting`'s root element has `role="status"` and `reauth-required`'s has `role="alert"`.
- [x] 4.6 **Persistence test (spec.md "The reauth-required treatment is persistent, not auto-dismissing"):** assert no code path hides the treatment before the call-to-action is used or the connection closes.
- [x] 4.7 **Role-uniformity test (spec.md "Copy and CTA are identical regardless of participant or facilitator role"):** mount the two real host components — `SessionConnectionHost.tsx` (participant) and `FacilitatorConnectionHost.tsx` (facilitator) — and assert the rendered text, DOM structure, role, and CTA are identical between them. Per Marcus Oyelaran's engineer design-review (Finding 1), this test MUST exercise both real host components, not two instances of `ConnectionStatusBanner.tsx` — the latter would pass trivially without ever rendering `FacilitatorReadinessGrid.tsx`, the file where the divergence this task exists to catch actually lived.
- [x] 4.8 **Fix the existing facilitator-grid reauth-required test (resolves engineer design-review Finding 1):** `FacilitatorReadinessGrid.test.tsx`'s "renders no grid-marker variant when...reauth-required" test (currently ~line 236) only asserts the grid rows disappear — it does not assert the CTA button or `role="alert"` are present, so it would stay green even if the facilitator gap this change fixes were never actually fixed. Extend that test to also assert the button and `role="alert"` are present, matching the equivalent assertions already made for `ConnectionStatusBanner.tsx`.

## 5. Reveal-Window Non-Special-Casing (Enforced, Not Just Documented)

**Depends on: nothing. Independent verification that Groups 2-4 didn't introduce coupling.**

- [x] 5.1 Code-review / grep check: confirm none of `connectionHealth.ts`, `ConnectionStatusBanner.tsx`, `FacilitatorReadinessGrid.tsx`, or the new D9 shared subcomponent references reveal state, topic status, or any other session-moment-specific signal, before and after this change's edits (spec.md "connectionHealth.ts and ConnectionStatusBanner.tsx contain no reveal-state references").
- [ ] 5.2 **Test:** simulate the `reauth_required` message arriving with a stubbed "reveal in progress" flag set versus unset in whatever surrounding test harness is available; assert the `reauth-required` treatment's rendered text, timing, and behavior are identical in both cases (spec.md "The reauth-required treatment renders identically whether or not a reveal is concurrently in progress"). If no reveal-state harness exists yet (live voting UI is not yet built), track this as a follow-up integration test against the same eventual vote-compose UI change named in task 1.3 and `openspec/specs/vote-compose-recovery/spec.md`'s own tasks.md task 8.3 — that change is where a reveal-state harness will first exist, so this test rides along with the same forward hook rather than a new, unnamed one.

## 6. Facilitator Copy & Visual-Register Sign-Off Gate (Named, Not Resolved Here)

**Depends on: Groups 2-4 existing in at least placeholder/functional form. Mirrors `websocket-staleness-signal`'s own Group 6 gate pattern verbatim in spirit — do not let this close on a paragraph of guidance.**

- [ ] 6.1 Produce a real visual-register mock for the `reauth-required` treatment (color, weight, icon) against actual session-screen contexts — `SessionConnectionHost.tsx` and `FacilitatorConnectionHost.tsx`, not their bare placeholders — per design.md Decision D1's bound (assertive, persistent, distinct, non-modal). Since both hosts render the D9 shared subcomponent, one mock realized through that subcomponent covers both contexts.
- [ ] 6.2 Priya Nair sign-off on the mock from 6.1. Per Tomás Ferreira's security review, this sign-off is explicitly checked against a negative constraint in addition to the visual-register bounds: the mock MUST NOT include any animated element (progress bar, spinner, CSS animation) whose duration or visual timing is derived from or approximates the grace-period constant (design.md Decision D3) — this is a non-textual countdown surrogate and is barred by the same rationale as the digit-pattern test, before a mock exists to regress against.
- [ ] 6.3 Priya Nair sign-off on the finalized copy from Group 3, evaluated in actual layout against the mock.
- [ ] 6.4 Live usability test with Priya Nair evaluating specifically whether the shipped `reauth-required` treatment reads as an alarm without failing to be noticed (both failure directions checked, not just one).
- [ ] 6.5 Update `specs/websocket-staleness-signal/spec.md`'s "Pilot-readiness gate" status notes once 6.2–6.4 close, so the gate's "current status" section reflects reality rather than going stale (same discipline this change applied when correcting the prior change's stale §6 vote-loss framing).
- [ ] 6.6 Until 6.2–6.4 close, this treatment ships to non-pilot/staging environments only, per the extended pilot-readiness gate — do not enable it for a real pilot team's first live session.

## 7. Regression Confirmation

**Depends on: all prior groups. Final check before this change is considered complete.**

- [x] 7.1 Confirm `connectionHealth.ts` has zero diff from its state before this change — this change touches rendering and behavior within `ConnectionStatusBanner.tsx`'s and `FacilitatorReadinessGrid.tsx`'s `reauth-required` branches (via the D9 shared subcomponent) only.
- [x] 7.2 Confirm `unknown-reconnecting`'s rendering, role, timing, and retry behavior are unchanged in both `ConnectionStatusBanner.tsx` and `FacilitatorReadinessGrid.tsx`'s grid-marker treatment — run the existing `websocket-staleness-signal` test suite in full and confirm no regression.
- [x] 7.3 Confirm `AuthContext.tsx` and `AuthErrorPage.tsx` are unmodified by this change — both are reused, not edited.
- [x] 7.4 Confirm `ConnectionStatusBanner.tsx`'s and `FacilitatorReadinessGrid.tsx`'s `reauth-required` rendering is byte-for-byte identical, both now consuming the D9 shared subcomponent — this is the check that would have caught the gap Marcus Oyelaran's engineer design-review found (Finding 1) had it existed before this change.
