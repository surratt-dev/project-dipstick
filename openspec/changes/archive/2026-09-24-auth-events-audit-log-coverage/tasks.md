## 1. Shared write-path plumbing

- [x] 1.1 Extract `withTimeout`, `AuditWriteTimeoutError`, and `AUDIT_WRITE_TIMEOUT_MS` out of `packages/backend/src/auth/session-invalidation-audit.ts` into a new `packages/backend/src/auth/audit-write-timeout.ts`. Update `session-invalidation-audit.ts` to import them from there. This is a pure extraction — confirm `session-invalidation-audit.test.ts` (or wherever its existing coverage lives) passes unchanged with no test edits required.
- [x] 1.2 Add `packages/backend/src/auth/fail-open-audit-write.ts`, exporting `writeFailOpenAuditRow({ operation, userId, actorGlobalRole, actorIp, teamId, metadata, log, failureAuditFields })` (design.md Decision D2). `operation` is typed `AuditEventName` (imported from `audit-logger.ts`), not `string` — this generalizes what was previously a single-event write path into a multi-event one, and losing the compile-time check against a typo'd operation name would reopen a class of bug the rest of this codebase's `emitAuditEvent` call sites are protected against (Engineer review, Finding 5). `actorGlobalRole` is a required parameter, not resolved internally — callers that already have it (see Section 2) pass it directly; callers that don't (Section 4) resolve it via `resolveActorGlobalRole` before calling. Wraps the INSERT in `withTimeout(..., AUDIT_WRITE_TIMEOUT_MS)` (imported from task 1.1's module); on any rejection, emits `auth.audit_write_failed` via `emitAuditEvent(log, "auth.audit_write_failed", { ...failureAuditFields, failureMode: err instanceof AuditWriteTimeoutError ? "timeout" : "error", sourceIp: actorIp })`, without rethrowing.
- [x] 1.3 Add `AuditWriteError` to `packages/backend/src/auth/errors.ts` (design.md Decision D7), alongside `MissingClaimError`: a static `"Audit log write failed"` message (never derived from the underlying database error) and a `causeClass: string` field capturing the underlying error's constructor name. This class covers both a failed audit `INSERT` and a failed `db.connect()` — a new failure mode this change introduces by checking out a pooled client in `/auth/callback` for the first time (Engineer review, Finding 2/4); the shared transactional helper that actually wraps both cases in this class is task 1.4, next — defined here first, ahead of it, so 1.4's own text doesn't reference a symbol that doesn't exist yet (Architect review, Finding 1). From an incident responder's perspective both failure modes are "this application's own database infrastructure, not the IdP." Add a fifth `AuthErrorCategory` value, `internal_error`, to `packages/shared/src/types/auth.ts`. Add a branch to `mapAuthError` (`error-handler.ts`): `AuditWriteError` → category `internal_error`, message "We couldn't finish signing you in due to a temporary internal problem. Please try again in a few moments. If this continues, contact your IT administrator." Add a matching branch to `sanitizeOidcError` (`oidc-error-sanitizer.ts`) preserving `message`/`stack`/`causeClass` unredacted, mirroring `MissingClaimError`'s existing treatment. Update `packages/frontend/src/pages/AuthErrorPage.tsx`'s `isRetryable` check to also treat `internal_error` as retryable. Update the `AuthErrorCategory` type-level assertions in `packages/shared/src/__tests__/index.test.ts` to include `internal_error`.
- [x] 1.4 Add `packages/backend/src/auth/audit-write-transaction.ts`, exporting `withAuditTransaction(domainWrite, auditInsert)` and `TRANSACTIONAL_AUDIT_STATEMENT_TIMEOUT_MS` (400ms) (design.md Decision D2/D3, Engineer Findings 1 and 6). This is the single shared implementation of the transactional group's connect/`BEGIN`/domain-write/audit-`INSERT`/`COMMIT`/`ROLLBACK`/`release` skeleton, used by all four transactional call sites (Sections 3, 5, 6) instead of each reproducing it independently. It: (a) wraps a failed `db.connect()` in `AuditWriteError` (task 1.3) and rethrows immediately, with no `ROLLBACK` attempted (no transaction was opened); (b) runs `domainWrite(client)` inside `BEGIN`/`COMMIT`, letting its own errors propagate unwrapped (this is `resolveOrCreateAccount`'s pre-existing UPSERT-failure path, unchanged per Decision D7); (c) immediately before calling `auditInsert(client, domainResult)`, issues `SET LOCAL statement_timeout = ${TRANSACTIONAL_AUDIT_STATEMENT_TIMEOUT_MS}` on the same client — a fixed internal constant, not request-derived, so inlining it into the query string carries no injection risk despite `SET` not accepting bind parameters; (d) wraps any error from `auditInsert` in `AuditWriteError` before it triggers `ROLLBACK`. Write a unit test proving the specific bug this exists to fix: simulate an audit `INSERT` that hangs past `TRANSACTIONAL_AUDIT_STATEMENT_TIMEOUT_MS` and confirm the mock client receives a `ROLLBACK` call promptly afterward (i.e., the mocked `client.query` for the `INSERT` actually rejects — via the simulated `statement_timeout` cancellation — rather than staying pending while a separate Node-side timer fires).
- [x] 1.5 **Mock setup — `auth.test.ts` (Architect review, Finding 2; Engineer review, Finding 4):** extend `auth.test.ts`'s `db` mock to add a `connect` method (today it mocks only `query`, unlike `teams.test.ts`, which already mocks both). `mockDbConnect` must return a **distinct mock client instance on each call**, not the same object across calls — `GET /auth/callback` with a `pendingJoinToken` opens two separate transactions in one request (`resolveOrCreateAccount`'s, via task 3.1, then `executeJoinFlow`'s, via task 6.2), and a shared client mock would land both transactions' `BEGIN`/`INSERT`/`COMMIT` sequences in one `client.query.mock.calls` array — silently breaking any positional ("the Nth call is the audit INSERT") assertion carried over from `teams.test.ts`'s pattern. Assert against each transaction's own client mock, not a shared array index. Built here, ahead of Sections 3 and 6, because both sections' tests (3.4–3.6, 6.4–6.7) need this fixture in place before they can be written correctly — moved up from its prior position (was task 9.8) for exactly that reason.
- [x] 1.6 **Mock setup — `join-links.test.ts` (Architect review, Finding 3):** verified directly — `join-links.test.ts`'s `db` mock (`vi.mock("../../db.js", ...)`, currently `{ query: (...args) => mockDbQuery(...args) }`) mocks only `query`, the same gap task 1.5 fixes for `auth.test.ts`; before this change, `join-links.ts` had no transactional (`db.connect()`-based) writes, so it never needed one. Add an equivalent `connect` method returning a distinct mock client instance per call, matching task 1.5's fixture shape and rationale. Needed before Section 5 is implemented (tasks 5.3–5.5 exercise it) and before Section 6's `join-links.ts` call site (tasks 6.1, 6.4–6.7).
- [x] 1.7 Extend `resolveOrCreateAccount` (`packages/backend/src/auth/account-resolver.ts`) to accept an optional `client` parameter (a `pg.PoolClient`, not the pool). When provided, its UPSERT runs via `client.query` instead of `db.query`. Add the `previousRole`-capturing CTE (design.md Decision D4):
  ```sql
  WITH prior AS (
    SELECT global_role FROM users WHERE oidc_subject = $1 AND oidc_issuer = $2
  )
  INSERT INTO users (...) VALUES (...)
  ON CONFLICT (oidc_subject, oidc_issuer) DO UPDATE SET ...
  RETURNING ..., (SELECT global_role FROM prior) AS previous_global_role
  ```
  Add `previousGlobalRole: string | null` to `ResolvedUser` (`null` when `isNewUser` is true — there is no prior row). Confirm every existing caller of `resolveOrCreateAccount` that does not pass a `client` is unaffected (the parameter is optional, defaulting to using the pool exactly as today).
- [x] 1.8 Confirm no new `AuditEventName` is needed — `auth.success`, `auth.session_created`, `auth.first_access_created`, `auth.role_claim_mapped`, `auth.idp_logout_failed`, `join.link_created`, `join.link_redeemed` all already exist in `audit-logger.ts`'s union. Add an inline comment near each, matching the file's existing documentation convention, noting that this change gives it a DB-row-backed counterpart and pointing to this change's design.md.

## 2. `auth.ts`: fail-open group (`auth.success`, `auth.session_created`)

> **Sequencing note (Architect review, Finding 4 — Optional):** in `GET /auth/callback`'s actual top-to-bottom code, Section 3's transactional wrap (around `resolveOrCreateAccount`, task 3.1) sits *earlier* in the function than this section's fail-open calls. Implementing Section 3 first, then Section 2, follows the handler's real code flow and avoids editing code immediately downstream of a restructuring that hasn't happened yet. This is a sequencing suggestion, not a hard dependency — Section 2 only reads fields already on the existing `ResolvedUser` shape and would work correctly in either order — so the section numbers below are left as-is (they group by mechanism, fail-open vs. transactional, not by required build order) rather than renumbering the two sections, which would require churning every cross-reference to Section 3's task numbers elsewhere in this document for a coordination convenience, not a correctness fix.

- [x] 2.1 At the `auth.session_created` emission (`GET /auth/callback`, after `request.session.regenerate()`), call `writeFailOpenAuditRow` with `operation: "auth.session_created"`, `actorGlobalRole: user.globalRole` (no `resolveActorGlobalRole` call — already in scope), `teamId: null`, `metadata: { authSessionId: request.session.sessionId, correlationId }`, `failureAuditFields: { userId: user.id, authSessionId: request.session.sessionId, operation: "auth.session_created" }`.
- [x] 2.2 At the `auth.success` emission (same handler, immediately after), call `writeFailOpenAuditRow` with `operation: "auth.success"`, `actorGlobalRole: user.globalRole`, `teamId: null`, `metadata: { oidcSubject: user.oidcSubject, oidcIssuer: user.oidcIssuer, isFirstAccess: user.isNewUser, correlationId }`, `failureAuditFields: { userId: user.id, operation: "auth.success" }`.
- [x] 2.3 Confirm both calls are awaited (not fire-and-forget) and sequenced before `request.session.save()`/the redirect, consistent with `session_invalidated`'s existing awaited-with-bounded-timeout precedent — no unawaited promise introduced.

## 3. `auth.ts`: transactional group (`auth.first_access_created`, `auth.role_claim_mapped`)

- [x] 3.1 In `GET /auth/callback`, replace the direct call to `resolveOrCreateAccount` with a call to `withAuditTransaction` (task 1.4): `domainWrite` calls `resolveOrCreateAccount(claims, request.log, client)`; `auditInsert` issues the conditional audit `INSERT` (task 3.2/3.3 below) or no-ops. Commit before proceeding to `request.session.regenerate()`.
- [x] 3.2 Inside `withAuditTransaction`'s `auditInsert` closure, when `user.isNewUser`, issue `INSERT INTO audit_log (actor_user_id, actor_global_role, actor_ip, operation, team_id, metadata) VALUES ($1, $2, $3, 'auth.first_access_created', NULL, $4)` on `client`, with `metadata: { oidcSubject: user.oidcSubject, oidcIssuer: user.oidcIssuer, globalRole: user.globalRole, correlationId }`. `withAuditTransaction` itself wraps any failure from this `INSERT` as `AuditWriteError` (task 1.3/1.4) — distinguishing this from `resolveOrCreateAccount`'s own UPSERT failure, which continues to propagate unwrapped, unchanged from today (design.md Decision D7); no per-call-site try/catch is needed here. **Move the existing `emitAuditEvent(..., "auth.first_access_created", ...)` structured-log call to fire only after `withAuditTransaction` returns successfully** — not at its current source position immediately after `resolveOrCreateAccount` returns (Engineer review, Finding 3). Leaving it at today's position would let a rolled-back transaction still produce a structured log claiming the account was created, reproducing the exact "log says X, DB doesn't have X" gap this change exists to close.
- [x] 3.3 Inside the same `auditInsert` closure, when `!user.isNewUser && user.globalRole !== "engineer"`, issue the equivalent `INSERT INTO audit_log ... operation = 'auth.role_claim_mapped'` on `client`, with `metadata: { oidcSubject: user.oidcSubject, globalRole: user.globalRole, previousRole: user.previousGlobalRole, correlationId }`. **Move the existing `emitAuditEvent(..., "auth.role_claim_mapped", ...)` call to fire only after `withAuditTransaction` returns successfully**, same reasoning as task 3.2 (Engineer review, Finding 3).
- [x] 3.4 Verify a thrown error from either `INSERT` rolls back `resolveOrCreateAccount`'s UPSERT — write a test asserting the `users` row is not created/updated when the audit `INSERT` fails (design.md Decision D3's stated coupling; do not let this silently degrade to fail-open during implementation). Also assert that the corresponding structured-log `emitAuditEvent` call (task 3.2/3.3) was never made in this case — confirming the move in tasks 3.2/3.3 actually took effect, not just that the DB row is absent.
- [x] 3.5 Verify that when the rolled-back error (now an `AuditWriteError`) propagates up into `GET /auth/callback`'s catch block, `mapAuthError` returns category `internal_error` with the internal-problem message (not `authentication_failed`'s default text) and `sanitizeOidcError`'s output preserves `message`/`stack`/`causeClass` unredacted rather than falling into the "unrecognized class" branch (design.md Decision D7).
- [x] 3.6 Verify a failed `db.connect()` (simulated pool exhaustion) at this call site is also wrapped as `AuditWriteError` and reaches `mapAuthError`/`sanitizeOidcError` with the same `internal_error` classification as task 3.5 — not `mapAuthError`'s default `authentication_failed` branch (Engineer review, Finding 2/4; design.md Decision D7's widened scope).

## 4. `auth.ts`: fail-open group (`auth.idp_logout_failed`)

- [x] 4.1 At the `auth.idp_logout_failed` emission (`POST /auth/logout`, the `catch` block around `getEndSessionUrl()`), resolve `actorGlobalRole` via `resolveActorGlobalRole(userId)` (imported from `connection-reauthorization.ts`, matching `session_invalidated`'s existing usage in the same handler), then call `writeFailOpenAuditRow` with `operation: "auth.idp_logout_failed"`, `teamId: null`, `metadata: { authSessionId: sessionId }`, `failureAuditFields: { userId, authSessionId: sessionId, operation: "auth.idp_logout_failed" }`.
- [x] 4.2 Add `sourceIp: request.ip` to the existing `emitAuditEvent(request.log, "auth.idp_logout_failed", { userId, sessionId })` structured-log call at the same site (design.md Decision D6).

## 5. `join-links.ts`: `join.link_created`

- [x] 5.1 Wrap `POST /api/teams/:teamId/join-links`'s `INSERT INTO join_links` in a call to `withAuditTransaction` (task 1.4): `domainWrite` issues the `join_links` INSERT; `auditInsert` issues the audit row (task 5.2). The existing permission-check `SELECT`s stay on the plain pool — only the write span is wrapped.
- [x] 5.2 In the `auditInsert` closure, issue `INSERT INTO audit_log (..., operation, team_id, metadata) VALUES (..., 'join.link_created', $teamId, $metadata)` on the transaction client, with `actor_global_role` from the already-resolved `userRole`/`memberRole` check earlier in the handler (no additional lookup), `team_id: teamId` (the route's own param), `metadata: { linkId: row.id, expiresAt: row.expires_at.toISOString() }`. `withAuditTransaction` wraps any failure from this `INSERT` as `AuditWriteError` (task 1.4) — `join-links.ts` has no catch block to consume the distinction (design.md Decision D7), but the class is still thrown for consistency and for any future error-shape-aware handling in this file. **Move the existing `emitAuditEvent(..., "join.link_created", ...)` structured-log call to fire only after `withAuditTransaction` returns successfully** — not at its current source position immediately after the `join_links` INSERT resolves (Engineer review, Finding 3).
- [x] 5.3 Test: a failed audit `INSERT` rolls back the `join_links` row (Decision D3's coupling, same assertion shape as task 3.4), and the structured `emitAuditEvent(..., "join.link_created", ...)` call from task 5.2 is never made in that case.
- [x] 5.4 Test: the rolled-back `AuditWriteError` from task 5.2 is not sanitized or reclassified — it reaches Fastify's default error handler as an ordinary thrown error, producing a plain 500 with no `mapAuthError`/`sanitizeOidcError` involvement (design.md Decision D7's named asymmetry with the through-auth path).
- [x] 5.5 Test: a failed `db.connect()` at this call site also surfaces as a plain, unclassified 500 (same asymmetry as task 5.4) — `AuditWriteError` is thrown either way, but nothing in this file consumes it (Engineer review, Finding 2/4).

## 6. `join-links.ts` and `auth.ts`: `join.link_redeemed` (both call sites)

- [x] 6.1 In `join-links.ts`'s `GET /api/join/:token`, wrap the `INSERT INTO team_memberships ... RETURNING id` and, conditionally (only when a row was actually inserted), the `join.link_redeemed` audit `INSERT`, in one call to `withAuditTransaction` (task 1.4). `team_id: link.team_id`, `actor_user_id: session.userId`, `metadata: { linkId: link.id }` (the join-link delta spec requires `linkId` on both `join.link_created` and `join.link_redeemed` — task 5.2 covers the former; BA review, Finding 1). When the INSERT is conflict-suppressed (`rows.length === 0`), the `auditInsert` closure no-ops and the transaction still commits — matching the existing structured-log gating exactly. `withAuditTransaction` wraps any failure from the audit INSERT as `AuditWriteError` (task 1.4) — no catch block in this file consumes the distinction, but it's thrown for consistency (design.md Decision D7). **Move the existing `emitAuditEvent(..., "join.link_redeemed", ...)` structured-log call to fire only after `withAuditTransaction` returns successfully, and only when a row was actually inserted** — not at its current source position immediately after the INSERT resolves (Engineer review, Finding 3).
- [x] 6.2 In `auth.ts`'s `executeJoinFlow`, apply the identical transactional treatment (via `withAuditTransaction`) to its own `INSERT INTO team_memberships`/conditional `join.link_redeemed` audit row — same fields as task 6.1 (`team_id: link.team_id`, `actor_user_id: session.userId`, `metadata: { linkId: link.id }` per the join-link delta spec; BA review, Finding 1) — including the same `AuditWriteError` wrapping around the audit INSERT (this call site's rethrow *is* consumed by `GET /auth/callback`'s catch block, since `executeJoinFlow` is invoked from within that same try block — task 3.5's classification assertion applies here too). **Move the existing `emitAuditEvent(..., "join.link_redeemed", ...)` structured-log call in `executeJoinFlow` to fire only after `withAuditTransaction` returns successfully**, same reasoning as task 6.1 (Engineer review, Finding 3). Confirm the surrounding `SELECT id, team_id, expires_at, revoked_at FROM join_links` (validation) and the trailing `SELECT id FROM sessions WHERE team_id = ...` (redirect computation) stay on the plain pool — only the membership-insert-plus-audit-insert span is transactional, matching task 6.1's scoping.
- [x] 6.3 Resolve `actor_global_role` for the `join.link_redeemed` audit row at both call sites — this is two different fixes behind one event name, not a single lookup pattern to apply uniformly (design.md Decision D5's resolution split):
  - `auth.ts`'s `executeJoinFlow`: add a new required parameter, `actorGlobalRole: string`. Update its call site in `GET /auth/callback` (`auth.ts:385`) to pass `user.globalRole`, already resolved two statements earlier for `auth.success`'s emission. Use this parameter directly for `actor_global_role` — no additional lookup.
  - `join-links.ts`'s `GET /api/join/:token`: no `global_role` value is in scope anywhere in this handler today (verified — only `session.userId` is read). Issue a new `SELECT global_role FROM users WHERE id = $1` using `session.userId` before writing the audit row; use the result for `actor_global_role`. **Connection scoping (Architect review, Finding 5; BA review, Finding 2):** this `SELECT` runs on the plain pool (`db.query`), *before* `withAuditTransaction`'s `db.connect()`/`BEGIN` opens — not on the checked-out transaction client. The resolved role must already be in hand for the `auditInsert` closure to use once the transaction starts; it is a read with no correctness dependency on the pending `team_memberships` INSERT, so there is no reason to hold it on a connection a subsequent failure would roll back. Also see design.md Decision D5, updated to state this explicitly.
- [x] 6.4 Test: idempotent re-join (existing member, `ON CONFLICT DO NOTHING` suppresses the insert) at both call sites produces no `audit_log` row and no structured `join.link_redeemed` log entry — unchanged from today's behavior.
- [x] 6.5 Test: a failed audit `INSERT` at either call site rolls back the `team_memberships` insert (Decision D3's coupling), and the structured `emitAuditEvent(..., "join.link_redeemed", ...)` call from tasks 6.1/6.2 is never made in that case.
- [x] 6.6 Test: the rolled-back `AuditWriteError` from `executeJoinFlow` (task 6.2) is classified by `mapAuthError`/`sanitizeOidcError` identically to task 3.5's assertion (category `internal_error`, unredacted diagnostic detail) when it propagates into `GET /auth/callback`'s catch block; the equivalent failure at `join-links.ts`'s `GET /api/join/:token` (task 6.1) is not — same asymmetry assertion shape as task 5.4.
- [x] 6.7 Test: a failed `db.connect()` at `executeJoinFlow`'s call site is classified identically to task 3.6/6.6 (`internal_error`, via `GET /auth/callback`'s catch block); the equivalent failure at `join-links.ts`'s `GET /api/join/:token` surfaces as a plain 500, same asymmetry as task 5.5 (Engineer review, Finding 2/4).

## 7. Adjacent small fixes (Decision D6)

- [x] 7.1 Add `userId` to `executeJoinFlow`'s two `join.link_rejected` structured-log emissions (`not_found` and `revoked`/`expired` branches) — `userId` is already a parameter in scope at both call sites.
- [x] 7.2 Confirm `join-links.ts`'s `GET /api/join/:token` `join.link_rejected` branches are unchanged — they fire before any session check and structurally have no `userId` to add.

## 8. Deferred scope and tracked follow-ups

- [x] 8.1 Confirm GitHub issue #156 (filed during this change's proposal stage) accurately names all four deferred events (`auth.authorization_initiated`, `auth.callback_received`, `auth.failure` at all three call sites, `join.link_rejected` at all five call sites) and the volume/latency questions blocking a decision. No code change in this change's scope touches any of these four events beyond the `auth.failure` catch-all branch remaining exactly as it is today.
- [x] 8.2 Confirm GitHub issue #157 ("`auth.role_claim_mapped`'s reversion-to-default is structurally invisible," filed during design review) remains open and accurately reflects design.md Decision D4's stated blind spot (Security review, F3). No code change in this change's scope alters `role_claim_mapped`'s firing condition.
- [x] 8.3 Confirm the comment recorded on GitHub issue #131 ("No monitoring consumer exists for any `AuditEventName` signal") naming this change's new signals (`auth.audit_write_failed` for three more events; `errorClass: "AuditWriteError"` on `auth.callback_error`) as candidate inputs for that issue's eventual fix (Security review, F2) still accurately reflects what shipped. No code change in this change's scope builds the alert/dashboard itself.

## 9. Tests

- [x] 9.1 For each of the seven in-scope events, a test asserting exactly one `audit_log` row is written with the expected `operation`, `actor_user_id`, `actor_global_role`, `actor_ip`, `team_id` (NULL for the five account-level events, populated for both `join.*` events), and `metadata` shape stated in the delta specs.
- [x] 9.2 Fail-open group (`auth.success`, `auth.session_created`, `auth.idp_logout_failed`): simulated DB error and simulated timeout each produce `auth.audit_write_failed` with the correct `failureMode`, and the triggering response still succeeds — matching `session_invalidated`'s existing test pattern.
- [x] 9.3 Transactional group (`auth.first_access_created`, `auth.role_claim_mapped`, `join.link_created`, `join.link_redeemed` at both call sites): simulated audit-INSERT failure rolls back the accompanying domain write and the request fails — one test per event (tasks 3.4, 5.3, 6.5 cross-referenced here, not duplicated).
- [x] 9.4 `auth.role_claim_mapped`: test that `previousRole` reflects the value on record immediately before the current authentication's UPSERT, across a role change (`engineer` → `engineering_manager`) and a same-role repeat login (`previousRole === globalRole`). Also explicitly assert the brand-new-user case named in the auth-error-handling spec (BA review, Finding 4): when `isNewUser` is true, `previousGlobalRole` is `null` on the CTE's return value / `ResolvedUser`, directly — not merely inferred from `previousRole`'s absence in `first_access_created`'s metadata shape (task 9.1), since no `role_claim_mapped` row fires at all for a first-ever login.
- [x] 9.5 `auth.session_created`/`auth.success`: test that no `resolveActorGlobalRole` SELECT is issued — `actor_global_role` comes from `user.globalRole` directly (design.md's stated latency optimization).
- [x] 9.6 Full regression: existing `auth.test.ts` and `join-links.test.ts` suites pass with only the mocking changes this section's new transactional/fail-open assertions require — no change to existing response bodies, status codes, or redirect behavior.
- [x] 9.7 `AuditWriteError` classification and its named asymmetry (design.md Decision D7): tasks 3.5 and 6.6 (classified via `mapAuthError`/`sanitizeOidcError` at the two `auth.ts` call sites reaching `GET /auth/callback`'s catch block) and tasks 5.4 and 6.6's second half (plain, unclassified 500 at `join-links.ts`'s two call sites) are cross-referenced here, not duplicated. Tasks 3.6, 5.5, and 6.7 (the `db.connect()`-failure variant of the same asymmetry) are cross-referenced here too.
- [x] 9.8 **Mock setup (Engineer review, Finding 4; Architect review, Findings 2/3):** the `auth.test.ts` and `join-links.test.ts` mock-fixture work (adding a `connect` method returning a distinct mock client instance per call) is built in Section 1 (tasks 1.5, 1.6) — moved there because Sections 3, 5, and 6's tests need it in place before they can be written correctly, the same reason 3.4/5.3/6.5 are cross-referenced rather than duplicated elsewhere in this section. Cross-referenced here, not duplicated.
- [x] 9.9 Structured-log-fires-only-after-commit (Engineer review, Finding 3): for each of the four transactional events (`auth.first_access_created`, `auth.role_claim_mapped`, `join.link_created`, `join.link_redeemed` at both call sites), confirm via the `emitAuditEvent` mock/spy that the structured log call happens strictly after the corresponding `withAuditTransaction` call resolves — not merely that both eventually happen in some order. Cross-referenced with tasks 3.4, 5.3, and 6.5's "never called on rollback" assertions, which this ordering guarantee makes possible.

## 10. Spec sync and documentation

- [x] 10.1 Confirm `specs/auth-error-handling/spec.md`'s delta and `specs/join-link/spec.md`'s delta both match shipped behavior scenario-by-scenario before this change is archived.
- [x] 10.2 Update `docs/deployment.md`'s `### Logging` at-risk-event enumeration: remove `auth.success`, `auth.session_created`, `auth.first_access_created`, `auth.role_claim_mapped`, `auth.idp_logout_failed`, `join.link_created`, `join.link_redeemed` (now DB-backed); confirm `auth.authorization_initiated`, `auth.callback_received`, `auth.failure`, `join.link_rejected`, and `auth.audit_write_failed` remain listed and accurately described, with a pointer to issue #156 for the four newly-deferred events.
- [x] 10.3 Confirm GitHub issue #156 remains open and accurately scoped at archive time — this change's job is to have filed it, not to resolve it.
- [x] 10.4 Confirm proposal.md's Impact section states SEC-15's write-only guarantee as an inherited, unchanged property of the `audit_log` table's existing permissions, and states the `audit_log`/Postgres operational-reliability posture (the existing `/health/ready` coverage and its limits) rather than an invented SLA — no code change, a documentation-completeness check only.
