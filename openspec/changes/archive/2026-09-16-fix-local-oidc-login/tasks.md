## 1. Fix

- [x] 1.1 In `packages/backend/src/routes/auth.ts`, change the callback URL reconstruction (`GET /callback` handler, ~line 122) from `request.hostname` to `request.host`, so the reconstructed URL preserves a non-default port.
- [x] 1.2 Add a single-sentence comment on that line explaining why `request.host` is used instead of `request.hostname` (that `hostname` drops the port, breaking `redirect_uri` matching against the value registered at authorization). One sentence is enough — this isn't the JWKS-fix comment's incident-report length, just the same "here's the mistake and why" intent.
- [x] 1.3 No new writing here: the reverse-proxy/production-inertness reasoning is already fully recorded in `design.md` (Decisions — "No additional check against staging/prod topology before shipping"). Reference it if a future reader lands on this file first; no additional analysis is required before shipping.

## 2. Documentation (no re-fix)

- [x] 2.1 Confirm no task in this list touches `docker/oidc/server.js` or otherwise re-implements the JWKS key fix — it is already fixed by commit `4cd06e3` (persona-login), keeping the current fixed 2048-bit key. This task is a scope check, not code to write. Verified: `docker/oidc/server.js` untouched by this change; key block still the fixed 2048-bit RSA key from commit `4cd06e3`.
- [x] 2.2 Confirm no task in this list introduces new configurability (e.g., a host/port handling flag) — this is a one-property swap in trusted internal dev tooling, not a candidate for a new option. This task is a scope check, not code to write. Verified: only change is the `request.hostname` → `request.host` swap plus its comment; no new env var, flag, or config option added.

## 3. Verification

- [x] 3.1 Start the local stack per the README's "Local development" section: `docker compose up` and `npm run dev`. Done: `docker compose up -d` (postgres/redis/oidc all healthy); backend dev server (`tsx watch`) already running on port 3000 and picked up the fix via hot reload.
- [x] 3.2 In a browser, navigate to `/auth/login`, complete the mock IdP flow, and confirm the browser lands on an authenticated page (e.g. `/team/:id`) — with no manual patching, no port override, and no second OIDC instance. No browser extension was available in this session, so this was verified with an equivalent HTTP-level walkthrough (cookie-jar `curl`, driving the exact same request sequence a browser would make) against the real running stack. `GET /auth/login?loginHint=participant-001` redirected to the OIDC provider with `redirect_uri=http%3A%2F%2Flocalhost%3A3000%2Fauth%2Fcallback` (port preserved). Following the auto-approved login/consent interactions back to `GET /auth/callback` returned `302` to `/no-team` (the legitimate authenticated landing page for an account with zero team memberships, per `packages/frontend/src/App.tsx`) with a `sessionId` cookie set. `GET /auth/session` with that cookie returned a populated authenticated session: `{"user":{"id":"a5cf9f16-...","displayName":"Alex Participant","email":"participant@example.com"},"teamMemberships":[],...}`. Postgres confirms a fresh user row was created at the moment of this login (`oidc_issuer=http://localhost:4011`, matching the real running mock IdP port), meaning the token exchange succeeded end-to-end.
- [x] 3.3 Confirm a redirect to an error page does not occur. Landing on a redirected error page does not count as a pass, even if the redirect itself completes without a thrown exception. Confirmed: the callback redirected to `/no-team`, not `/auth/error`, and the session endpoint reflects a real authenticated user — not an error-page redirect that merely avoided throwing.
