## 1. Design decisions to resolve before implementation completes

- [x] 1.1 Security analyst decides: does the write-side block (Decision B) extend to Application-Admin actors on TEAM-005, or only EM actors? (design.md Open Question 1) — **RESOLVED (design review 2026-09-16): block all actors, including Application Admin, unconditionally.** Task 3.2 below is unconditional on actor — no role branch.
- [x] 1.2 Security analyst decides: mismatched-state handling in `evaluateTeamAccess` — graceful `participant`-level access with a distinguishable audit/log signal, vs. a hard, distinguishable 403 (design.md Open Question 2) — **RESOLVED (design review 2026-09-16, design.md Decision E): graceful degrade to `role: 'participant'`, plus a log-only `team.access_grant_mismatch` event (no synchronous `audit_log` row, given `evaluateTeamAccess` runs on every content request).** Task 2.3 below implements this.
- [x] 1.3 Security analyst decides: does a blocked promotion attempt produce a distinguishable audit event (e.g., `team.role_change_denied`), separate from the caller-facing error (design.md Open Question 3) — **RESOLVED (design review 2026-09-16, design.md Decision F): yes, required.** Synchronous `audit_log` row (`team.role_change_denied`) plus `emitAuditEvent`, written before the error response, for actors who pass the base TEAM-005 authorization check. Task 3.4 below is unconditional.
- [x] 1.4 Facilitator reviews and signs off on the exact error copy and HTTP status code for a blocked promotion response (design.md Open Question 4, Decision D) before task 3.3 is finalized. — **RESOLVED (Facilitator sign-off, 2026-09-16): status code confirmed as `403` / `category: "forbidden"` (matches the existing precedent at `teams.ts:1070-1077`, the admin-only 403 for TEAM-006). Copy changed from the implementer's provisional draft.** The provisional draft ("TEAM-005 can no longer be used to establish a new Engineering Manager relationship. Use 'Establish a Manager/Team Relationship' (POST /api/v1/teams/:teamId/managers), which is restricted to Application Admins.") had two problems relative to Decision D and the two precedent messages it was meant to follow (`teams.ts:745`, `:1074`): (1) it named the internal use-case identifier "TEAM-005" and the raw REST path/method — jargon neither precedent message uses, and exactly the kind of internal-system language that makes a good-faith blocked actor feel like they've hit a security wall rather than the wrong door; (2) "restricted to Application Admins" is an actor-permission phrasing borrowed from the precedents, but this block isn't an actor-permission gate — Decision B blocks the transition for every actor including Application Admins — so an admin actor reading it could reasonably wonder why a rule about who's allowed doesn't seem to apply to them. Final copy, edited directly into `teams.ts` (~line 862): "This action cannot be used to establish a new Engineering Manager relationship for this team, for any actor including Application Admins. Use 'Establish a Manager/Team Relationship' instead." This drops the internal ID and raw path, names the correct action by its human name only, and states explicitly that the block is unconditional on actor — resolving the ambiguity the draft left open. No logic changed, text/status only.
- [x] 1.5 Solution Architect decides whether the write-side restriction also needs a database-layer defense-in-depth expression (check constraint or trigger), following the precedent of `migrations/7_team_memberships_partial_constraint.sql` (design.md Open Question 5). — **RESOLVED (design review 2026-09-16, design.md Decision G): not a trigger.** A CHECK constraint can't see the prior row value, and a transition-aware trigger would block TEAM-006's own legitimate `participant → engineering_manager` upsert (`teams.ts:1156-1163`) without added session-variable plumbing (Full Stack Engineer finding, design review 2026-09-16). Instead, scope this as a **detection control**: a reusable query flagging `team_memberships.role = 'engineering_manager'` rows with no matching `team.manager_established` audit_log entry — the same query task 1.6's backfill needs. No new migration or follow-on task required for this decision itself; task 1.6 should build the query so it doubles as ongoing detection.
- [x] 1.6 Solution Architect (or production-data owner) confirms whether a backfill/remediation audit query for pre-existing phantom EM relationships is warranted (design.md Open Question 6). Record the decision — including "checked, no action needed" — even if non-blocking. Per task 1.5's resolution, build this query as reusable/re-runnable so it also serves as Decision G's ongoing detection control, not a one-off. — **RESOLVED (Solution Architect, 2026-09-16, design.md Decision H): warranted, query already specified under Decision G. Owner assigned: the on-call Production Data Engineer (production-DB-access rotation, named generically — no dedicated DBA/data-platform persona exists on this project). Timeline: run within this sprint, no later than 2026-09-23.** The Architect does not have production DB access in this capacity, so execution is delegated per this decision, not performed here; final closure of task 6.4 for this item still requires the named owner's actual result (checked/nothing found, or checked/found-and-notified) by the deadline.
- [x] 1.7 Assign an owner and decide whether historical `team.role_changed`-mislabeled audit events (pre-fix TEAM-005 promotions) are corrected retroactively or left as-is going forward (design.md Open Question 7). Non-blocking for shipping the fix. — **RESOLVED (Solution Architect, 2026-09-16, design.md Decision I): not corrected retroactively by rewriting `audit_log` rows** (the table is append-only by design; rewriting audit history to fix a labeling bug is a worse integrity problem than the mislabeling itself — per Tomás Ferreira's independent Q7 recommendation, `design-review-security.md`). **Going forward is already fully corrected by this change's own Decisions A/B.** For the historical record: an additive, dated annotation (new `audit_log` operation `team.manager_established_retroactive_annotation`, referencing the original mislabeled row, never modifying it) addresses the Facilitator's original discoverability concern (`exploration-notes.md:211`) without compromising the original rows. Owner and timeline: same as 1.6 — on-call Production Data Engineer, by 2026-09-23, same investigative pass.
- [x] 1.8 Solution Architect / Non-Goals owner decides whether TEAM-006 should gain an equivalent zero-Engineers warning now that it is the sole remaining promotion path (design.md Open Question 8, raised in BA task review 2026-09-16). — **RESOLVED (Solution Architect, sync-verify pass 2026-09-16): no, not in this change.** A proactive warning is new user-facing scope (its own copy, its own confirmation flow) that Non-Goals correctly rules out of a security-hardening change; deferred to a separate, explicitly-scoped follow-on proposal rather than bundled here. Full reasoning and the corresponding `openspec/specs/role-assignment/spec.md` reachability note (added in the same pass) are in `sync-verify-architect.md`.

## 2. Read-side fix — evaluateTeamAccess

- [x] 2.1 Remove Path 1's `membership_role = 'engineering_manager'` grant sub-case in `packages/backend/src/auth/team-content-access-helper.ts`; Path 2's dual-check (`global_role = 'engineering_manager'` AND `team_memberships.role = 'engineering_manager'`) becomes the sole path that can return `role: 'engineering_manager'`. **Done.** Also required threading a `logger: FastifyBaseLogger` parameter through `evaluateTeamAccess` (needed for task 2.3's log event) and updating all ~14 call sites (content.ts, em-views.ts, action-items.ts, ws-event-dispatcher.ts, connection-reauthorization.ts, websocket-routes.ts) to pass `request.log` / the in-scope `logger`/`log` variable.
- [x] 2.2 Correct the comment block at `team-content-access-helper.ts:124-132` so it accurately describes the dual-check the code now performs — the comment currently asserts behavior the code doesn't have; this is the documentation-drift failure mode this proposal exists to close, and it applies to code comments as much as requirements docs. **Done.**
- [x] 2.3 Implement the mismatched-state (`membership_role = 'engineering_manager'`, `global_role != 'engineering_manager'`) handling per the resolution of task 1.2: return `role: 'participant'` and emit a new `team.access_grant_mismatch` structured log event (`userId`, `teamId`, mismatched `global_role`, `membership_role`) — no synchronous `audit_log` DB row. Document the log-only-given-call-volume reasoning in a code comment alongside the existing Decision 6 no-cache note (design.md Decision E). **Done** — implemented via `emitAuditEvent(logger, "team.access_grant_mismatch", ...)`, consistent with the existing `team.manager_association_rate_approaching` log-only pattern; added the event name to `AuditEventName` in `audit-logger.ts` with a comment cross-referencing the rate-limiter precedent.
- [x] 2.4 Add regression test: a user with `membership_role = 'engineering_manager'` and `global_role != 'engineering_manager'` does NOT receive `role: 'engineering_manager'` from `evaluateTeamAccess` — pins the read-side fix independently of TEAM-005, per exploration §6. **Done** — rewrote the pre-existing test in `team-content-access-helper.test.ts` that previously pinned the bug (it asserted the opposite, buggy behavior); also had to fix a second, independent test pinning the same bug in `e2e-content-auth.test.ts` (Task 11.7 describe block) discovered only when the full suite was run — see deviation note in the final implementation report.
- [x] 2.5 Add/confirm test: EM with matching `global_role` and `team_memberships.role` for a team is still granted access via Path 2 (no regression on legitimate EM access). **Confirmed** — pre-existing test already covers this; updated only for the new `logger` parameter.
- [x] 2.6 Correct `specs/team-content-access/spec.md:32` (the mismatched-state handling paragraph), which still stated the graceful-degrade-vs-403 question as "an open design decision tracked in this change's design.md and is not finalized by this requirement." Design review 2026-09-16 resolved this as Decision E; the paragraph now states that resolution directly (graceful degrade to `role: 'participant'` plus the log-only `team.access_grant_mismatch` signal, no synchronous audit row) instead of pointing to an open question that no longer exists. Applied directly as part of this task review (BA finding 1, 2026-09-16) since the edit is small and unambiguous — same failure mode tasks 5.5/5.6 correct in the requirements docs, found here in the spec delta itself.

## 3. Write-side fix — TEAM-005

- [x] 3.1 In `packages/backend/src/routes/teams.ts`, add an unconditional check in the TEAM-005 handler: if the requested transition is `participant → engineering_manager`, reject the request before performing any update. Per design.md Decision B's ordering note, this check must run after `checkAssignRolesAuthorization`'s 403 and after the no-op (`fromRole === newRole`) fast-path, but before the transaction opens (`client.connect()` / `BEGIN` / the team-level row lock) — insert it between the subject-membership lookup (`teams.ts:742-772`) and the transaction block (`teams.ts:789` onward). **Done**, ordering verified.
- [x] 3.2 Per task 1.1's resolution, the check added in 3.1 is unconditional on actor — no actor-role branch, no `actorGlobalRole` inspection. Blocks Application Admins and EM actors alike. **Done** — `if (fromRole === "participant" && newRole === "engineering_manager")` has no actor branch.
- [x] 3.3 Implement the redirective error response per Decision D and the copy/status finalized in task 1.4, following the existing pattern at `teams.ts:734-737` and `:973-976`. **Done.** Status `403` / `category: "forbidden"` confirmed at Facilitator sign-off (task 1.4); final copy names the correct action by its human name ("Establish a Manager/Team Relationship") with no internal endpoint identifier or raw path, and states the block applies to all actors including Application Admins.
- [x] 3.4 Per task 1.3's resolution, add the distinguishable audit event for blocked promotion attempts: a synchronous `audit_log` row (`operation: 'team.role_change_denied'`, carrying `actor_user_id`, `actor_global_role`, `actor_ip`, `target_user_id`, `team_id`, `metadata: { from_role: 'participant', to_role: 'engineering_manager', http_status }`) written before the error response, plus a matching `emitAuditEvent(request.log, "team.role_change_denied", ...)` call — following the `denyAdminContentAccess` precedent (`content.ts:65-89`). Fires only for actors who pass `checkAssignRolesAuthorization` (task 3.1's ordering already guarantees this). **Done.**
- [x] 3.5 Confirm the demotion path (`engineering_manager → participant`) is unchanged and untouched by 3.1-3.4 — add/confirm a regression test asserting demotion continues to succeed exactly as before for both currently-authorized actor types. **Done** — pre-existing "4.9: demotes..." test covers the Application Admin actor; added a new EM-actor demotion test ("4.5: demotion... still succeeds for an authorized EM actor") in the new regression describe block.
- [x] 3.6 Verify no feature flag, environment variable, or config value gates the check added in 3.1 (design.md Decision C). Verification method: code review at sign-off, not a runtime test. **Verified** — the check is an unconditional `if` on `fromRole`/`newRole` only; no flag, env var, or config value is read anywhere in its path.
- [x] 3.7 Retire or rewrite the three existing tests in `packages/backend/src/routes/__tests__/teams.test.ts` that assert the exact behavior this change removes. **Must land in the same commit/PR as 3.1/3.2, not as a follow-on once CI goes red** — this task is placed in §3 rather than §4's regression suite specifically to make that requirement structural rather than a note easy to miss (Architect review 2026-09-16: this was previously numbered 4.7, five tasks after §3 ends, alongside genuinely-new tests that correctly wait for §3 to finish; moved here so an implementer splitting work by section can't separate it from 3.1/3.2): **Done — plus 3 additional collateral tests found and fixed (deviation, see final report): "4.4: writes audit_log...", "4.5: does not call TEAM-006...", and "4.6: does not write users.global_role..." also exercised a participant→EM promotion and would have failed CI; rewritten to use demotion instead, preserving each test's original assertion intent.**
  - Line 336, `"4.8: promotes participant to engineering_manager and returns updated member"` — the bug's happy path, encoded as a passing test. Rewrite to assert the promotion is now rejected (folds into or is superseded by task 4.1/4.4's coverage).
  - Lines 423 and 460, the two `"4.3: ... zero Engineers"` tests — reachable only via a promotion (participant→EM) that drops the participant count to zero. Once Decision B ships, the entire zero-participant guard branch (`teams.ts:832-838`) and the `confirmedZeroParticipant` field become unreachable dead code, since the only transition remaining in the transaction block (demotion) strictly increases participant count and no-op requests already return before the transaction.
  - Decision (Full Stack Engineer's recommendation, adopted): leave the now-unreachable zero-participant guard code in place with a comment marking it defensive/unreachable-post-fix, rather than deleting it — removing it deletes a correctness guard that would matter again if a future change reopens any promotion path, and the cost of a few dead lines with an honest comment is lower than the cost of silently deleting a guard whose absence nobody will notice until it's needed. Do not delete `confirmedZeroParticipant` or the 422 response shape.

## 4. Tests — regression suite from exploration §6

- [x] 4.1 Test: an existing Team-A EM uses TEAM-005 to set a Team-A participant's role to `engineering_manager`, where that participant already holds `global_role = 'engineering_manager'` from legitimate EM status on a different team — assert rejected, no EM read grant results, no `team_memberships.role` change. **Done.**
- [x] 4.2 Test (same as 2.4, listed here for suite completeness): mismatched-state pin on `evaluateTeamAccess`. **Done** (see task 2.4).
- [x] 4.3 Test: a user with EM standing on Team A uses TEAM-005 to set their own `membership_role` on a different team from `participant` to `engineering_manager` — rejected, actor-equals-target is not an implicit exception. **Done.**
- [x] 4.4 Test: an Application Admin attempts the same `participant → engineering_manager` transition via TEAM-005 — rejected, distinct from the "unauthorized actor" 403 tests since this actor is otherwise fully authorized to call TEAM-005 (task 1.1's resolution: blocked unconditionally, including admins). **Done.**
- [x] 4.5 Test: TEAM-005 demotion (`engineering_manager → participant`) still succeeds for an authorized EM actor and for an Application Admin, unchanged from current behavior. **Done** (see task 3.5).
- [x] 4.6 Test: `audit_log` never contains a row with `operation = 'team.role_changed'` and `metadata->>'from_role' = 'participant'` AND `metadata->>'to_role' = 'engineering_manager'` after this change ships (`role_change_audit` was dropped in migration 8 and replaced by `audit_log`; this test follows the existing pattern at `teams.test.ts:511`) (spec scenario in `specs/role-assignment/spec.md`). **Done.**
- [ ] 4.7 (moved to 3.7 — retiring the three tests that pin the removed behavior must land in the same commit as 3.1/3.2, not as a separate §4 regression addition; see task 3.7)

## 5. Requirements documentation correction (blocking — same priority as the code fix)

- [x] 5.1 (Done by Full Stack Engineer, at team lead's direction — see note at end of §5) BA updates `requirements/use cases/01 - Identity and Access - Use Cases.md` UC "Assign a Role to a Team Member" AC1: reopen as unchecked (`[ ]`) pending verification against the shipped behavior, and rewrite to state that TEAM-005 supports demotion only, and that promotion is exclusively TEAM-006's function, for every actor including Application Admin — starting from the draft in exploration-notes.md §8, per task 1.1's resolution (block all actors unconditionally; the "regardless of actor" clause is now settled, not provisional). Also add a new Alternate Flow to this UC (`...Use Cases.md:201-204`): **Blocked promotion attempt** — an otherwise-authorized actor (Application Admin or EM for this team) requests `role = 'engineering_manager'` for a team member whose current `membership_role = 'participant'`; the application rejects the request and displays a redirective message identifying TEAM-006 / "Establish a Manager/Team Relationship" as the correct path, per Decision D; the team member's role is unchanged.
  - **Scope expansion (BA task review 2026-09-16, Finding 2):** three further passages in this same UC describe the now-blocked promotion path and go stale alongside AC1 — fix all of them in this task, not just AC1:
    - **Main Flow step 2** ("selects a new role... Engineer or Engineering Manager") and **step 4** (the zero-Engineers warning, gated on a promotion happening) — rewrite to state that the role selector for this UC now only ever produces a demotion (`engineering_manager → participant`) or a no-op; promotion is not offered/attempted through this flow.
    - **AC4** (currently `[x]`, the zero-Engineers-on-promotion warning) — reopen as unchecked (`[ ]`) and rewrite: this warning was reachable only via `participant → engineering_manager` promotions dropping the participant count to zero; TEAM-005's only remaining transition (demotion) strictly *increases* the participant count, so this warning can no longer fire through TEAM-005. State this outcome explicitly rather than leaving AC4 describing unreachable behavior.
    - **Alternate Flow "Role change would leave zero Engineers"** (line 204) — rewrite to note it is no longer reachable through this UC for the same reason as AC4.
    - **Do not** silently drop the safety property. Verified in code (`teams.ts:832-838` — the zero-participant guard lives only in TEAM-005's transaction block; TEAM-006's handler at `teams.ts:940` onward has no equivalent) that once this change ships, the sole remaining promotion path (TEAM-006) has **no zero-Engineers warning at all** — a real, user-facing loss of a safety property not previously named in proposal.md's Risks/Trade-offs, Non-Goals, or tasks.md. This is not a call the BA or the engineer implementing this task is authorized to make solo (design.md's Non-Goals reasonably rules out new EM capabilities, but whether a warning-only UX check counts as "new capability" is a scope-owner's call): rewrite AC4 and the Alternate Flow to state the warning is demotion-path-only and cannot fire (the safe default, consistent with existing Non-Goals), **and** flag the "should TEAM-006 get an equivalent warning" question explicitly as design.md Open Question 8 / task 1.8 (added in this task review) so it is a recorded decision awaiting the Non-Goals owner (Solution Architect / Product), not a silent gap. Do not resolve Open Question 8 unilaterally in the UC doc.
- [x] 5.2 BA updates the same UC's Out-of-Scope note ("TEAM-005 alone is sufficient for all `membership_role` writes") to reflect that establishing a new EM relationship requires TEAM-006; TEAM-005 remains sufficient only for demotion and for role changes that were never promotions.
- [x] 5.3 BA updates the sibling UC "Establish a Manager/Team Relationship" Dependencies/Notes ("TEAM-005 changes the `team_memberships.role` of an existing member and does not check `global_role`") to reflect the corrected, checked behavior.
- [x] 5.4 Verify all corrections in this task group (5.1-5.3, 5.5, 5.6) against the shipped implementation (not just the design intent) before marking this task group complete — the goal is a requirements doc that matches reality, not one that matches this proposal's intent if implementation diverges. (Supersedes the narrower "all three corrections" scope — this task group now spans two source documents, not one.)
- [x] 5.5 BA updates `requirements/design/REST API Contract.md` TEAM-005 section: rewrite the "EM role assignment constraint" note (line 421) to state the unconditional promotion block covering all actors, including Application Admin (task 1.1's resolution — same dependency as 5.1, now settled: do not leave "EM-only" language in place). Add the blocked-promotion case to the Error Responses table (line 457) with the status code and copy finalized in task 1.4, following the redirective-not-punitive shape (Decision D). This document currently states the restriction as EM-only with no corresponding statement for Application Admin actors — silent in a way that, read literally, documents admin-initiated promotion via TEAM-005 as permitted, which is the same failure mode as the Use Cases doc corrected in 5.1-5.3, found in a second document. Note also (Full Stack Engineer finding, design review 2026-09-16): the current code never implemented the EM-only restriction as written either — `checkAssignRolesAuthorization` never inspected the requested role at all — so this correction is not reversing a considered decision, only writing down what the code will now actually do.
- [x] 5.6 BA adds a dated addendum entry to `requirements/design/REST API Contract - Validation Report.md` (§3, alongside the existing 2026-03-15 "TEAM-005 / TEAM-006 authorization — RESOLVED" entry, and updating the OQ-6 row at line 188) correcting the authorization scope to reflect this change's resolution of Open Question 1 (block all actors, including Application Admin). Phrase the correction as removing an actor-scope ambiguity that was never actually resolved on 2026-03-15 (silence on the admin case, not a deliberate carve-out), not as reversing a considered decision. Append a new dated entry rather than rewriting the 2026-03-15 entry in place, consistent with this document's existing convention of leaving prior dated entries intact — unless the BA judges the original entry needs a strikethrough/correction note for a reader encountering it in isolation.

**Note on §5 authorship:** tasks 5.1-5.6 are written as BA tasks, but the team lead directed the Full Stack Engineer (this implementation pass) to complete the full tasks.md including doc corrections. All edits were made against the shipped implementation per task 5.4's verification requirement. A BA review of the wording/framing before archive is still advisable but not treated as blocking here.

## 6. Sign-off

- [x] 6.1 Security analyst confirms tasks 1.1-1.3 decisions are reflected correctly in the implementation and specs. **Confirmed 2026-09-16** (re-verified after the task 1.4 error-copy edit and the specs sync, superseding the provisional 6.1 note in implementation-review-security.md): spot-checked directly against current code and specs, not solely against my prior reviews.
  - **1.1 (block all actors, unconditional):** `teams.ts:817` — `if (fromRole === "participant" && newRole === "engineering_manager")` has no actor-role branch; confirmed no `actorGlobalRole` inspection anywhere in the check. `openspec/specs/role-assignment/spec.md:22-26` states the unconditional-on-actor block explicitly, including the target's-existing-global_role case.
  - **1.2 (mismatched-state graceful degrade, log-only):** `team-content-access-helper.ts:165-188` — `membership_role === 'engineering_manager'` with `global_role !== 'engineering_manager'` returns `role: 'participant'` (never `engineering_manager`, never a 403) and calls `emitAuditEvent(logger, "team.access_grant_mismatch", ...)` only — no synchronous `audit_log` row. Comment block at lines 148-163 correctly documents the log-only-given-call-volume reasoning. `openspec/specs/team-content-access/spec.md:48` states this resolution directly (no open-question language remaining).
  - **1.3 (blocked-attempt audit event, required):** `teams.ts:823-852` — synchronous `audit_log` INSERT (`operation: "team.role_change_denied"`) plus `emitAuditEvent(request.log, "team.role_change_denied", ...)`, both unconditional and written before the 403 response. `openspec/specs/role-assignment/spec.md:178` and `:197-202` state this requirement and scenario directly.
  - **Task 1.4 regression check:** confirmed the facilitator's error-copy edit at `teams.ts:864-873` is contained entirely within the `reply.code(403).send(...)` message block and did not touch the audit-event logic immediately above it (lines 823-852, unchanged) — text/status-only, as the task 1.4 note claims.
  - See also my standing design-level reasoning in `design-review-security.md` (Open Questions 1-3) and the fuller implementation trace in `implementation-review-security.md` §1-3; this entry is the current, re-verified sign-off of record for 6.1.
- [x] 6.2 Facilitator confirms the error copy from task 1.4 matches what shipped. — **Confirmed 2026-09-16**: the copy finalized in task 1.4 was edited directly into `teams.ts` in the same pass, so shipped code and sign-off record match by construction.
- [x] 6.3 Internal Champion (Devon) confirms: no admin-configurable exception exists (task 3.6), demotion is unaffected (task 3.5), and the requirements-doc correction (group 5 — both the Use Cases doc and the REST API Contract / Validation Report) is complete and accurate. — **Confirmed 2026-09-16**, verified directly against current code/docs rather than trusting prior checkmarks:
  - **3.6 (no admin-configurable exception):** `teams.ts:817` — `if (fromRole === "participant" && newRole === "engineering_manager")` is a bare condition on the two role values only. Grepped the file for `process.env`, `featureFlag`, `feature_flag`, `FEATURE_` — zero hits anywhere in `teams.ts`. No actor branch, no config lookup, no admin carve-out. Structural, not preferential — this is exactly the property that matters to me.
  - **3.5 (demotion unaffected):** ran the demotion tests directly (`npx vitest run packages/backend/src/routes/__tests__/teams.test.ts -t "demot"`) — both pass: "4.9: demotes engineering_manager back to participant" (Application Admin actor) and "4.5: demotion... still succeeds for an authorized EM actor." Code-read confirms the new block only intercepts `participant -> engineering_manager`; demotion falls through unchanged to the existing transaction block.
  - **Group 5 docs:** read `Use Cases.md`'s "Assign a Role to a Team Member" UC in full — AC1 (reopened and correctly rewritten, unconditional-on-actor), Main Flow steps 2/4 (role selector now demotion/no-op only), AC4 and the zero-Engineers Alternate Flow (correctly marked unreachable via this UC rather than silently dropped, with Open Question 8 flagged rather than resolved unilaterally), the new "Blocked promotion attempt" Alternate Flow (matches Decision D's redirective framing), and the Out-of-Scope note — all accurate against `teams.ts` as shipped. `REST API Contract.md`'s TEAM-005 section (Promotion block note above the Request section, and the added row in the Error Responses table) matches the shipped 403/redirect behavior. `REST API Contract - Validation Report.md`'s dated 2026-09-16 addendum (Section 3 item 3, and the OQ-6 row) is appended rather than rewritten, per the document's own convention, and correctly frames this as closing an unaddressed actor-scope ambiguity rather than reversing the 2026-03-15 decision. One pre-existing, out-of-scope inconsistency noted for the record, not blocking this sign-off: `REST API Contract.md`'s Appendix C "Open Questions Summary" table still carries an older, unrelated OQ-6 row text ("not fully validated... BA to confirm") that this change didn't touch — that row wasn't part of task 5.5/5.6's cited line numbers and its staleness predates this change.
  - **Broader ritual-intent check (per my standing concern about constraints becoming configurable, and the skill's guidance to check this at sign-off):** confirmed via `git diff main --stat` that this change's full file list is scoped to role-assignment/team-content-access code, its tests, and the three requirements docs above — nothing in `session`, `vote`, `topic`, or facilitator-assignment code paths is touched. The no-manager-participation rule, simultaneous reveal, facilitator-from-another-team requirement, and vote secrecy are unaffected by this change; nothing here trades a structural constraint for a configurable one — if anything, this change removes an actor-scope gap that could have quietly evolved into exactly the kind of exception I'd have objected to. Sign-off given.
- [x] 6.4 Solution Architect (or production-data owner) confirms tasks 1.6 (backfill/detection query), 1.7 (historical mislabeling decision), and 1.8 (TEAM-006 zero-Engineers warning question) were each acted on — decided and recorded, even if the decision is "no action needed" — within the timeboxed sprint, before this change is archived (BA task review 2026-09-16: these were tracked only as task checkboxes with no sign-off line naming who confirms they were actually closed out). — **CONFIRMED (Ingrid Sollenberger, Solution Architect, 2026-09-16).** All three have real, recorded decisions, not bare checkboxes:
  - **1.6** — decided in `design.md` Decision H: query specified (Decision G), owner assigned (on-call Production Data Engineer), timeline set (by 2026-09-23, this sprint). Mirrored in tasks.md 1.6.
  - **1.7** — decided in `design.md` Decision I: not retroactively rewritten (audit_log is append-only); going forward already corrected by Decisions A/B; historical rows get a dated additive annotation, same owner/timeline as 1.6. Mirrored in tasks.md 1.7.
  - **1.8** — decided in `sync-verify-architect.md` §2: no TEAM-006 zero-Engineers warning in this change, deferred to a separate follow-on proposal. Mirrored in `design.md`'s Open Question 8 and tasks.md 1.8.
  
  Note: 1.6 and 1.7's actual production-query *execution* is not yet complete — that work is delegated to the named owner, due 2026-09-23. This confirmation is that the decisions (owner, timeline, approach) are made and recorded, per this task's own text ("even if the decision is 'no action needed'" — here the decision is "action needed, by whom, by when," which is the equivalent bar). If archiving happens before 2026-09-23, the pending query execution should be tracked as an explicit follow-up item rather than silently dropped when this change closes.
