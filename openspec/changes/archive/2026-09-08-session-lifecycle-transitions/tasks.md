## 1. Session-Phase Entry Points (SESSION-004, SESSION-005)

**Sequencing note:** these must land first — every other transition in this change requires a session that can reach `active`, and none of it is end-to-end testable without these two endpoints existing.

- [x] 1.1 Add `StartSessionResponse` to `packages/shared/src/types/session.ts` (design.md Decision D1's "Response shapes" note — exact field list transcribed from the REST API Contract's `SESSION-004` entry), export it from `packages/shared/src/index.ts` alongside the other `session.ts` exports, for tasks 1.2/1.3 below to return.
- [x] 1.2 Implement `POST /api/v1/sessions/:sessionId/start` (`SESSION-004`) in `facilitator-sessions.ts`, following the exact `teams.ts` transaction pattern already used by the `draft → lobby` (`/advance`) handler in the same file: authorize (facilitator only), require `sessions.status = 'lobby'` (409 otherwise), `BEGIN`, `UPDATE sessions SET status = 'pre_session', started_at = NOW()`, write `audit_log` (`operation = 'session.state_changed'`, `metadata = { session_id, prior_status: 'lobby', new_status: 'pre_session' }`), `COMMIT`.
- [x] 1.3 In the same handler, query and return open/in-progress action items from the team's completed sessions (reuse the existing query shape the REST API Contract's `SESSION-004` response documents — staleness computed at query time against `application_settings.staleness_threshold_sessions`), ordered oldest-first. Confirm the empty-items case is a genuine pass-through condition, not a screen the facilitator must dismiss. Return a `StartSessionResponse` (task 1.1) from this handler.
- [x] 1.4 After the transaction commits, call `publishSessionStateChange(sessionId, { sessionId, teamId, previousStatus: 'lobby', newStatus: 'pre_session', changedAt })`.
- [x] 1.5 Add `BeginVotingResponse` to `packages/shared/src/types/session.ts` (design.md Decision D1's "Response shapes" note — exact field list transcribed from the REST API Contract's `SESSION-005` entry), export it from `packages/shared/src/index.ts`, for task 1.6 below to return. `currentTopic.sessionTopicId` in the response is `firstSessionTopicId` — the `session_topics.id` from task 1.6's query — never `firstTopicId`.
- [x] 1.6 Implement `POST /api/v1/sessions/:sessionId/begin-voting` (`SESSION-005`) in `facilitator-sessions.ts`: authorize (facilitator only), require `sessions.status = 'pre_session'` (409 otherwise), `BEGIN`. Read the session's first topic: `SELECT id, topic_id, topic_name, topic_prompt, vote_type FROM session_topics WHERE session_id = $1 AND display_order = 1` — this returns two distinct values (design.md's id-space note): `id` (`firstSessionTopicId`, a `session_topics.id`) and `topic_id` (`firstTopicId`, a `topics.id`). Then `UPDATE sessions SET status = 'active', voting_started_at = NOW(), current_topic_id = $firstTopicId WHERE id = $sessionId` and `UPDATE session_topics SET status = 'voting' WHERE id = $firstSessionTopicId` — **not** the same variable against both tables. Write `audit_log` (`operation = 'session.state_changed'`, `metadata = { session_id, prior_status: 'pre_session', new_status: 'active' }`), `COMMIT`. Both table writes are in the same transaction — neither commits without the other. Return a `BeginVotingResponse` (task 1.5) from this handler.
- [x] 1.7 After the transaction commits, call `publishSessionStateChange(sessionId, { sessionId, teamId, previousStatus: 'pre_session', newStatus: 'active', changedAt })`.
- [x] 1.8 Unit/integration tests: successful `SESSION-004` and `SESSION-005` transitions; non-facilitator rejected with 403 on both; wrong-status rejected with 409 on both; `SESSION-005`'s dual-table write is atomic (a forced failure after the `sessions` UPDATE but before the `session_topics` UPDATE leaves neither committed); `SESSION-005` asserts `sessions.current_topic_id` equals the first topic's `topics.id` (`firstTopicId`), not `session_topics.id` (`firstSessionTopicId`) — a regression test for the id-space defect both design reviews found.

## 2. Vote Lock-In / Reveal Race Fix (Prerequisite for Group 3)

**Sequencing note (design.md Decision D5):** this must land before the reveal write (Group 3) is exercised in any real end-to-end test, so no test run can observe the race this task closes. The endpoint-level concurrent-race test that exercises this fix against a live reveal request now lives in Group 3 as task 3.11, not here — the reveal endpoint has no real write logic to race against until Group 3 lands (architect review finding); this group only contains what is testable with the schema and this fix alone.

- [x] 2.1 In `sessions.ts`'s lock-in handler, inside its existing transaction (already using the `teams.ts` pattern for the vote-submission audit write), add `SELECT status FROM session_topics WHERE id = $1 FOR UPDATE` immediately before the `INSERT INTO votes ... ON CONFLICT ... DO UPDATE`.
- [x] 2.2 If the selected `status !== 'voting'`, `ROLLBACK` and return `422` with the handler's existing `invalid_request` error category, message distinguishing "voting has closed for this topic" from the handler's existing EM-rejection and non-participant-403 messages. (design.md Decision D5: `422`, not `409` — matches the handler's pre-existing, structurally-identical `topic_status !== 'voting'` check at `sessions.ts:213-221`, so the same failure condition returns the same code regardless of which check path catches it.)
- [x] 2.3 Integration test: lock-in rejected with 422 when `session_topics.status` is `revealed` at the time of the request.

## 3. Reveal Write (voting → revealed)

**Depends on: Group 2.**

- [x] 3.1 In `facilitator-sessions.ts`'s existing `POST .../reveal` endpoint, refactor from its current bare-validation-only body to the `teams.ts` transaction pattern. Add `current_topic_id` to the endpoint's existing precondition `SELECT` (`facilitator-sessions.ts:498-501` currently selects `id, team_id, facilitator_id, status` only) — the handler needs `topicId = sr.current_topic_id` (a `topics.id`, per design.md's id-space note) in scope before the transaction opens; without it there is no value to identify which `session_topics` row to transition.
- [x] 3.2 Add `RevealAlreadyRevealedResponse` to `packages/shared/src/types/team-content-access.ts` (design.md Decision D3's exact shape), for task 3.4 below to return.
- [x] 3.3 Add the conditional-`UPDATE` precondition write (design.md Decision D2), keyed by `session_id` + `topic_id` rather than an assumed `session_topics.id`: `UPDATE session_topics SET status = 'revealed', revealed_at = NOW() WHERE session_id = $1 AND topic_id = $2 AND status = 'voting' RETURNING id, revealed_at` (`$1 = sessionId`, `$2 = topicId` from 3.1). Check `rowCount`.
- [x] 3.4 If `rowCount === 0`: `ROLLBACK`, then run a follow-up `SELECT id, revealed_at FROM session_topics WHERE session_id = $1 AND topic_id = $2` to populate the response (this read is for response content only, not concurrency control — D2's conditional `UPDATE` above is the sole lock). Return `409` with the new `already_revealed` error state (`RevealAlreadyRevealedResponse`, from task 3.2: `errorState: "already_revealed"`, `sessionId`, `teamId`, `sessionTopicId` = the row's `id`, `revealedAt`).
- [x] 3.5 If `rowCount === 1`: the `RETURNING` row's `id` is `sessionTopicId`. Call `recordRevealTriggeredAudit` (already built and unit-tested, `facilitator-sessions.ts` lines 31–63) in the same transaction, then `COMMIT`. Remove its `TODO(#26)` comment — this is now the real call site.
- [x] 3.6 After the transaction commits (never before, never inside it), call `publishVoteRevealed(sessionId, { sessionId, sessionStatus })` (already built, `ws-pubsub.ts`). Remove its `TODO(#26)` comment.
- [x] 3.7 Add `TopicAdvanceBlockedResponse` to `packages/shared/src/types/team-content-access.ts` (design.md Decision D3's exact shape), consumed by Group 4's task 4.3.
- [x] 3.8 Integration test: successful reveal transitions `session_topics.status` to `revealed`, writes one `session.reveal_triggered` audit row, and publishes exactly one `vote_revealed` event.
- [x] 3.9 Integration test: a second reveal on an already-revealed topic returns `409`/`already_revealed`, writes no additional audit row, and publishes no additional event.
- [x] 3.10 Integration test: two concurrent reveal requests for the same topic produce exactly one committed transition and exactly one published event (design.md Decision D2's concurrency claim, verified against the real row lock — not asserted from code inspection alone).
- [x] 3.11 **Required test case named explicitly in design.md Decision D5 (closes out Group 2's prerequisite fix) — distinct from task 3.10's duplicate-reveal test:** a lock-in request and a live `POST .../reveal` request for the same `session_topic_id`, issued concurrently while the topic is `voting`. Assert the two requests resolve to a single consistent outcome: either the vote commits and is included in the reveal, or the reveal commits first and the lock-in is rejected. Assert no vote is ever inserted after the reveal transaction for that topic has committed. This is the first point in the task list where a real reveal request exists to race against (architect review finding) — do not run this test, or attempt to write it, before task 3.5 lands.
- [ ] 3.12 Run the WebSocket change's previously-blocked end-to-end task for `vote_revealed` (tasks 11.1's reveal-triggered portion, `websocket-delivery-time-authorization`'s tasks.md) against this implementation and confirm it now passes.
- [x] 3.13 Integration test (design.md Decision D3's ordering requirement): a non-facilitator's reveal attempt against an already-revealed topic still returns the existing generic 403 — never `already_revealed`. Confirms authorization runs, and passes, strictly before the precondition check.

## 4. Topic Advance and Wrap-Up Entry (SESSION-012)

**Depends on: Group 3** (requires a real `revealed` topic to test its precondition).

- [x] 4.1 Implement `POST /api/v1/teams/:teamId/sessions/:sessionId/topics/advance` (`SESSION-012`) as a new route in `facilitator-sessions.ts`, using the `teams.ts` transaction pattern. Fetch the session row including `current_topic_id` (needed as `topicId` below, per design.md's id-space note). Authorize with **both** checks the existing `advance`/`complete` handlers perform for the same two-path-param shape (design.md Decision D4's "Authorization, named explicitly" note) — not "facilitator only" alone: (1) `sessionRow.team_id !== teamId → 403`, and (2) `sessionRow.facilitator_id !== userSession.userId → 403`. Both checks must complete, and pass, before task 4.2's precondition check runs.
- [x] 4.2 Add the conditional-`UPDATE` precondition write on the current topic (design.md Decision D2's pattern, applied here), keyed by `session_id` + `topic_id` rather than an assumed `session_topics.id`: `UPDATE session_topics SET status = 'complete', completed_at = NOW() WHERE session_id = $1 AND topic_id = $2 AND status = 'revealed' RETURNING id` (`$1 = sessionId`, `$2 = topicId` from 4.1). Check `rowCount`.
- [x] 4.3 If `rowCount === 0`: `ROLLBACK`, return `409` with `TopicAdvanceBlockedResponse` (from task 3.7: `errorState: "advance_blocked"`, `requiresReveal: true`).
- [x] 4.4 If `rowCount === 1`: the `RETURNING` row's `id` is `completedSessionTopicId` (a `session_topics.id`). Query whether a next topic exists in `display_order` for this session, selecting the denormalized topic fields directly off `session_topics` (no join to `topics` — see design.md Decision D4a's corrected "Query cost" note): `SELECT id, topic_id, topic_name, topic_prompt, vote_type FROM session_topics WHERE session_id = $1 AND display_order = (SELECT display_order + 1 FROM session_topics WHERE session_id = $1 AND topic_id = $2)`. If found, this row's `id` is `nextSessionTopicId` (`session_topics.id`) and `topic_id` is `nextTopicId` (`topics.id`) — two distinct values, per design.md's id-space note.
- [x] 4.5 Add `session.topic_advanced` to the `AuditEventName` union in `audit-logger.ts`, following the file's existing naming/commenting convention. Must land before task 4.7 below, which writes an `audit_log` row using this operation value — `AuditEventName` is a closed union, so 4.7's write does not compile until this task lands.
- [x] 4.6 Add `TopicAdvanceResponse` to `packages/shared/src/types/session.ts` (design.md Decision D4a's exact field list), export it from `packages/shared/src/index.ts`, for tasks 4.7/4.8 below to return. `currentTopic.sessionTopicId` is `nextSessionTopicId`, never `nextTopicId` — mirrors the 1.5/4.7 id-space rule.
- [x] 4.7 **Next-topic branch:** `UPDATE session_topics SET status = 'voting' WHERE id = $nextSessionTopicId` (not `$nextTopicId`); `UPDATE sessions SET current_topic_id = $nextTopicId WHERE id = $sessionId` (not `$nextSessionTopicId`) — the two writes use different variables, sourced from 4.4's lookup; write `audit_log` with the `operation = 'session.topic_advanced'` value (task 4.5), `metadata = { session_id, completed_session_topic_id: completedSessionTopicId, new_session_topic_id: nextSessionTopicId }`; `COMMIT`; return a `TopicAdvanceResponse` (task 4.6) with `status: 'active'`, `completedTopic.sessionTopicId = completedSessionTopicId`, and `currentTopic` populated from the next topic's row (`currentTopic.sessionTopicId = nextSessionTopicId`).
- [x] 4.8 **Wrap-up-entry branch (no next topic):** `UPDATE sessions SET status = 'wrap_up', wrap_up_started_at = NOW(), current_topic_id = NULL WHERE id = $sessionId`; write `audit_log` with the existing `operation = 'session.state_changed'` value, `metadata = { session_id, prior_status: 'active', new_status: 'wrap_up', completed_session_topic_id: completedSessionTopicId }` — include `completed_session_topic_id` here too (design.md Decision D4's audit note; security review finding), matching the topic-to-topic branch, so the audit log alone identifies which topic was completed at wrap-up entry without cross-referencing `session_topics.completed_at`; `COMMIT`; return a `TopicAdvanceResponse` (task 4.6) with `status: 'wrap_up'`, `completedTopic.sessionTopicId = completedSessionTopicId`, and `wrapUpStartedAt` (no `currentTopic`).
- [x] 4.8a **Post-implementation-review addendum (Tomás, security review), not a gap in 4.7/4.8's wording above but a gap in what got built from it:** both branches wrote the `audit_log` DB row correctly but neither called `emitAuditEvent` afterward — the structured-log counterpart every other state-transition endpoint in this file (draft→lobby, start, begin-voting, complete, reveal via `recordRevealTriggeredAudit`) already emits for operational alerting. Not a lost record (the DB row is authoritative and was always written), but it would not surface in whatever log stream ops/SIEM watches. Fixed: both branches now call `emitAuditEvent(request.log, <same operation value as their audit_log write>, {...})` after commit, before the publish call(s), matching the existing pattern exactly. Covered by dedicated assertions added to tasks 4.11/4.13's tests.
- [x] 4.9 After commit, next-topic branch: call `publishTopicHistoryUpdate(teamId, { teamId, updateType: 'topic_advanced', sessionId, topicId: nextTopicId, updatedAt })` (already built, `ws-pubsub.ts`; remove its `TODO(#26)` comment — this is now a real call site, alongside 5.x below).
- [x] 4.10 After commit, wrap-up-entry branch: call both `publishSessionStateChange(sessionId, { sessionId, teamId, previousStatus: 'active', newStatus: 'wrap_up', changedAt })` and `publishTopicHistoryUpdate(teamId, { teamId, updateType: 'topic_advanced', sessionId, topicId: <the just-completed last topic's topic_id>, updatedAt })` for the last topic's completion.
- [x] 4.11 Integration test: advancing with a next topic available transitions the current topic to `complete`, the next to `voting`, updates `sessions.current_topic_id` to the next topic's `topics.id` (not its `session_topics.id` — a regression test for the id-space defect both design reviews found), keeps `sessions.status = 'active'`, publishes one `topic_history_update` event, and returns a `TopicAdvanceResponse` with `status: 'active'` and a populated `currentTopic` (design.md Decision D4a).
- [x] 4.12 Integration tests (design.md Decision D4's authorization note; security review finding): (1) a facilitator of a *different* session attempting `SESSION-012` against this session's URL is rejected with 403 (team-id cross-check), matching the existing `advance`/`complete` test pattern; (2) a non-facilitator's advance attempt against a not-yet-revealed topic still returns the existing generic 403 — never `advance_blocked` (auth-before-precondition ordering, mirroring task 3.13).
- [x] 4.13 Integration test: advancing past the final topic transitions `sessions.status` to `wrap_up`, sets `wrap_up_started_at`, clears `current_topic_id`, publishes both a `session_state_change` and a `topic_history_update` event, and returns a `TopicAdvanceResponse` with `status: 'wrap_up'`, `wrapUpStartedAt` set, and no `currentTopic`.
- [x] 4.14 Integration test: advancing before the current topic is revealed returns `409`/`advance_blocked` with `requiresReveal: true`, and modifies no state.
- [ ] 4.15 Run the WebSocket change's previously-blocked end-to-end task for `topic_history_update` (tasks 5.6/11.2's real-trigger portion, `websocket-delivery-time-authorization`'s tasks.md) against this implementation and confirm it now passes.

## 5. Frontend Integration Notes (Named, Not Implemented Here)

- [ ] 5.1 **Named for the frontend implementation task, not built in this backend-scoped change (design.md Decision D4's "known integration gap"):** the client must subscribe to both its session channel (for `vote_readiness_update`/`session_state_change`/`vote_revealed`) and its team channel (for `topic_history_update`) to receive topic-to-topic advance notifications during a live session — `topic_history_update` is delivered via the team-scoped registry by design, not a bug to fix on the backend.
- [ ] 5.2 **UX acceptance task, not a pure API contract detail (design.md Decision D3):** confirm the frontend renders the `already_revealed` response indistinguishably from a successful reveal (no error chrome), and renders the `advance_blocked` response with the reveal action offered directly, not as a dead end. This is an acceptance condition for this change's UX requirement, tracked here so it is not silently satisfied by "the backend returns the right status code."

## 6. REST API Contract Corrections

**Sequencing note (architect review):** 6.1–6.3 correct contract text that design.md has already fully determined and carry no code dependency — per design.md's Migration Plan, they may proceed in parallel with Groups 1–4, not after them. 6.4 and 6.5 each carry their own harder gating condition, stated in their own task below.

- [x] 6.1 Add a new `SESSION-012` entry to `requirements/design/REST API Contract.md`, fully specified: method/URL, request, the `TopicAdvanceResponse` success shape for both branches (transcribed from design.md Decision D4a — already fully designed, not invented at this step), authorization, error responses including `409`/`advance_blocked` (Decision D3), following the existing `SESSION-00X` entry format.
- [x] 6.2 Rewrite Appendix D's `reveal.trigger` and `topic.advance` rows, **and `SESSION-005`'s own contract entry** (Description and Notes, REST API Contract lines ~1268/~1316 — currently state `SESSION-005` "triggers the WebSocket `topic.advanced` broadcast," which does not match what design.md Decision D1 actually specifies `SESSION-005` calls: `publishSessionStateChange`), to describe the actual architecture: an authenticated REST trigger (already-existing `POST .../reveal` and the new `SESSION-012`), followed by a single simultaneous WebSocket broadcast for fan-out, publish-after-commit. Narrow the rationale column so it no longer implies the trigger transport itself is the integrity mechanism. Leave `vote.submit`'s REST exclusion untouched.
- [x] 6.3 Correct `VOTE-003`'s note (currently: "The reveal itself is triggered via WebSocket (`reveal.trigger`), not this endpoint") to match the corrected Appendix D text.
- [x] 6.4 Correct the stale "action item finalization" framing in GitHub issue #26's own text and in the WebSocket change's dispatcher comments (`ws-pubsub.ts`, `ws-event-dispatcher.ts`) to state plainly that `topic_history_update`'s real trigger set is topic-to-topic advance only — finalization is already satisfied by the existing session-complete write. No code change; comment/issue-text correction only. **Sequencing (security review, confirmed by architect review):** do this immediately after Group 4 lands — not deferred further, and not held for Groups 5, 7, or 8, which have no bearing on it. `audit-logger.ts`'s `AuditEventName` comment block currently says `session.reveal_triggered`/`session.state_changed` are blocked on issue #26, and leaving that stale comment in place any longer than necessary after the real call sites exist risks it rotting into a misleading audit-code paper trail.
- [x] 6.5 Specify the correction to `websocket-session-authorization`'s Purpose section "Production trigger status" note (`openspec/specs/websocket-session-authorization/spec.md`), gated on Groups 3 and 4 both being wired in: remove the "not yet wired to any production trigger" language for `vote_revealed` and `topic_history_update`, consistent with how the WebSocket change itself corrected the archived `team-content-access` spec's "gate-blocked" note. This is a Purpose-section edit outside the delta-spec Requirements format. **This task documents the correction's content and gating condition only; task 7.3 performs the actual edit, at spec-sync time** (BA review: 6.5 and 7.3 previously read as two tasks doing the same thing — this is the disambiguation).
- [x] 6.6 Confirm the topic-skip escape-hatch timing correction (proposal.md's "Topic-Skip Decision" and this change's `specs/session-topic-lifecycle/spec.md` "Mid-session topic-skipping is not supported" requirement — both corrected during propose-stage BA review to say "before the session is created," not "before the session starts," citing `SESSION-001`'s snapshotting behavior as the mechanism) carries through unchanged into the synced spec at task 7.1, so it isn't silently dropped or re-drifted alongside the Appendix D corrections.

## 7. Spec and Documentation Finalization

- [x] 7.1 Sync the delta spec (`specs/session-topic-lifecycle/spec.md` in this change) into `openspec/specs/session-topic-lifecycle/spec.md` as a new capability.
- [x] 7.2 Sync the delta spec (`specs/team-content-access/spec.md` in this change) into `openspec/specs/team-content-access/spec.md`, replacing the "Live session error states" requirement with its extended version (Error States 1a/1b added).
- [x] 7.3 Perform task 6.5's Purpose-section correction on the synced `openspec/specs/websocket-session-authorization/spec.md`, as part of this group's spec-sync step (alongside 7.1/7.2). This is the actual edit; 6.5 documents its content and gating condition — see 6.5 for what to write and why it must wait for Groups 3 and 4.
- [x] 7.4 File the deferred team-membership-removal change as its own OpenSpec change stub, referencing this change's Out of Scope decision and its dependency on GitHub issue #23. Owner: Marcus Delgado (BA), per the exploration notes' Section 3 recommendation.
- [x] 7.5 File the topic-skip / creation-time topic-list-confirmation follow-on as its own OpenSpec change stub, referencing this change's "Topic-Skip Decision" caveat (facilitators get no signal a post-creation topic-list edit didn't take) and Priya Nair's facilitator-initiated-skip scenario from the Facilitator's explore-stage review. Owner: Marcus Delgado (BA), per the same exploration-notes recommendation that names ownership for task 7.4.

## 8. End-to-End Verification

- [x] 8.1 Full session run-through, exercising every transition this change adds in sequence: `lobby → pre_session → active`, vote lock-in, reveal, topic-to-topic advance (at least once), reveal on the final topic, advance into `wrap_up`, existing `wrap_up → complete` (unmodified by this change). Confirm no regression in the existing session-close (`SESSION-006`) or lobby-advance behavior.
- [x] 8.2 Confirm no regression in HTTP content-access authorization or WebSocket delivery-time authorization (both from the archived changes this one builds on) — this change adds real triggers to existing hooks, it does not modify the authorization or dispatch layers themselves.
- [x] 8.3 Confirm the vote lock-in race fix (Group 2) does not regress the existing vote-submission audit logging or the `ON CONFLICT ... DO UPDATE` resubmission behavior for votes submitted before reveal.
