## 1. Scope gates (must clear before implementation-ready)

- [x] 1.1 Draft copy for the empty state message, each staleness tier's badge/label text (not just colors), and the summary line (both its "N items need attention" form and its neutral no-stale-items variant); get stakeholder approval. Per proposal.md, this is a gate, not a polish pass — do not begin frontend rendering work ahead of this. **Rachel Okonkwo (VP of Engineering) personally reviews the drafted staleness-tier and empty-state copy before it ships to a pilot team** — route the draft to her directly and record her sign-off; the gate is not satisfied by generic stakeholder approval alone for those two pieces. **Implementation note:** drafted to reviewed-quality bar in `copy.md` (no live stakeholder session available during implementation); Rachel's actual sign-off is still outstanding and must be recorded before pilot rollout — see `copy.md`'s "Outstanding sign-off" section. Frontend rendering proceeds on this draft per the same reasoned-default approach used for 1.2.
- [x] 1.2 Confirm item ordering (chronological oldest-first vs. stale-first) with stakeholders and record the decision; update design.md Decision 6 with the outcome before task 5.2 below is implemented. **Corrected during task review (architect finding #1):** the original text pointed at "task 4.x," but section 4 (WebSocket subscription, fetch/branch logic, Start Session control) never touches item ordering — the actual consumer is 5.2, which renders the list per this decision. **Implementation note:** no live stakeholder available; reasoned default recorded in design.md Decision 6 — stale-first (red/orange/yellow/none, age as tiebreaker), per the Facilitator persona's explore-review preference. Still needs real stakeholder confirmation.
- [x] 1.3 File the follow-up issue for inline status-update controls (owner-on-own-item, facilitator-on-any-item), scoped explicitly to: rendering the controls, wiring to the already-shipped `PATCH /api/v1/action-items/:actionItemId/status`, and the fetch-then-subscribe race this change's design.md flags for that follow-up. File before this change merges, **with a target of merged within 2 weeks of this change's own merge** (per executive review — this is a wiring task against already-shipped endpoints, not new design work, so an open-ended "fast follow" isn't acceptable). Filed as GitHub issue #103.

## 2. Backend: staleness mapping fix (blocking — complete before any task in Section 3)

**Corrected during task review (architect finding #2 / BA finding 1):** the original header read "blocking, do before 2.4/2.5" — no such tasks exist in this document (a leftover from an earlier numbering pass). Section 3's own 3.1 already establishes this fix as a prerequisite for the whole section, not just two sub-tasks, so the pointer is dropped rather than replaced with a narrower one.

- [x] 2.0a Fix `computeStalenessLevel` (`facilitator-sessions.ts`) to compare `sessionsSinceUpdate` against the fixed literals 1/2/3 directly (`none` at 0, `yellow` at 1, `orange` at 2, `red` at 3+), removing its `threshold` parameter and its read of `application_settings.staleness_threshold_sessions`. Per design.md Decision 7 — this corrects already-shipped `SESSION-004` behavior to match UC: Flag Stale Action Items' decided requirement; it is not new scope creep into "how thresholds are configured" (that setting's row/migration are left untouched, just unread).
- [x] 2.0b Add a unit/route test asserting the legend's stated thresholds (1/2/3 sessions → yellow/orange/red) and `computeStalenessLevel`'s actual output agree, including at the current `application_settings.staleness_threshold_sessions` default value in the seed data — this is the regression guard called for by the finding that caught the original drift.
- [x] 2.0c Confirm via a quick check of `POST /start`'s existing tests (if any assert specific staleness colors) that this fix doesn't silently break an assertion that was itself written against the pre-fix multiplier behavior; update any such assertion to the corrected mapping.

## 3. Backend: review endpoint

- [x] 3.1 Export `fetchPreSessionActionItems` from `packages/backend/src/routes/facilitator-sessions.ts` (or move to a shared module) without changing its signature or query behavior (the staleness fix above is a separate, already-completed prerequisite, not part of this export step).
- [x] 3.2 Add `GET /api/v1/sessions/:sessionId/action-items-review` calling `evaluateSessionSubscriberAccess(userId, sessionId)`; return `404` on a `null` grant. Start a timing-floor clock (`Date.now()`) at handler entry. **Apply `applyTimingFloor()` and set `Cache-Control: no-store` on the `404` response itself, inline, before returning it** (design.md's F1/F2 — see task review architect finding #4: these are design requirements, not later hardening, so they're built into each response as it's constructed rather than bolted on afterward in 3.6).
- [x] 3.3 Add the explicit `sessions.status` read and `pre_session`-only gate (uniform across both grant variants, per design.md Decision 1); return `409` for any other status, with body `{ currentSessionStatus, isFacilitator }` (design review engineer finding #2/#3 — `isFacilitator` is derived from `grant.path`, already resolved before this gate runs, so it costs nothing extra to include here). **Apply `applyTimingFloor()` (clock from 3.2) and set `Cache-Control: no-store` on this `409` response, inline, before returning it** (F1/F2).
- [x] 3.4 Call `fetchPreSessionActionItems(grant.teamId)` and return `{ actionItems, isFacilitator: grant.path === 'facilitator' }` on success. **Apply `applyTimingFloor()` (clock from 3.2) and set `Cache-Control: no-store` on this `200` response, inline, before returning it** (F1/F2), matching the same treatment given to the `404`/`409` paths in 3.2/3.3.
- [x] 3.5 Add route tests: authorized participant success, authorized facilitator success, no-grant 404, Engineering Manager 404 (inherited from `evaluateSessionSubscriberAccess`), wrong-session-status 409 (both `lobby` and `active` cases, asserting `currentSessionStatus` and `isFacilitator` in the body), response shape/`isFacilitator` correctness for both grant paths on `200`.
- [x] 3.6 **Design review security findings F1/F2 — cross-cutting regression tests.** **Narrowed during task review (architect finding #4):** with `applyTimingFloor()` and `Cache-Control: no-store` now applied inline in 3.2/3.3/3.4 as each response is built (not deferred to this task), 3.6 covers only the net-new regression coverage that legitimately needs all three response paths to already exist: a route test asserting denied (`404`/`409`) and authorized (`200`) responses are not detectably faster than each other at the floor (mirrors the existing `content.ts` timing test), and a test asserting `Cache-Control: no-store` is present on all three response codes.

## 4. Frontend: session-status awareness in SessionLobbyPage

**Revised during design review (engineer finding #2):** the original task 4.1 called for fetching status via `GET /api/v1/teams/:teamId/sessions/:sessionId` (content.ts). Rejected — that route requires a `:teamId` path param `SessionLobbyPage`'s actual route (`/session/:sessionId`) does not have, and gates on a different authorization mechanism (`evaluateTeamAccess`) than the new endpoint (`evaluateSessionSubscriberAccess`). See design.md Decision 3.

**Restructured during task review (architect finding #3):** the original document put this section's error-handling and Start-Session-retry tasks in a separate section 6 ("error states"), two full sections later, creating an unflagged forward dependency (4.2 forward-referenced 6.1; 6.1/6.2 referenced back to 4.2). Per the architect's preferred resolution, those tasks are folded in here as 4.3/4.4 (GET failure handling, next to the fetch logic they extend) and 4.6 (Start Session retry, next to the control it extends) — section 6 as a standalone "error states" bucket was otherwise just satellites of sections 4 and 5.

- [x] 4.1 Establish the WebSocket subscription to the session's `session_state_change` event *before or concurrently with* the initial data fetch below, not after (design review engineer finding #4 / design.md Risks — closes the fetch-then-subscribe race for the `start`/`begin-voting` transitions this change wires in).
- [x] 4.2 On mount, call `GET /api/v1/sessions/:sessionId/action-items-review` (the Decision 1 endpoint) directly — no separate status call. Branch on the response: `200` → `pre_session` (render the review component with the returned data, task 5.1); `409` with `currentSessionStatus: 'lobby'` → `lobby` branch (existing waiting message + new Start Session control, task 4.5); `409` with any other `currentSessionStatus` → transition out of the page; `404` → existing no-access handling; network/5xx failure → the error state from task 4.3 (this is the same fetch's error state, not a second call).
- [x] 4.3 (folded in from the original section 6, per task review architect finding #3) Render an explicit error state with retry when the review GET (task 4.2) fails with a network error or 5xx, for any participant including the facilitator (no blank screen, no indefinite spinner) — see design.md Decision 3/4's reconciliation.
- [x] 4.4 (folded in from the original section 6, per task review architect finding #3) Keep "Begin First Topic" disabled until the same review GET (task 4.2/4.3) has succeeded — one gate, not two. **Corrected during design review (engineer finding #3):** the original task gated the control on `POST /start`'s payload; under this change's actual architecture that payload never reaches the rendered screen, so it was replaced. Add a generic advance-failure fallback message on `begin-voting` itself that does not assume a clean 4xx (covers the pre-existing "no topics configured" 500).
- [x] 4.5 Add the facilitator-only "Start Session" control to the `lobby` branch (design review engineer finding #1 — resolves the previously-unscoped gap that nothing in the codebase could move a session out of `lobby`). Visible only when the `409` response's `isFacilitator` field is `true`. Wire it to `POST /api/v1/sessions/:sessionId/start`; on failure, show inline retry and remain on `lobby` (no new backend behavior — `/start` is unmodified).
- [x] 4.6 (folded in from the original section 6, per task review architect finding #3) Render an inline retry affordance on the `lobby` branch when the new Start Session control's `POST /start` call (task 4.5) fails; the page remains on `lobby`.
- [x] 4.7 On receiving `session_state_change` via the WebSocket subscription (task 4.1), re-branch per task 4.2's logic (re-fetch task 4.2's endpoint, or trust the event's `newStatus` directly, per implementer's choice — either satisfies the race mitigation as long as the subscription from 4.1 was live before the transition committed).

## 5. Frontend: review component

- [x] 5.1 Render from the same `200` response task 4.2 already fetched to determine the `pre_session` branch — no second call to the review endpoint. Render `isFacilitator` from that response for use by task 5.5.
- [x] 5.2 Render each action item read-only: description, owner display name, status label, originating session number (`originatingSessionNumber`, displayed as "Session #N" — not `originatingSessionId`), staleness color — per the ordering decision confirmed in task 1.2.
- [x] 5.3 (folded in from the original section 6, per task review architect finding #3) Render items with a failed per-item staleness computation without a staleness indicator, without blocking the rest of the screen.
- [x] 5.4 Render the always-visible staleness legend (1/2/3+ sessions → yellow/orange/red, matching the corrected `computeStalenessLevel` from task 2.0a) and the one-line summary at the top of the list (always shown; "N items need attention" or the neutral no-stale-items variant), using the copy approved in task 1.1.
- [x] 5.5 Render the empty state (using approved copy) when `actionItems` is empty, identical for all participants; render the facilitator's advance control only when `isFacilitator` is true.
- [x] 5.6 Wire the "Begin First Topic" control to `POST /api/v1/sessions/:sessionId/begin-voting`; keep it disabled until the review data has loaded successfully (the same single gate task 4.4 establishes).

## 6. Verification

- [x] 6.0 Manual check (**new, resolves design-review engineer finding #1 — this is the check that was previously impossible to run**): a facilitator can actually move a fresh session from `lobby` to `pre_session` in the running application using only the new Start Session control, with no manual DB/API intervention. All subsequent checks below (6.1-6.7) depend on this working. **Verified against the real running app** (real Postgres/Redis, real backend+frontend dev servers, headless-Chromium-driven browser sessions) — confirmed working. **Blocked initially by two pre-existing, unrelated local-auth bugs** that prevented any login at all (wrong callback hostname in `auth.ts`; broken hardcoded JWKS in the simulated OIDC provider) — diagnosed, worked around locally (uncommitted, reverted after verification) to unblock this checklist, and filed as GitHub issue #104 for a real fix. No repo files changed by that workaround.
- [x] 6.1 Manual/integration check: two browser sessions (facilitator + participant) both land on the review screen simultaneously on session start, and both leave it simultaneously when the facilitator advances. Confirm both transitions (`lobby → pre_session` via Start Session, `pre_session → active` via Begin First Topic) are received via the WebSocket subscription established before the transition, not only via page refresh (design.md Risks — session-state-transition race). **Verified**: both the facilitator's and participant's browser sessions transitioned to the review screen together on Start Session, and off it together on Begin First Topic, with no manual refresh in either browser.
- [x] 6.2 Manual check: a session with zero open action items (first-ever session, or all-resolved) shows the empty state identically to both roles, facilitator can advance immediately. **Verified**: empty-state copy rendered, Begin First Topic control present and clickable for the facilitator.
- [x] 6.3 Manual check: a late joiner arriving after the session has moved to `active` does not see the review screen or receive review data (409 from the endpoint, `currentSessionStatus: 'active'`). **Verified**: a fresh navigation to an already-`active` session rendered the "moved past the review" branch, no review data, no facilitator controls.
- [x] 6.4 Confirm no Engineering Manager account can reach the review screen, the Start Session control, or the review endpoint's data under any test account setup. **Verified**: an account with `global_role = 'engineering_manager'` and an EM team membership got `404` directly from the endpoint and the no-access branch in the browser — no review data, no controls.
- [x] 6.5 Manual check: with an item at exactly 3 sessions elapsed, confirm it renders `red` (not `yellow`) — the concrete case the staleness-mapping fix (task 2.0a) corrects. **Verified**: an item seeded at exactly 3 completed sessions elapsed rendered "Carried over 3+ sessions" (red), matching the fixed mapping, not the pre-fix multiplier behavior's `yellow`.
- [x] 6.6 Confirm the review endpoint's `404`/`409`/`200` responses all carry `Cache-Control: no-store` and are not distinguishable by response timing (design review security findings F1/F2; covered by the route tests in task 3.6, spot-checked here against the running app). **Spot-checked**: `404` and `409` responses from the running app both carry `Cache-Control: no-store`; `200` confirmed via the automated route tests (task 3.6) — response-timing indistinguishability is a route-test-level property (task 3.6), not independently re-measured by hand here.
- [x] 6.7 (**new, resolves task review BA finding 3**) Manual check: the review screen shows only the caller's own team's items, with no ranking, aggregation, or cross-team/cross-owner comparison UI anywhere on screen — converts spec.md's "no cross-team or cross-owner comparison" scenario (satisfied by absence of a feature) from an assumption into something explicitly confirmed. **Verified**: rendered screen showed only this team's 4 items with per-item facts (description/owner/status/session/staleness) and a single plain count in the summary line — no ranking, leaderboard, or cross-item comparison UI anywhere.
