## 1. Shared session-expiry detection helper

- [x] 1.1 Add a new shared frontend module implementing `detectSessionExpiry(res: Response): Promise<{ isSessionExpired: boolean; body: unknown }>` (or equivalent name/shape) per design.md Decision 1: owns the single `.json()` body read (resolves `body: null` on a non-JSON or already-consumed body, never throws), checks `status === 401 AND` parsed `error.category === "session_expired"`, accepts no pre-parsed body from callers, and returns the parsed body alongside the boolean so a caller that gets `isSessionExpired: false` on a 401 (e.g. `category: "provider_unavailable"`) can still derive its own generic-error message from `body` without a second read.
- [x] 1.2 Unit tests: a 401 with `category: "session_expired"` is recognized (`isSessionExpired: true`); a 401 with any other/absent category is not (`isSessionExpired: false`, `body` still populated with the parsed object so the caller can read `body.error.message`); any non-401 status is never recognized regardless of body content; the helper does not throw on a non-JSON or empty body (`body` resolves to `null`).

## 2. Backend: returnTo allow-list additions

- [x] 2.1 Add `RETURN_TO_ALLOW_LIST` entry for `/team/:teamId/session/:sessionId` (both segments UUID-pinned, optional trailing query string), matching the existing entries' style (`packages/backend/src/routes/auth.ts`).
- [x] 2.2 Add `RETURN_TO_ALLOW_LIST` entry for the literal path `/sessions/new` (optional trailing query string), per design.md Decision 3's note that this is a new, non-UUID shape class for this list.
- [x] 2.3 Tests: `/team/<uuid>/session/<uuid>` is accepted; `/sessions/new` is accepted; `/sessions/new?foo=bar` is accepted (exercises design.md Decision 3's query-string-tolerance clause as an actual accepted-case test, not just prose); `/sessions/new/anything` and `/sessions/newer` are rejected; existing `/session/:id` and `/team/:id` scenarios still pass unchanged (regression).

## 3. SessionLobbyPage.tsx

- [x] 3.0 Add the shared idempotency gate per design.md Decision 2's "Structural home for the gate": one page-level `reauthRequired` state (`{ role: "facilitator" | "participant"; returnTo: string } | null`, initially `null`), set only via `setReauthRequired((prev) => prev ?? { role, returnTo })` from every signal source in 3.1–3.4 below (never overwritten once non-null), and one top-level early return — `if (reauthRequired) return <ReauthRequiredTreatment role={reauthRequired.role} returnTo={reauthRequired.returnTo} />;` — placed before the existing `branch`/`startError`/`beginVotingError` JSX tree. This is the single shared mechanism tasks 3.1–3.4 wire into; none of those four render `ReauthRequiredTreatment` directly.
- [x] 3.1 Wire the shared helper into the `action-items-review` GET call site (`fetchReview`): on a recognized session-expiry, call `setReauthRequired` (via 3.0's gate) with `returnTo` = current path+query and `role` computed per design.md Decision 1a (`session?.canFacilitateSessions ? "facilitator" : "participant"`) — in place of the existing branch logic, without altering the existing 200/409/404/generic-error branches for any non-401 outcome, and without introducing a new `branch.kind` variant for this case.
- [x] 3.2 Wire the shared helper into the `start` POST call site (`handleStartSession`): on a recognized session-expiry, call `setReauthRequired` (via 3.0's gate) with `role="facilitator"` (this button is only reachable when `branch.isFacilitator === true`) instead of setting `startError` to the generic message; on a non-session-expiry `!res.ok`, derive `startError`'s message from the helper's returned `body` per Decision 1's updated contract, unchanged from today's fallback text otherwise.
- [x] 3.3 Wire the shared helper into the `begin-voting` POST call site (`handleBeginVoting`): on a recognized session-expiry, call `setReauthRequired` (via 3.0's gate) with `role="facilitator"` instead of setting `beginVotingError` to the generic message; on a non-session-expiry `!res.ok`, derive `beginVotingError`'s message from the helper's returned `body`, unchanged from today's fallback text otherwise.
- [x] 3.4 Stop discarding `useConnectionHealth`'s `state` value; on `state === "reauth-required"`, call `setReauthRequired` (via 3.0's gate) with `role` computed the same proxy as 3.1 (`session?.canFacilitateSessions ? "facilitator" : "participant"`, per design.md Decision 2's explicit statement of this) and `returnTo` = current path+query.
- [x] 3.5 Test (structural): confirm 3.0's gate is the only place `ReauthRequiredTreatment` is rendered on this page — none of 3.1–3.4 render it independently — so the idempotency rule holds by construction, not by each call site remembering to check a shared flag correctly.
- [x] 3.6 Test: fetch-response session-expiry on each of the three call sites independently renders the treatment (via 3.0's gate) with the correct `role`/`returnTo` — for `action-items-review`, cover both `session?.canFacilitateSessions` true and false, confirming `role` follows the proxy from design.md Decision 1a — and leaves non-session-expiry failures on their existing generic-error path, with the message still correctly derived from the helper's returned `body`.
- [x] 3.7 Test: `state === "reauth-required"` alone (no fetch 401 yet) renders the treatment, with `role` following the same `canFacilitateSessions` proxy as 3.1.
- [x] 3.8 Test (idempotency, the race design.md Decision 2 names explicitly): simulate the WS signal firing first, then a call site's fetch resolving with a session-expiry — confirm only one `ReauthRequiredTreatment` render, using the first signal's `role`/`returnTo`, and no second banner. Simulate the reverse order (a `start`/`begin-voting` 401 arriving before the WS close) — confirm the same: exactly one treatment on screen, not two.

## 4. DraftSessionHost.tsx

Depends on 2.1 (backend `RETURN_TO_ALLOW_LIST` entry for `/team/:teamId/session/:sessionId`) landing first — see design.md's Migration Plan for the deploy-order reasoning.

- [x] 4.1 Wire the shared helper into the mount-time `facilitator-state` GET call site (`loadFacilitatorState`): on a recognized session-expiry, render `ReauthRequiredTreatment` with `role="facilitator"` and `returnTo` = the current page's path (`/team/:teamId/session/:sessionId`) + query string, in place of "Unable to load this session."; on a non-session-expiry `!res.ok`, derive the existing generic-error message from the helper's returned `body` (per Decision 1's updated contract) instead of the current `.catch(() => null)`-guarded second `res.json()` — same fallback text ("Unable to load this session.") as today, same non-throwing behavior on a bad body.
- [x] 4.2 Wire the shared helper into the `advance` POST call site (`openTheRoom`): on a recognized session-expiry, render the treatment with `role="facilitator"` and `returnTo` = the current page's path (`/team/:teamId/session/:sessionId`) + query string, in place of "Could not open the room. Please try again."; on a non-session-expiry `!res.ok` (including the real, reachable `provider_unavailable` 401 named in design.md's Context), derive the existing generic-error message from the helper's returned `body` instead of a second `res.json()` read — same fallback text as today.
- [x] 4.3 Test (GET, its own acceptance condition per design.md Decision 4 / exploration-notes.md): a facilitator landing on this page (refresh, bookmark, direct hit) after their session already expired sees the treatment, not the generic load error.
- [x] 4.4 Test (POST, its own acceptance condition, stated separately from the GET's): a facilitator whose session expires between clicking "Yes, open the room" and the `advance` response landing sees the treatment, not "Could not open the room."
- [x] 4.5 Test: confirm the confirm-step's in-memory `confirming` phase is not expected to survive a reauth round trip — a session expiring while `advanceState.phase === "confirming"` and the user later returning via `returnTo` lands them on the bare draft view, not back on the confirm step (locks in design.md's named, correct-as-is behavior so it isn't "fixed" by accident later).
- [x] 4.6 Produced a rendered dev-build screenshot of the reauth banner following a simulated session-expiry 401 on the "Yes, open the room" confirm step: `screenshots/4.6-confirm-step-before.png` (confirm dialog) and `screenshots/4.6-reauth-required-after.png` (resulting treatment). Confirmed the treatment is a full-page early return, not an overlay — the confirm dialog is entirely gone from the DOM before the treatment renders, so the two states never appear simultaneously and there is no misclick risk between them. When rendered, the treatment is distinguishable from the confirm dialog by label text ("Log in again" vs. "Yes, open the room"/"Cancel"), an amber border/background, a lock icon, and position — not by color alone. Human sign-off given 2026-09-24.

## 5. SessionCreationPage.tsx

Depends on 2.2 (backend `RETURN_TO_ALLOW_LIST` entry for `/sessions/new`) landing first — see design.md's Migration Plan for the deploy-order reasoning.

- [x] 5.1 Wire the shared helper into the `eligible-for-session` GET call site (`loadEligibleTeams`): on a recognized session-expiry, render `ReauthRequiredTreatment` with `role="facilitator"` and `returnTo` = `/sessions/new` (this page's fixed route) + current query string, if any, in place of the generic `listError` message.
- [x] 5.2 Wire the shared helper into the `sessions/draft` POST call site (`confirmCreate`): on a recognized session-expiry, render the treatment with `role="facilitator"` and `returnTo` = `/sessions/new` + current query string, if any, instead of falling into the existing 409/403/generic-error branches — without altering those other branches' behavior.
- [x] 5.3 Test: session-expiry on the picker's GET renders the treatment; session-expiry on the confirm screen's POST renders the treatment and does not fall into the 409 (`session_already_exists`) or 403 (`membership_conflict`) branches; the existing 409/403/generic-error behaviors for non-401 outcomes are unchanged (regression).

## 6. MemberManagement.tsx (`submitRoleChange`)

No 4.6-equivalent mock-review task here — see design.md Decision 7's added note: `submitRoleChange`'s confirm dialog is already gone from the render tree (state moves to `submitting` before the fetch fires) by the time a 401 could render the treatment, so there's no layout collision to review.

- [x] 6.1 Wire the shared helper into the `submitRoleChange` PATCH call site: on a recognized session-expiry (on either the initial submission or the `confirmedZeroParticipant: true` re-submission after a 422), render `ReauthRequiredTreatment` with `role="facilitator"` (per design.md Decision 1a — suppresses the inapplicable vote-loss sentence, not a facilitator claim) and `returnTo` = current path (`/team/:teamId`), in place of setting `roleChangeState` to `{ status: "error", message: "Role change failed." }` — without altering the existing 422 `requiresConfirmation` branch or the generic-error/network-failure branches for any non-401 outcome. On a non-session-expiry `!res.ok`, derive `roleChangeState.message` from the helper's returned `body` (per Decision 1's updated contract) rather than the existing bare, unguarded `await res.json()` — this incidentally fixes `submitRoleChange`'s missing `.catch` guard (today's version throws on a non-JSON body; `DraftSessionHost.tsx`'s equivalent branches already guard theirs), bringing both restructured call sites to the same non-throwing behavior.
- [x] 6.2 Confirm no new `RETURN_TO_ALLOW_LIST` entry is needed (`/team/:teamId` already matches the existing `/team/:id` entry) — no backend task required for this call site.
- [x] 6.3 Test: session-expiry on the initial PATCH renders the treatment and does not render "Role change failed."; session-expiry on the confirmed re-submission (after a prior 422) also renders the treatment; the existing 422 `requiresConfirmation` and generic-error/network-failure behaviors for non-401 outcomes are unchanged (regression).
- [x] 6.4 Test: confirm the `awaiting_confirmation` state is not expected to survive a reauth round trip — a session expiring while `roleChangeState.status === "awaiting_confirmation"` and the user later returning via `returnTo` lands them on the bare member list, not back on the pending confirmation (locks in design.md Decision 7's named, correct-as-is behavior so it isn't "fixed" by accident later).

## 7. Deferred-scope follow-up

- [x] 7.1 File a separate, non-blocking issue covering `MemberManagement.tsx`'s `loadMembers` GET, the three EM pages (`EmSessionHistoryPage.tsx`, `EmTrendDataPage.tsx`, `EmActionItemsPage.tsx`), and the `teams.ts` `invalid_request`→`session_expired` category-label fix for its three "user row missing" 401 sites — per proposal.md's scope decision, none of this is built in this change. Link it from this change's PR description so it isn't lost. Filed as https://github.com/surratt-dev/project-dipstick/issues/159.

## 8. Verification

- [x] 8.1 Integration-test confirmation that a `session_expired` 401 on `DraftSessionHost.tsx`'s `advance` POST does not advance session state server-side: simulate an expired session on this call, then re-fetch `facilitator-state` and confirm the room did not open. Verifies design.md Decision 4's no-partial-execution guarantee, which the confirm step's "cannot be undone" copy depends on being literally true — named as its own scenario per spec.md's no-partial-execution requirement, not folded into 8.3's general smoke check.
- [x] 8.2 Integration-test confirmation that a `session_expired` 401 on `MemberManagement.tsx`'s `submitRoleChange` PATCH (either the initial submission or the `confirmedZeroParticipant: true` re-submission) does not apply the role change server-side: simulate an expired session on this call, then re-fetch team members and confirm the role is unchanged. Verifies design.md Decision 7's identical no-partial-execution guarantee for this call site — named as its own scenario for the same reason as 8.1.
- [x] 8.3 Manual or integration-test confirmation, for at least one call site, that the fix resolves the actual observed "generic error, retry forever" behavior against a real expired session — not just the unit-level response-shape assertions above (design.md's named trade-off).
- [x] 8.4 Run the full frontend and backend test suites; confirm no regression in existing `SessionLobbyPage.tsx`, `DraftSessionHost.tsx`, `SessionCreationPage.tsx`, `MemberManagement.tsx`, or `auth.ts` tests.
