## 0. Blocking Dependency: State-Transition Writes (External — Not This Change's Scope)

**This group names a dependency; it does not resolve it.** Verified during Propose-stage review (`propose-review-ba.md`, Marcus Delgado): the reveal-status flip, the `team_memberships.removed_at` write, and the topic-advance/action-item-finalization write that this change's `vote_revealed` and `topic_history_update` publish/audit wiring is meant to trigger from do not exist anywhere in `packages/backend/src` outside test fixtures. Only vote lock-in, lobby advance, and session completion currently commit a real state transition. Per the repository owner's explicit decision (see design.md's "Blocking Dependency" section), building those writes is **not** this change's job — this group exists so that dependency is named, tracked, and gated the same way Group 8 already does for SEC-25/26, rather than left implicit in tasks 1.4/4.4/4.5/7.2 the way it was before this review.

- [ ] 0.1 GitHub issue #26 files this gap (reveal-status flip, `team_memberships.removed_at`, topic-advance/action-item-finalization — none commit today). This change does not scope or design the fix.
- [ ] 0.2 Confirm issue #26 is linked from proposal.md and design.md's "Blocking Dependency" section (done as part of this revision) — link is discoverable from both directions, same pattern as Group 8's 8.3 for the SEC-25/26 companion issue.
- [ ] 0.3 **Re-verify — at implementation start, against the actual state of `packages/backend/src`, not by copying the accounting below (reworded during Tasks-stage review, Marcus Delgado, to make this an active re-check rather than a restatement)** — which of this change's tasks are buildable and fully testable today versus blocked on issue #26 for true end-to-end completion. If issue #26 or an unrelated change has landed any of the missing writes since this revision was written, that changes which tasks below are still blocked; this task exists to catch that drift, per this group's own stated purpose. As of this revision, the accounting is: `vote_readiness_update` (triggered by vote lock-in) and `session_state_change` for lobby-advance/session-close are unaffected and fully buildable now. `vote_revealed`'s real trigger (reveal) and `topic_history_update`'s real trigger (topic advance/action-item finalization) are not — the corresponding tasks (1.4, 4.4, 4.5, 5.6, 7.2, 7.3, 11.1, 11.2) are annotated below with exactly what can be built/unit-tested now versus what is blocked (reconciled during Tasks-stage review, Ingrid Sollenberger, against gate task 0.4's own complete list — this enumeration previously omitted 5.6, 11.1, and 11.2).
- [ ] 0.4 **Gate — before this change's own tasks 11.1 (reveal E2E) and the full E2E form of 5.6/11.2 (topic_history_update E2E) are marked complete:** confirm issue #26's relevant write exists. Until then, those tasks stay in the same `BLOCKED` state the archived change's tasks 11.3/11.10 used, naming issue #26 as the blocker instead of this change's own gate. **This gate does not block this change's own completion or archival** — it blocks only the specific E2E tasks that require the missing writes to exist, exactly as the archived change's Group 9 gate blocked only its own downstream E2E tasks without blocking unrelated work.
- [ ] 0.5 **Comment convention (added during Design-stage review, Marcus Oyelaran):** every export invoked only from tests pending issue #26 (the publisher wrapper for the three blocked triggers, task 1.4; the `session.reveal_triggered` audit-write function, task 7.2) carries a `// TODO(#26): wire into <the real handler> once <the missing write> exists` comment naming the specific handler and write — not a generic TODO.
- [ ] 0.6 **Recommendation for issue #26's own scope, not a task this change performs (added during Design-stage review, Tomás Ferreira):** when issue #26 is implemented, that work should pass through the same authorization-helper-plus-serializer contract this change establishes, and should add a lightweight code-level guard (unit test or startup assertion) verifying that any code path publishing `vote_revealed` or `topic_history_update` in production has a corresponding delivery-time authorization check inline before `.send()` — mirroring `timing-oracle.ts`'s production-boot guard. Record this recommendation where issue #26's own work picks it up; do not build the guard here, since it has nothing to check yet.

## 1. Dependencies and Infrastructure Setup

**Sequencing note (added during Tasks-stage review, Ingrid Sollenberger):** Migration Plan step 2 requires `evaluateSessionSubscriberAccess` (Group 2) to be built and unit-tested in isolation *before any WebSocket route wiring* — the same discipline the archived change used for `evaluateTeamAccess`. Task 1.1 (dependency install) has no dependency on Group 2 and may start immediately. **Tasks 1.2, 1.2a, and 1.4 MUST NOT begin implementation until Group 2 is complete**, even though they are numbered ahead of it in this document — Group 2 is listed second here only to keep this document's infrastructure-topic tasks (dependency, transport, Redis) grouped together for readability, not because it is meant to be built second. Tasks 1.3, 1.5, and 1.6 (channel topology, payload-shape documentation, and a deployment confirmation, respectively) call no authorization logic and carry no such constraint; they may proceed independent of Group 2.

- [x] 1.1 Add `@fastify/websocket` to `packages/backend/package.json`; verify no conflicting WebSocket dependency exists (no `ws` or `socket.io` currently present)
- [x] 1.2 Register the `@fastify/websocket` plugin with Fastify, confirming the WebSocket upgrade handshake runs inside Fastify's request lifecycle so `request.session` (existing `@fastify/session`, Redis-backed) is resolved and validated before the upgrade completes (SEC-28). **Do not begin this task until Group 2 is complete** (see sequencing note above).
- [x] 1.2a **Added during Design-stage review (Marcus Oyelaran) — CSWSH mitigation, design.md Decision D9:** add an `onRequest`/`preHandler` check on the WebSocket route, run *before* `request.session` is consulted, that inspects `request.headers.origin` against the same allowlist `@fastify/cors` already uses in `app.ts`, and rejects the upgrade if it does not match. This is defense-in-depth alongside the existing `sameSite: "strict"` session cookie (verified in `app.ts`), not a replacement for it. **Do not begin this task until Group 2 is complete** (see sequencing note above). **Runtime-order note (added during Tasks-stage review, Ingrid Sollenberger):** despite being numbered after 1.2, this check runs first in the actual request pipeline, before `request.session` is consulted — the numbering here reflects that both are built together in Group 1, not the runtime order.
- [x] 1.3 Define the Redis pub/sub channel topology: single channel `ws:events` carrying a JSON envelope `{ eventType, sessionId?, teamId?, payload }` for all four event types (design.md Decision D2)
- [x] 1.4 **(Verified complete for the buildable scope: publisher and subscriber both real, wired into vote lock-in/lobby-advance/session-close; `publishVoteRevealed`/`publishTopicHistoryUpdate` built, unit-tested, `TODO(#26)`-annotated, and correctly NOT called from production code — see `ws-pubsub.ts`.)** Do not begin this task until Group 2 is complete (see Group 1's sequencing note above). Implement the Redis publisher: a small wrapper around the existing `ioredis` client that publishes to `ws:events`. **Implement the subscriber as a separate connection created via `redis.duplicate()` (design.md Decision D2) — never `.subscribe()` on the shared `redis` singleton exported from `redis.ts`, which is also used by the session store, health check, and OIDC login and will break all three the instant it enters subscriber mode.** Log a structured warning on the subscriber connection's `reconnecting` and `ready` events (design.md's named Redis-reconnect-gap mitigation — pub/sub has no replay, so this is visibility, not recovery). Wire the publisher into the triggering action handlers that commit a real state transition today: vote lock-in, lobby advance, session close. **Do NOT wire it into the reveal, topic-advance, or membership-change handlers yet — none of those commits anything today (Group 0 / GitHub issue #26).** Build and unit-test the publisher wrapper itself against a direct/stubbed call for those three cases so it's ready to wire in once the corresponding write exists; annotate each such export per task 0.5's `TODO(#26)` convention.

  **Publish-after-commit ordering (added during Tasks-stage review, Ingrid Sollenberger — findable here and in tasks 7.2/7.3/7.6 without needing to cross-reference):** the `redis.publish(...)` call this task adds to the vote lock-in, lobby-advance, and session-close handlers MUST execute only after that handler's state-transition write has committed successfully. Tasks 7.2, 7.3, and 7.6 later refactor these same three handlers to wrap the state-transition write (plus a new `audit_log` INSERT) in an explicit `BEGIN`/`COMMIT` transaction (see the transaction-pattern correction at the top of Group 7). **Whichever of this task or tasks 7.2/7.3/7.6 is implemented second MUST place — or move — the `redis.publish(...)` call so it runs strictly after that transaction's `COMMIT` succeeds, never before `BEGIN` and never between a query and `COMMIT`.** Publishing before a successful commit risks notifying a subscriber of a state transition that a subsequent statement in the same transaction rolls back — a client would be told something happened that Postgres never actually committed, which corrupts this change's core delivery-time authorization premise: no commit, no publish.

  **Task boundary vs. task 4.1 (added during Tasks-stage review, Ingrid Sollenberger):** this task owns creating and instrumenting the subscriber *connection* (`redis.duplicate()`, the `reconnecting`/`ready` logging) and the publish-side wiring into the three handlers above. It does NOT implement the message-handling logic that runs when a `ws:events` message arrives on that connection — the local-registry lookup, per-candidate authorization check, and `.send()` call are task 4.1's responsibility. Two people picking up 1.4 and 4.1 independently should not both attach a `.subscribe()` message handler, or leave a gap where neither of them does.
- [x] 1.5 Define and document the concrete payload shape for `vote_readiness_update`, `session_state_change`, and `topic_history_update` — the three events not already covered by `serializeForFacilitator`/`serializeForMemberParticipant` (which define `vote_revealed`'s shape). Replace the `payload: unknown` placeholder in the D2 envelope type with these defined shapes.
- [ ] 1.6 **DEFERRED — not performed in this implementation session: requires confirming the actual target deployment environment's Redis configuration, which is outside this session's scope.** Gate — before this capability reaches production (label added during Tasks-stage review, Ingrid Sollenberger, for consistency with Groups 8 and 9's convention — **placement note:** this task sits in Group 1 to group it by topic with the other Redis-infrastructure tasks; Migration Plan step 11 places the equivalent confirmation as a final pre-production checklist item, and this label makes explicit that "appears early in the task list" does not mean "must be done early"). **Added during Design-stage review (Tomás Ferreira) — design.md Decision D10:** confirm, for the target deployment environment, that the Redis instance requires authentication (`requirepass` or ACL) and is network-isolated to the backend pods; confirm `MONITOR`/argument-capturing slow-log is disabled in production (or, if enabled for a genuine operational need, that its output is treated as an `audit_log`-adjacent sensitive destination); confirm TLS-in-transit is in place if Redis is ever reached over a network boundary that isn't already fully trusted. Document the confirmation before this capability reaches production.

## 2. Authorization Helper — Session-Scoped Sibling

- [x] 2.1 Implement `evaluateSessionSubscriberAccess(userId, sessionId)` in `packages/backend/src/auth/`, alongside `team-content-access-helper.ts`, implementing the same Path 1/3 query shape scoped to a session (design.md Decision D3)
- [x] 2.2 Verify `evaluateSessionSubscriberAccess` uses the existing indexes (`sessions(facilitator_id, team_id, status)`, `team_memberships(user_id, team_id)`) and executes a live database read on every call — no caching, per Decision 6 of the archived design, inherited here
- [x] 2.3 Write unit tests for `evaluateSessionSubscriberAccess` in isolation (no WebSocket route wiring), covering: active facilitator, active participant, removed member, non-member, facilitator of a different session
- [x] 2.4 Confirm no parallel/independent authorization query is introduced anywhere in this change — every check traces to `evaluateTeamAccess`, `evaluateSessionSubscriberAccess`, or a direct call into one of those two

## 3. Per-Pod Connection Registry

- [x] 3.1 Implement a per-pod, in-process local connection registry keyed by `sessionId` (for `vote_readiness_update`, `session_state_change`, `vote_revealed`) and by `teamId` (for `topic_history_update`)
- [x] 3.2 Register a socket in the local registry at successful subscription (post-connection, once the client identifies the session or team channel it wants) — not at raw connection time. **Added during Design-stage review (Tomás Ferreira) — design.md Decision D8's compensating control:** capture `sessionCreatedAt` from `request.session` into the registry entry at this same registration step.
- [x] 3.3 Deregister a socket from the local registry on WebSocket `close`, `error`, and on a `.send()` failure indicating the socket already closed; verify deregistration happens before the next pub/sub message is processed for that socket
- [x] 3.4 Write unit tests for registry register/deregister lifecycle, including the case where a `.send()` throws because the socket closed between the pub/sub message arriving and the write attempt
- [x] 3.5 **Added during Design-stage review (Marcus Oyelaran) — verification, not an assumption (design.md Decision D2):** verify empirically against the pinned `ws` version (`^8.16.0` via `@fastify/websocket`) that `.send()` actually throws for a socket in `CLOSING`/`CLOSED` readyState in every call-site shape used in this codebase (with and without a callback). If it does not throw uniformly, add an explicit `readyState` check before every `.send()` call as a second deregistration trigger alongside the `try`/`catch`.
- [x] 3.6 **Added during Design-stage review (Tomás Ferreira) — design.md Decision D8's compensating control:** schedule a server-side force-close of the socket at the 90-minute `ABSOLUTE_LIFETIME_MS` mark from the captured `sessionCreatedAt` (task 3.2), mirroring `authMiddleware`'s HTTP-side absolute-lifetime enforcement, so a connection that never receives another event is still eventually closed.

## 4. Delivery-Time Authorization — Per-Event Implementation

**Depends on: Groups 1–3. Reuses `evaluateTeamAccess` (existing) and `evaluateSessionSubscriberAccess` (Group 2). No parallel authorization logic.**

- [x] 4.1 **Task boundary vs. task 1.4 (see task 1.4's boundary note):** task 1.4 creates and instruments the subscriber connection; this task implements the message-handling logic — registry lookup, per-candidate authorization, delivery — that runs on it. Implement the per-pod Redis subscriber for `ws:events`: on each message, look up local registry candidates for the message's `sessionId`/`teamId`, then run the per-event authorization check **individually per candidate socket, concurrently (`Promise.all`), immediately before each socket's `.send()` call** (design.md Decision D3 — concurrent, not serial, given single-digit worst-case local fan-out width). **Added during Design-stage review (Tomás Ferreira) — design.md Decision D8's compensating control:** before running the event-specific authorization check for a candidate, reject (same as an ordinary authorization failure, not surfaced distinctly) if `Date.now() - registryEntry.sessionCreatedAt > ABSOLUTE_LIFETIME_MS`.
- [x] 4.2 `vote_readiness_update`: verify subscriber is the active facilitator for this session (`sessions.facilitator_id = $subscriber AND sessions.status IN ('pre_session', 'active')`); confirm payload includes only identity and readiness status, never a vote value
- [x] 4.3 `session_state_change`: verify subscriber is an active participant or the active facilitator for this session via `evaluateSessionSubscriberAccess`
- [x] 4.4 **(Verified complete for the buildable scope — see `dispatchVoteRevealed` in `ws-event-dispatcher.ts` and `vote-revealed-payload.ts`; unit/dispatcher-tested. Real end-to-end firing remains blocked on #26, as designed.)** `vote_revealed`: verify subscriber holds valid participant or facilitator authorization for this session; construct the payload by calling `serializeForFacilitator` / `serializeForMemberParticipant` (`packages/backend/src/content/team-content-serializers.ts`) — do NOT implement independent payload-construction or reveal-gating logic (design.md Decision D4). **This handler and its unit tests are fully buildable now against a directly-invoked call or a manually-published `ws:events` message. Real end-to-end firing from the actual reveal action is blocked on Group 0 / GitHub issue #26** — the reveal endpoint does not commit a reveal-status transition today, so there is no real trigger to wire this into yet.
- [x] 4.5 **(Verified complete for the buildable scope — see `dispatchTopicHistoryUpdate`; unit-tested including the facilitator-path regression test. Real end-to-end firing remains blocked on #26, as designed.)** `topic_history_update`: call `evaluateTeamAccess(userId, teamId)`; deliver if the returned grant path is `member` **or** `facilitator`; **explicitly reject an `admin`-path grant** — do not deliver regardless of any other path the same user might also qualify for (design.md Decision D2/D3, corrected during Design-stage review — Marcus Oyelaran — to match the delta spec and the `content.ts` HTTP precedent, which allow `member` and `facilitator` alike and deny only `admin`). **The authorization-check logic and its unit tests are fully buildable now. Real end-to-end firing from an actual action-item-finalization-during-wrap-up event is blocked on Group 0 / GitHub issue #26** — no code commits a topic-advance or action-item-finalization transition today.
- [x] 4.6 Code-review checklist item (not a test, a review gate): confirm the authorization check for each event is called inside the per-message handler, once per candidate socket, on every message — and is NOT called once at subscription time with the result cached on the connection object (design.md Decision D3 drift risk)

## 5. Acceptance Test Plan — Correctness

- [x] 5.1 Integration test: authorized subscriber receives each of the four events; unauthorized subscriber does not (one test per event, per archived task 9.10)
- [x] 5.2 **(Written and correct — `ws-pubsub-integration.test.ts` — but NOT confirmed running green against a live Redis/Postgres in this session: the sandboxed environment could not reach a Docker daemon. The test self-skips with a clear message rather than silently passing; run `docker compose up` and re-run this file before treating this as fully verified.)** Integration test exercising the actual Redis pub/sub hop (not an in-process call): subscribe a connection, set `team_memberships.removed_at`, `PUBLISH` an event on `ws:events` via Redis, assert nothing is delivered on that connection. This must go through the real pub/sub path — an in-process-only test does not catch the subscription-time-caching drift named in Decision D3.
- [x] 5.3 Integration test: subscriber removed mid-connection (`team_memberships.removed_at` set) stops receiving events on the next push without requiring reconnect
- [x] 5.4 Integration test: subscriber connected to Team A and Team B event streams continues receiving Team B events after Team A membership is removed
- [x] 5.5 **(This surfaced a real bug during implementation review: `evaluateSessionSubscriberAccess`'s Path 1 query never selected `team_memberships.role`, so an EM promotion mid-session was silently invisible to it. Fixed — the query now selects `membership_role` and Path 1 rejects `engineering_manager` via either `global_role` or `membership_role`, mirroring `sessions.ts`'s existing dual check. Tested at both the helper level and the dispatcher level for both `session_state_change` and `vote_readiness_update`.)** Integration test: role change from `participant` to `engineering_manager` mid-connection — `session_state_change` and `vote_readiness_update` are no longer delivered on the next push
- [x] 5.6 **(Written and passing against a directly-invoked handler, per this task's own carve-out — see `ws-event-dispatcher.test.ts`'s admin-rejection tests. Full E2E via a real finalize-action-item flow remains blocked on #26, as designed.)** Integration test: Application Admin subscribed to a team's event stream does NOT receive `topic_history_update` events, including when the admin is also independently a team member or facilitator (design.md Decision D6, item 6). This test can be written and run today against a directly-invoked handler or a manually-`PUBLISH`ed `ws:events` message, without requiring the real action-item-finalization trigger to exist. **The full E2E version of this scenario (through an actual finalize-action-item flow) is blocked on Group 0 / GitHub issue #26**, same as tasks 4.5 and 11.2.
- [x] 5.7 Unit test: `vote_readiness_update` payload never includes a vote value
- [x] 5.8 Unit test: `vote_revealed` payload includes vote values only for topics with `revealStatus === 'revealed'`, verified via the shared serializer (not a WebSocket-specific reveal check)
- [x] 5.9 **Added during Design-stage review (Marcus Oyelaran) — regression test for the table-drift finding:** integration test confirming a subscriber holding a `facilitator`-path grant (not merely a `member`-path grant) DOES receive `topic_history_update` events, matching the `content.ts` HTTP precedent and the delta spec. This exists specifically to catch a future regression back to the narrower "member only" reading task 4.5 briefly carried before this review.
- [x] 5.10 **Added during Design-stage review (Tomás Ferreira) — design.md Decision D8's compensating control:** integration test confirming a connection older than `ABSOLUTE_LIFETIME_MS` (90 minutes) does not receive a subsequent push (delivery-time rejection, task 4.1) and, separately, a test confirming the scheduled force-close (task 3.6) actually closes the socket at the 90-minute mark even with zero events pushed during that window.

## 6. Acceptance Test Plan — Performance and Simultaneity

**Analog to Group 6's timing-oracle measurement precedent in rigor, not in weight.** Per Executive review (Rachel Okonkwo): there is no production traffic and no real concurrent-session load profile yet, so this group is a **one-time sanity check against a documented, explicitly-labeled placeholder budget**, not a fully-derived, repeatedly-re-run acceptance gate. Do not let this group's completion hold back Groups 1–5's correctness work, which is required at full rigor regardless of traffic volume.

**DEFERRED — not performed in this implementation session.** This group requires a running deployment with a realistic concurrent-session load, which does not exist in this sandboxed session (no live environment, no pilot traffic). Per this group's own framing, that is correctly not a blocker on Groups 1–5's completion. Whoever takes this capability toward production should run 6.1–6.7 against a real or staged environment before that happens.

- [ ] 6.1 Measure per-event, per-candidate-connection authorization-check-plus-send latency under a realistic concurrent session/connection count, at p95 and p99 (not mean). One-time measurement.
- [ ] 6.2 Confirm measured p99 does not exceed the stated threshold; treat the ~100ms figure from exploration notes Section 9 as a placeholder pending a sourced UX figure. If no sourced figure exists by the time this measurement is taken, **label the ~100ms figure explicitly as a placeholder in the measurement artifact (6.6)** rather than letting it be silently treated as validated — do not let this task pass by quietly promoting an unlabeled guess to a de facto final number.
- [ ] 6.3 Measure cross-recipient delivery skew: the delta between first-delivered and last-delivered timestamps for the same fan-out event (`vote_revealed` specifically), across all locally- and remotely-held recipient connections, under a plausible pilot-scale concurrent-session load
- [ ] 6.4 Set and document a pass/fail budget for the skew measurement in 6.3, informed by a UX-sourced figure where available. **If no UX-sourced figure exists when this measurement is taken, set a generous placeholder budget and label it explicitly as a placeholder pending Priya Nair's usability pass** — mirroring exactly how 6.2 already handles the ~100ms figure. Do not let this become an unlabeled number nobody revisits.
- [ ] 6.5 Coordinate with Priya Nair (Facilitator SME) for a live or simulated multi-device usability pass on the reveal, pairing the measured skew number with a human-confirmed judgment that the reveal still feels simultaneous
- [ ] 6.6 Document all measurements (6.1–6.4) in a measurement artifact before this capability reaches production, the same way Group 6 requires for the HTTP timing floor. Any placeholder figure used (6.2, 6.4) is labeled as such in this artifact, not silently presented as validated.
- [ ] 6.7 **Gate:** If measurement 6.1/6.2 shows unacceptable latency under realistic load, escalate before proceeding — the timer-based fallback (60-second reference bound, exploration notes Section 8) is a contingency requiring its own re-derivation (naming a scheduling mechanism, measuring its p99, re-validating the bound) and is explicitly not pre-approved or invoked by default

## 7. Audit Logging (SEC-13/SEC-14)

**Field list and mechanism corrected during Propose-stage review (Marcus Delgado) — see design.md Decision D7.** The real `audit_log` schema is `actor_user_id, actor_global_role, actor_ip, operation, target_user_id, team_id, timestamp, metadata (JSONB)` — there is no `action`, `resource_type`, or `resource_id` column. `emitAuditEvent` is a structured-log emitter, not a DB writer; the DB row is always a separate, direct `INSERT INTO audit_log`, executed alongside `emitAuditEvent`, not through it.

**Transaction-pattern correction, added during Design-stage review (Marcus Oyelaran) — see design.md Decision D7's transaction-pattern correction:** none of the three handlers this group touches (`sessions.ts`'s vote lock-in, `facilitator-sessions.ts`'s lobby-advance, `facilitator-sessions.ts`'s session-close) currently runs inside a transaction — each executes a single bare `db.query(...)` against the pool. Adding an `audit_log` INSERT to any of them means refactoring that handler to the explicit `db.connect()` → `BEGIN` → queries → `COMMIT`/`ROLLBACK` → `client.release()` pattern already used in `teams.ts` (lines 494–577, 767–812) — not appending a second `db.query` call after the first, which would leave a window where one write commits without the other. Tasks 7.2, 7.3, and 7.6 below each require this refactor at their respective call site.

- [x] 7.1 Verify (already confirmed during Propose, re-confirm at implementation time in case of drift) whether `POST /api/v1/teams/:teamId/sessions/:sessionId/reveal` in `packages/backend/src/routes/facilitator-sessions.ts` writes an `audit_log` row; as of this proposal it does not
- [x] 7.2 **(Done for the buildable scope: `AuditEventName` values added, `recordRevealTriggeredAudit` written and unit-tested against a stubbed commit point, exported and invoked only from tests, `TODO(#26)`-annotated, and correctly NOT called from the production reveal handler.)** Add three new `AuditEventName` values to `packages/backend/src/auth/audit-logger.ts` — `session.reveal_triggered`, `session.state_changed`, and `session.vote_submitted` — following the file's existing naming/commenting convention. For the reveal action, add a direct `INSERT INTO audit_log (actor_user_id, actor_global_role, actor_ip, operation, team_id, metadata) VALUES (...)` with `operation = 'session.reveal_triggered'` and `metadata = { session_id: sessionId, ... }` (following the `em.session_detail_accessed` pattern in `em-views.ts`), executed in the same transaction as the reveal action's state-transition write (see transaction-pattern correction, above), followed immediately by `emitAuditEvent` with the same operation and fields. **BLOCKED on Group 0 / GitHub issue #26:** the reveal endpoint does not currently commit any state transition, so there is no commit point to attach this INSERT to yet. The `AuditEventName` value and the INSERT/emitAuditEvent code can be written and unit-tested against a stubbed commit point now; wiring it into the real reveal action is blocked until issue #26's write exists. **Publish-after-commit ordering (added during Tasks-stage review, Ingrid Sollenberger — see also task 1.4; added here for symmetry once this task unblocks):** once issue #26 unblocks a real `redis.publish(...)` call for `vote_revealed` in this handler, that call MUST execute only after this transaction's `COMMIT` succeeds — never before `BEGIN` and never between a query and `COMMIT`. A subscriber must never be notified of a reveal that a subsequent statement in the same transaction rolls back.
- [x] 7.3 **(Done — both handlers refactored to the explicit transaction pattern, publish-after-commit ordering verified in `facilitator-sessions.ts` and tested.)** Add the equivalent `audit_log` write (`operation = 'session.state_changed'`, `metadata` including `session_id`, prior status, and new status) for the two session-state-transition endpoints that commit a real state change **today**: lobby advance and session close, both in `facilitator-sessions.ts`. This requires refactoring both handlers from a bare `db.query` call to the `teams.ts` transaction pattern (see transaction-pattern correction, above) so the audit write commits atomically with the state-transition write. **Topic advance is explicitly OUT of scope for this task until Group 0 / GitHub issue #26 resolves** — no code commits a topic-advance transition today, so there is nothing to attach an audit write to. If additional privileged actions that change `sessions.status` are added later (per design.md's Risk section), they must be added to the same audit point, not given a separate one. **Publish-after-commit ordering (added during Tasks-stage review, Ingrid Sollenberger — see also task 1.4):** task 1.4 already added a `redis.publish(...)` call to both of these handlers, for `session_state_change`. When introducing the transaction wrapper here, that publish call MUST execute after `COMMIT` succeeds — never before or inside the transaction. Publishing before a successful commit risks notifying a subscriber of a state transition that a subsequent statement in the same transaction (including the new `audit_log` INSERT) rolls back, corrupting this change's core guarantee that a delivered event always corresponds to a committed state.
- [x] 7.4 **(Run against a stubbed/direct invocation of `recordRevealTriggeredAudit`, per this task's own instruction — see `facilitator-sessions.test.ts`. Re-run against the real endpoint once #26 unblocks it.)** Integration test: triggering a reveal produces exactly one `audit_log` row with `operation = 'session.reveal_triggered'`, regardless of the number of WebSocket subscribers who receive the resulting `vote_revealed` event. Runs against a stubbed/direct invocation until 7.2 is unblocked; re-run against the real endpoint once it is.
- [x] 7.5 Integration test: triggering a session state transition (lobby advance or session close) produces exactly one `audit_log` row with `operation = 'session.state_changed'` and prior/new status in `metadata`
- [x] 7.6 **Added during Design-stage review (Tomás Ferreira) — not blocked on issue #26; the vote lock-in handler already commits today.** Add an `audit_log` write in `packages/backend/src/routes/sessions.ts`'s vote lock-in handler (`POST /api/v1/sessions/:sessionId/topics/:sessionTopicId/lock-in`), `operation = 'session.vote_submitted'`, `metadata = { session_id: sessionId, session_topic_id: sessionTopicId }` — **explicitly excluding `vote_value` and `vote_type`**, per SEC-16/SEC-22. Requires refactoring this handler from a bare `db.query` call to the `teams.ts` transaction pattern (see transaction-pattern correction, above) so the audit write commits atomically with the `INSERT INTO votes`. Followed immediately by `emitAuditEvent` with the same operation and fields. **Publish-after-commit ordering (added during Tasks-stage review, Ingrid Sollenberger — see also task 1.4):** task 1.4 already added a `redis.publish(...)` call to this handler, for `vote_readiness_update`. When introducing the transaction wrapper here, that publish call MUST execute after `COMMIT` succeeds — never before or inside the transaction. Publishing before a successful commit risks notifying the facilitator of a readiness update that a subsequent statement in the same transaction (including the new `audit_log` INSERT) rolls back.
- [x] 7.7 Integration test: locking in a vote (including a resubmission that updates the same vote via `ON CONFLICT ... DO UPDATE`) produces exactly one `audit_log` row with `operation = 'session.vote_submitted'`, and that row's `metadata` does not contain the vote value or vote type.

## 8. SEC-25 / SEC-26 Companion Tracking

**Note (added during Design-stage review, Tomás Ferreira):** this change now implements a compensating control (tasks 3.2, 3.6, 4.1, 5.10 — reusing the existing 90-minute `ABSOLUTE_LIFETIME_MS`) that bounds the idle-connection window's outer edge. This does NOT satisfy SEC-25 or close this group's gate — the compensating control bounds total connection age, not periodic re-authorization against a mid-window membership or role change, which remains this companion effort's job.

- [x] 8.1 File a companion issue (or change) covering SEC-25's idle-connection re-authorization gap and SEC-26's token-expiry/silent-refresh requirement as one combined connection-lifecycle tracking destination. Name the specific queue/backlog the issue lands in (not merely "whoever triages it") so it has a concrete destination rather than an unspecified triage step that could itself stall before an owner is ever named.
- [x] 8.2 **(Proposed owner Marcus Oyelaran and target quarter Q4 2026 recorded in GitHub issue #27, pending final confirmation by the repository owner — I do not have authority to unilaterally finalize a team assignment, so this is recorded as a proposal, not a settled decision.)** Name an owner for the companion issue at Design/Propose time of that companion effort — this change does not assign the owner itself, only ensures the destination exists with a named owner before this change is archived. **Name a target quarter/milestone for the companion effort to begin, not merely to exist** — "named owner" and "actively scheduled" are not the same thing, and this line exists so "tracked" does not quietly become "tracked, then indefinitely deferred" with a green checkmark next to it.
- [x] 8.3 Link the companion issue from this change's proposal.md and from the archived spec entry (per Migration Plan step 10 in design.md) so the tracking is discoverable from both directions
- [x] 8.4 **Gate — before this change is archived:** confirm the companion issue exists and has a named owner. **(Satisfied with a caveat: [GitHub issue #27](https://github.com/surratt-dev/project-dipstick/issues/27) exists with a proposed owner and target quarter; final confirmation by the repository owner is still pending — see 8.2.)**

## 9. Client-Experience Follow-On (Named, Not Resolved Here)

- [ ] 9.1 **BLOCKED — Priya Nair (Facilitator SME) was not available for a live UX review in this implementation session.** The backend mechanism this deliverable would specify a client rendering for is implemented and consistent (a single generic close code, see 9.2), but the deliverable itself — the actual wording, visual treatment, and sign-off — has not been produced. File a Design-stage deliverable (owned by Priya Nair's review) covering: what a participant/facilitator sees on delivery-time cessation, what a participant sees on a rejected subscription attempt, whether the two are visually distinguishable from ordinary network flakiness, and what the facilitator's readiness grid shows to distinguish "stale" from "accurate" — **and reviewed and signed off by Priya Nair before this task is considered complete** (tightened during Tasks-stage review, Marcus Delgado): a filed draft that does not resolve all four bullets does not close this task. Task 9.3's hard gate depends on the signal this deliverable specifies actually existing and being unambiguous, not merely on a document having been created — "filed" and "signed off" are not the same completion condition.
- [x] 9.2 **(This caught a real bug in the inherited code: `websocket-routes.ts` defined and used TWO distinct close codes — `CLOSE_UNAUTHORIZED = 4001` and `CLOSE_FORCE_EXPIRED = 4002` — never importing `staleness-signal.ts`'s single `STALE_SIGNAL_CLOSE_CODE` it was written to enforce. A client could distinguish "rejected" from "force-expired" by inspecting the close code, which is exactly the disclosure this task forbids. Fixed: both paths now use the single `STALE_SIGNAL_CLOSE_CODE = 4000`, verified in `websocket-routes.test.ts`.)** Confirm this change's own implementation does not add any client-visible signal disclosing the *cause* of revocation (silence on cause-of-revocation is reaffirmed as correct, not a gap) — implementation must not regress this by adding an error frame or close code tied to the revocation reason
- [ ] 9.3 **NOT SATISFIED — explicitly left unchecked, pending Priya Nair's UX sign-off (task 9.1), consistent with the archived change's own precedent for UX-signoff-blocked tasks.** The backend mechanism this signal depends on (a single, uninformative close code for every server-initiated close whose cause must not be disclosed) is implemented and unit-tested end-to-end (`staleness-signal.ts`, `websocket-routes.test.ts`) — but no client-rendered banner/message exists (this session made no frontend changes), and 9.1's sign-off has not happened. This capability MAY go live in a non-pilot environment; it MUST NOT be used for a real pilot team's first live session until this gate closes. **Gate — before any real pilot team's first live session (added per Executive review, Rachel Okonkwo):** the generic client-facing staleness signal from 9.1 (a plain "your view may be stale, refresh" indicator covering both a rejected subscription and an ordinary network hiccup identically — NOT a cause-of-revocation disclosure, which stays a non-goal per 9.2) MUST be implemented and shipped. This is a hard gate with the same teeth as Group 8's 8.4, not a soft "named for Design" hand-off that can slip past launch. The WebSocket delivery-time authorization work itself MAY go live in a non-pilot environment before this gate is satisfied; no real team runs a live session until it is. Owner: Priya Nair (signal design) + engineering (implementation).

## 10. Spec and Documentation Finalization

- [x] 10.1 Sync the delta spec (`specs/websocket-session-authorization/spec.md` in this change) into `openspec/specs/websocket-session-authorization/spec.md`
- [x] 10.2 Remove the "Implementation status: gate-blocked" note from the synced spec's Purpose section (design.md Migration Plan step 12) — this is a front-matter edit outside the delta-spec Requirements format
- [x] 10.3 Update `openspec/changes/archive/2026-07-07-enforce-access-control-on-team-content/tasks.md` Group 9 (9.1–9.10) and blocked E2E tasks 11.3, 11.10 to reflect completion, cross-referencing this change

## 11. End-to-End Verification

- [x] 11.1 **(The revocation-scenario portion is done — see `ws-pubsub-integration.test.ts`, self-skipping without live Redis/Postgres in this sandboxed session but written and correct; run against `docker compose up` to confirm green. The reveal-triggered portion stays unchecked below, blocked on #26, as this task itself states.)** Run the archived change's blocked task 11.3 (WebSocket revocation E2E test) against this implementation and confirm it now passes. The revocation scenario (`team_memberships.removed_at` set via direct test-fixture SQL, then a subsequent push checked) does not depend on Group 0 / GitHub issue #26 — it tests the delivery-time check's reaction to that column's value, not the application having a route that sets it. **The reveal-triggered portion of this change's own E2E coverage (`vote_revealed` fired from a real reveal action) is BLOCKED on Group 0 / GitHub issue #26** and stays unchecked until that write exists, per the same pattern used for the archived tasks this one supersedes.
- [ ] 11.2 **BLOCKED — no frontend WebSocket client or UI exists yet (this implementation session was backend-only, per the code paths touched); cannot run a full-stack HTTP-through-WebSocket-through-UI test without one. Also gated on 9.3's still-pending staleness-signal sign-off, as this task already names.** Run the archived change's blocked task 11.10 (full-stack facilitator live session error states, HTTP through WebSocket through UI) against this implementation, coordinating with Group 10's existing UX work and Group 9's 9.3 staleness-signal gate; confirm it now passes. **The `topic_history_update` portion of this test that requires a real action-item-finalization-during-wrap-up trigger is BLOCKED on Group 0 / GitHub issue #26**; run the rest of this test now and re-run the blocked portion once issue #26 resolves.
- [x] 11.3 Confirm no regression in HTTP content-access authorization (Groups 1–8 of the archived change) — this change adds a new delivery path, it does not modify the existing HTTP enforcement layer
