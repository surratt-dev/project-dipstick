## 1. Shared audited join-link creation helper

- [x] 1.1 Extract the full creation sequence from `join-links.ts:65-122` — token generation (`randomBytes(32)`), `expiresAt` computation, the `join_links` INSERT-plus-audit-transaction (`AuditWriteError` handling), and the post-commit `emitAuditEvent(...)` call for `join.link_created` — into a standalone function, `createJoinLink({ teamId, createdByUserId, actorGlobalRole, actorIp, logger }): Promise<JoinLink>`, in a new module `packages/backend/src/auth/join-link-creation.ts` (not exported from `join-links.ts` — no existing route file imports from a sibling route file, and this keeps `routes/` a leaf layer, per engineer review). Callable from both `POST /api/teams/:teamId/join-links` and the new get-or-create call site. **Revised per engineer review:** the original framing scoped this to lines 76-104 (the transaction call only), which would have left token/expiry generation and `emitAuditEvent` to be reimplemented or invoked separately at each call site — exactly the drift risk this extraction exists to prevent. The extracted function now owns the entire sequence.
- [x] 1.2 Update `POST /api/teams/:teamId/join-links` to call the extracted function; confirm its existing tests pass unmodified (pure refactor, no behavior change).
- [x] 1.3 Add a single shared definition of "active" for `join_links` rows: export a SQL fragment constant, `export const JOIN_LINK_ACTIVE_SQL = "revoked_at IS NULL AND expires_at > NOW()"`, from `packages/backend/src/auth/join-link-creation.ts` (alongside the creation helper from Task 1.1 — both are shared join-link business logic outside the route layer). This is the one definition Task 2.1's get-or-create SELECT and Task 2.6's redemption-check refactor both consume, per `specs/join-link/spec.md`'s "Get-or-create join link" requirement ("one shared definition, not two independently-maintained copies"). See design.md Decision 2's addendum.

## 2. Get-or-create join link

- [x] 2.1 Implement a get-or-create helper in `facilitator-sessions.ts`: `SELECT` the team's active `join_links` row using the shared predicate from Task 1.3 (`` `SELECT ... FROM join_links WHERE team_id = $1 AND ${JOIN_LINK_ACTIVE_SQL} ORDER BY created_at DESC LIMIT 1` ``, not a retyped copy of the condition), most-recently-created wins if more than one; on no active row, call the shared helper from Task 1.1.
- [x] 2.2 Wire `POST /api/v1/teams/:teamId/sessions/draft` to call get-or-create to source the response's `joinToken` field. This endpoint has exactly one call site for this wiring; `POST /api/v1/teams` (new-team creation) is a separate, independent endpoint and is out of scope for this task by design (Decision 1's note) — its `joinToken` response field is removed entirely rather than wired to get-or-create, handled by Task 4.2. `actor_global_role` is the `global_role` value already resolved and confirmed `=== "facilitator"` earlier in this handler (`facilitator-sessions.ts:214-241`) — no additional lookup needed. This task only repoints the response field's source: the INSERT's `join_token` column value is left untouched here (the column is still `NOT NULL` at this point) and is removed later in Task 4.2, gated on Migration A (Task 4.1).
- [x] 2.3 Wire `GET .../facilitator-state` to call get-or-create for its `joinToken` field, replacing the `sessions.join_token` SELECT. **Blocking gap closed per engineer and security review:** this handler has no `global_role` lookup anywhere today (it authorizes purely on `sr.facilitator_id !== userSession.userId`). Add `const actorResult = await db.query<{ global_role: string }>("SELECT global_role FROM users WHERE id = $1", [userSession.userId])`, paid only on get-or-create's miss path (i.e., only when the active-row SELECT from Task 2.1 finds nothing), matching the pattern already used at this file's other `actorResult`/`global_role` call sites (e.g. `/advance`, `/start`). Pass the resolved value as `actorGlobalRole` to the shared creation helper. Codified in `specs/join-link/spec.md`'s modified "Join link creation and redemption are durably recorded" requirement.
- [x] 2.4 Add tests: reuse of an existing active link (no new row created, and no `audit_log`/`join.link_created` emission on reuse); creation when none active; deterministic tie-break when multiple active rows exist; concurrent get-or-create calls both succeeding without a database error; `facilitator-state`'s miss-path `actor_global_role` lookup (Task 2.3) resolving correctly and only firing on a miss; `POST /draft`'s get-or-create call continuing to source `actor_global_role` from the value already resolved earlier in that handler, with no additional `global_role` SELECT issued (parity with the negative-condition test above for `facilitator-state`).
- [x] 2.5 Update two existing unit tests that break in this commit (not Migration B) because `facilitator-state`'s data source changes, independent of the column ever being dropped — missed by the original Migration B footprint, caught independently by both design reviews:
  - `facilitator-sessions.test.ts` (~lines 2206-2231, `describe("GET .../facilitator-state")`) — currently mocks the sessions SELECT with `join_token: "join-token-abc"` and asserts `body.joinToken` equals that literal. Update the mock to stage a `join_links` lookup resolution and assert against that value instead.
  - `http-session-expiry-no-partial-execution.test.ts` (~lines 150-163, the `#146` session-expiry-continuity regression test) — currently stages one `mockDbQuery` resolution with `join_token: "tok"`; get-or-create needs a second staged resolution (the `join_links` SELECT). Update the mock so this test continues to actually exercise get-or-create rather than failing on an unstaged call or silently passing without coverage.
- [x] 2.6 Reconcile `GET /api/join/:token`'s active check (`join-links.ts:143-183`) with the shared predicate from Task 1.3 instead of leaving it as two independently-written JS conditionals (`if (link.revoked_at)`, `if (new Date(link.expires_at) < new Date())`). Add `(revoked_at IS NULL AND expires_at > NOW()) AS is_active` — built from the `JOIN_LINK_ACTIVE_SQL` constant, not retyped — to this handler's existing token SELECT, and gate the rejection branch on `!link.is_active`. Keep the existing per-reason `revoked` vs. `expired` distinction for the audit log and redirect (`link.revoked_at` and the expiry comparison still determine *which* reason to report) — only the pass/fail gate itself is unified, not the reason-reporting detail. Add a test confirming both rejection reasons still resolve correctly through the refactored check, closing the BA-review blocking gap that get-or-create and redemption otherwise check "active" via two independently-maintained copies of the same rule.

## 3. Frontend: correct URL and badge copy

- [x] 3.1 Update `DraftSessionHost.tsx`'s join-link URL construction from `` `${window.location.origin}/join/${token}` `` to the actual registered backend route, `` `${window.location.origin}/api/join/${token}` ``.
- [x] 3.2 Replace the "(not yet joinable)" badge copy with: "This link works already — anyone who opens it before you open the room won't see a waiting screen yet."
- [x] 3.3 No lobby-specific caution UI is added (design.md Decision 5, revised after propose-stage BA review confirmed the `live-readiness-view` branch of `DraftSessionHost.tsx` has no join-link/copy-button element to place one near). Confirm in code review that `live-readiness-view` remains unchanged by this task set — the `draft`-stage badge (Task 3.2) is the only caution shipped.
- [x] 3.4 Update `DraftSessionHost.test.tsx` to assert on the corrected URL and badge copy; remove any assertion tied to the old `sessions.join_token` shape.

## 4. Remove `sessions.join_token` (split across two commits for rolling-deploy safety — design.md Decision 6)

**Commit 1 — Migration A (constraint relax only), lands with the behavior changes:**

- [x] 4.1 Write Migration A: `ALTER TABLE sessions ALTER COLUMN join_token DROP NOT NULL` — relax the constraint only, do not drop the column yet. This keeps the column safely readable/writable-or-omittable by both old-code and new-code pods during a rolling deploy (design.md Decision 6).
- [x] 4.2 Remove the `join_token` generation line and its stale comment, and both `INSERT` sites' `join_token` column/value, in `facilitator-sessions.ts` (existing-team draft creation, `POST /draft`, and new-team creation, `POST /api/v1/teams`). Safe once Task 4.1 lands, since the column no longer requires a value. **Additionally, for `POST /api/v1/teams` specifically** (design.md Decision 1's note): remove the `joinToken` field from this endpoint's `201` response entirely — verified unconsumed by the frontend (`SessionCreationPage.tsx`'s new-team handler reads only `{ teamId, sessionId }`; its test's new-team mock response has no `joinToken` field). This is a decision, not an oversight: after this task, the field would otherwise be a fabricated value with nothing writing it, the exact failure mode this column removal exists to close. Update `facilitator-sessions.test.ts`'s `expect(typeof body.joinToken).toBe("string")` assertion for this endpoint (~line 566) accordingly.
- [x] 4.3 Confirm the `facilitator-state` SELECT no longer references `join_token` (superseded by Task 2.3).
- [x] 4.4 Fold in stale type/doc references caught by engineer review, not schema-breaking but within this change's own "every code and schema reference removed" bar: `packages/shared/src/types/session.ts:19` (`Session.joinToken: string` — dead, nothing imports this type) and `packages/shared/src/types/team-content-access.ts:219-224` (`FacilitatorSessionStateResponse.joinToken`'s JSDoc, which names `sessions.join_token` and "not-yet-joinable" directly — both false after this commit).
- [x] 4.5 Land Tasks 1–3 (behavior changes), Task 3.4 (`DraftSessionHost.test.tsx`), Tasks 4.1–4.4, and Tasks 5.1–5.2 (end-to-end verification, design.md Migration Plan step 6) in the same commit. This commit does not touch the five backend test files below — Migration A does not break them, since the column still exists.

**Commit 2 — Migration B (drop the column), a follow-up commit only after Commit 1's release is confirmed fully rolled out (zero pods remaining on pre-#166 code):**

- [x] 4.6 Write Migration B: `ALTER TABLE sessions DROP COLUMN join_token` (drops `sessions_join_token_unique` and `idx_sessions_join_token` automatically — no separate constraint/index drop statements).
- [x] 4.7 Update `ws-pubsub-integration.test.ts`: remove `join_token` from both `INSERT INTO sessions` statements.
- [x] 4.8 Update `facilitator-error-state-2-restricted-role.test.ts`: remove `join_token` from its `INSERT INTO sessions` statement.
- [x] 4.9 Update `facilitator-error-states-integration.test.ts`: remove `join_token` from its `INSERT INTO sessions` statement.
- [x] 4.10 Update `action-items-integration.test.ts`: remove `join_token` from its `INSERT INTO sessions` statement.
- [x] 4.11 Update `facilitator-sessions.test.ts` (test case 2.18, an existing test-numbering label internal to that file from a prior change — not this document's own Section 2): remove or rewrite the assertion on the literal constraint name `"sessions_join_token_unique"` against a still-existing constraint, since that constraint no longer exists after Task 4.6.
- [x] 4.12 Land Tasks 4.6–4.11 in the same commit — the migration and the test updates cannot be split without breaking CI on invalid SQL. `DraftSessionHost.test.tsx` (Task 3.4) and the `facilitator-state`/`POST /api/v1/teams` test updates (Tasks 2.5, 4.2) are deliberately not part of this commit — they landed in Commit 1 with the code they test, and have no dependency on the column actually being dropped (design.md Decision 6).

## 5. End-to-end verification

- [x] 5.1 Add (or update) an integration test that renders/drives the draft-session control view, captures the exact URL string it constructs and displays for copying, and follows that string against the running backend to confirm redemption succeeds — not a direct `GET /api/join/:token` call seeded with an independently-known-good token and path.
- [x] 5.2 Confirm this test would have failed against the pre-fix code (both the dead `sessions.join_token` and the `/join/:token` vs `/api/join/:token` path mismatch) — run it against a stashed pre-fix checkout or reason through why it exercises both bugs at once.

## 6. Documentation of scope boundary

- [x] 6.1 Confirm `design.md`'s Decision 5 (ship #166 now, ahead of #164, relying on the `draft`-stage badge alone with no lobby-specific caution added — Task 3.3) is reflected in the merged change, so a reviewer can see the #164 gap was a deliberate, named choice rather than an oversight.
- [ ] 6.2 File or update issue #164 so it carries a scope and a timeline, per the exec review's condition that the `draft`-stage badge is an interim stopgap, not a permanent substitute for a working `lobby`-state landing. Not a task of this change's implementation itself, but a follow-up commitment that should exist before or shortly after this change ships.
- [x] 6.3 Note (accepted, non-blocking, per BA task review): `specs/join-link/spec.md`'s "No duplicate creation logic" scenario is satisfied structurally — Task 1.1's shared creation helper has exactly one call site per caller (Task 1.2, and Task 2.1's miss branch) — and is enforced by code review, not an independent runtime test. This is a deliberate choice, not an oversight; no task adds a runtime assertion for it.
